# Issue #178 implementation lane — owner decision required

Refs #178, #176, #156. Inspected base: `127ea44abd121a33baa3182ef0fb0cb243ef752a`.

## Verdict and delivered scope

BLOCKED on the owner-gated merge contract in #176. This is a diagnostic/report
only, not an implementation of bounded reconciliation. No production, test,
policy, capability, instance-state, or schema change is delivered. Issue #178
is NOT complete; the positive merged-head witness (a) is not delivered.

The brief explicitly forbids deciding #176 unilaterally. At the pinned base,
meeting (a) requires changing the landing and verification contracts, not just
retrying a failed git operation:

- `src/plan.rs:268-274` authors `p7` with `merge_policy: "squash"`.
- `src/mutation.rs:3125-3242` implements a READ-ONLY rehearsal. Success says
  `mode: "rehearsal"`, `landed: false`, with no `merged_head`. It does not
  execute `git merge --ff-only` (the older description in #176 is no longer
  the current implementation). The actual policy merge is explicitly owned
  by the orchestrator/forge, also documented in
  `docs/contracts/spec-plans.md:51-64`.
- `src/mutation.rs:3244-3281` still requires exact ancestry of the reviewed
  feature head in `post_merge_verify`. A genuine squash landing with an
  identical result tree is not that ancestry proof.
- `tests/mutation_engine.rs:1329-1370` expressly requires the merge step to
  leave the integration head, all refs, remote, operator index, and operator
  edits untouched for BOTH policies. The existing positive tail test at
  lines 1234-1243 relies on an explicit fixture-side ff merge. It is not
  evidence of an autonomous merge and must not be presented as witness (a).
- `src/supervision.rs:930-958` dispatches only a never-attempted frontier;
  a recorded failed/refused attempt becomes `supervision.step_diagnosed`.
  Repeating that classification cannot reconcile or land a branch.
- The selected-issue spine has `p7 merge -> p8 cleanup`
  (`src/plan.rs:268-283`), not an authored `post_merge_verify` step. Calling
  the separate verifier in a test does not establish its reachability in
  the actual selected-issue tail.

The minimal owner decision is whether to retain squash and specify a
policy-aware, independently re-certified landing proof, or to change the
plan/policy contract to an ancestry-preserving merge. Landing authority
(daemon effect versus orchestrator/forge) must also be explicit. Neither is
selected in this lane. A subsequent authorized implementation must bind the
reconciled candidate and fresh independent evidence to the fetched published
base, retain a bounded retry budget, and prove the actual tail without a
fixture-side merge standing in for product behavior.

No live acceptance state, daemon, database, or run worktree was read or
modified. No run was dispatched, paused, retried, released, or resolved.
Synthetic fixtures only were exercised. No cap widening, gate bypass,
instance-state weakening, or real integration merge was performed.

## Witness table

| Case | Probe / mutation | Observed RED | Restored GREEN | Raw exits |
| --- | --- | --- | --- | --- |
| (a) behind-published delivery merges through bounded retry and passes verifier | NOT IMPLEMENTED: blocked on #176. A narrower diagnostic appends the real `v1` verifier call to the existing already-squashed fixture. | `refusal.evidence.stale`: reviewed feature is not an ancestor, despite equal result trees and a readable published ref. One assertion fails. | Original rehearsal-only test passes after removing the added verifier assertion. This is NOT a GREEN for (a). | diagnostic 101; original test 0 |
| (b) stale or unverifiable published view refuses | Existing two real-daemon/local-bare-origin tests, unchanged | Initial cold run has a transport failure, not a valid semantic RED (details below). | Stale published head, absent published ref, and unreadable origin all refuse in the unchanged tests. | warm baseline 0; 2 passed |
| (c) removing published-ref verification breaks (b) | In `published_integration_head`, replace `git ls-remote origin refs/heads/<integration>` with `git rev-parse --verify <integration>` | Both selected tests fail `expected refused response`, receiving `ok:true`, `mode:"rehearsal"`, `landed:false` instead. | Exact original source restored; both selected tests pass. | mutant 101; restored 0 |

The unverifiable test contains two sequential routes. Its mutation RED occurs
at the first (absent-ref) assertion, so the unreadable-origin route is NOT
claimed as independently mutation-proven. That second route DOES execute in
the baseline/restored GREEN. The stale-head test independently fails under
the same mutant. Neither RED is a compile error.

### Squash/ancestry blocker reproduction

The existing `cycle2_reviewed_merge("squash", true)` fixture creates and
publishes a synthetic squash, then records fresh synthetic PASS evidence.
The existing test successfully invokes `m1` but omits `v1`. The diagnostic
adds, immediately after its successful `m1` call:

```rust
scenario.apply_ok(16, "v1", Some(&feature), Some(&base));
```

The diagnostic also logs `git ls-remote origin refs/heads/staging` and both
tree IDs. Actual output from `04-squash-post-verify-red.log`:

```text
ISSUE178 published=901f6de00e0a93a701d8f21990990a097e9adfd5 refs/heads/staging
feature=f270d6d070268c099d331a71696d5f7f1afa7bd5 integration=901f6de00e0a93a701d8f21990990a097e9adfd5 feature_tree=3b8e85233a562b148937a6d1523dff899a84a952 integration_tree=3b8e85233a562b148937a6d1523dff899a84a952
assertion `left == right` failed: apply v1 (seed 16) failed: {"error":{"code":"refusal.evidence.stale","message":"feature head \"f270d6d070268c099d331a71696d5f7f1afa7bd5\" is not an ancestor of the integration branch"},"ok":false}
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 22 filtered out; finished in 5.20s
```

The tab between the remote head and ref above is rendered as a space. The
fixture's synthetic merge is ONLY a negative verifier reproduction; it does
not stand in for autonomous reconciliation or claim witness (a). The probe
is removed rather than committing a new test that blesses the defect.

### Published-view mutation and byte restoration

Only the command array in `published_integration_head` was substituted:

```rust
// Original:
"ls-remote", "origin", &format!("refs/heads/{}", ctx.integration_branch),
// Mutant (trust the local view, not the published ref):
"rev-parse", "--verify", ctx.integration_branch,
```

Actual output from `05-local-view-mutant-red.log`:

```text
failures:
    cycle2_rehearsal_refuses_an_unprovable_published_base_on_both_routes
    cycle2_rehearsal_refuses_when_the_published_integration_ref_moved

test result: FAILED. 0 passed; 2 failed; 0 ignored; 0 measured; 21 filtered out; finished in 8.19s
```

Both failures are assertions at `tests/mutation_engine.rs:323`, not crashes
or compiler errors. Their successful responses contain the stale local
`integration_head`. The restored run records:

```text
test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 21 filtered out; finished in 7.41s
```

Every substitution was enclosed in `try/finally`, backed up outside the
checkout, and restored byte-for-byte with a fresh mtime before its GREEN
run. `git diff --exit-code -- src tests` returned 0 after both probes.

| Leg | Mutated file SHA-256 | Restored file SHA-256 (also used by restored GREEN) |
| --- | --- | --- |
| Verifier diagnostic, `tests/mutation_engine.rs` | `5bb19683f9ac5584657bc4b7f6e70b849ba3f6230d16e9a274094780094f37c1` | `d60d39ac87e9a790f0a96cedab68d56f009f1788139f81d668ecbda0a3ce0685` |
| Published-view mutant, `src/mutation.rs` | `80023a8cd0051031b60ffdc89fe06840b96ccb87fed5f360c68a5b1e51ac4602` | `3269efa541efc7d6fb4264946d298d05f60c938126225e570ebbf3c34a688a96` |

The restored hashes are also the baseline file hashes. No new production
predicate was introduced, so there is no claimed mutation proof for a new
retry/re-certification implementation.

## Exact commands, raw exits, and measured durations

`just --list` was run once (exit 0). Tests/gates ran from the assigned lane
with `CARGO_TARGET_DIR=/tmp/impl-178-target` and `RUST_TEST_THREADS=1`. A
stdlib subprocess wrapper redirected each command's complete stdout/stderr
to the named log, retained the raw child status, and imposed a deadline.
Durations below are measured wall seconds including compilation unless
explicitly called test-body duration. No verdict is a pipeline/filter exit.
Local raw logs and JSON receipts are in `/tmp/impl-178-evidence/`.

Command A:

```text
cargo test --locked --test mutation_engine cycle2_rehearsal_refuses -- --test-threads=1 --nocapture
```

Command B:

```text
cargo test --locked --test mutation_engine cycle2_squash_rehearsal_succeeds_after_the_policy_squash -- --test-threads=1 --nocapture
```

Command C:

```text
python3 scripts/ci-test-driver.py --suite mutation_engine --aggregate-seconds 180 --per-suite-seconds 150 --log-dir /tmp/impl-178-evidence/mutation-engine-budget
```

| Named log | Command / source | Raw exit | Wall seconds | Actual result |
| --- | --- | --- | --- | --- |
| `01-published-baseline.log` | A, unchanged base; 300-second wrapper deadline | 101 | 183.216 | 1 passed, 1 failed: `client.read`, `Resource temporarily unavailable (os error 35)` |
| `02-published-baseline-warm.log` | A, same unchanged base; 180-second deadline | 0 | 8.999 | 2 passed, 0 failed |
| `03-just-ci.log` | `just ci`, unchanged production/tests; 1500-second deadline | 0 | 1007.353 | 42 Rust result blocks, 741 passed, 0 failed; docs/release/security completed |
| `04-squash-post-verify-red.log` | B, added verifier assertion; 180-second deadline | 101 | 7.437 | 0 passed, 1 failed: ancestry assertion |
| `04-squash-post-verify-restored.log` | B, original test restored; 180-second deadline | 0 | 4.796 | 1 passed, 0 failed; rehearsal only |
| `05-local-view-mutant-red.log` | A, local-ref substitution; 180-second deadline | 101 | 11.434 | 0 passed, 2 failed: refused-response assertions |
| `05-local-view-mutant-restored.log` | A, source restored; 180-second deadline | 0 | 9.068 | 2 passed, 0 failed |
| `06-mutation-engine-budget.log` | C, fully restored files; 200-second outer deadline | 0 | 64.199 | 23 passed; driver suite duration 64.0 s, survivors 0, timeout false |

The first run compiled in 1m49s, then one daemon RPC timed out. The unchanged
warm rerun passed. An intervening host observation recorded load averages
114.52/92.51/88.81 and 7.60% CPU idle. This initial failure is disclosed as a
transport failure under host contention, not counted as a mutation RED or
hidden by the subsequent pass.

Full-gate output includes:

```text
Ran 21 tests in 3.595s
OK
advisories ok, bans ok, licenses ok, sources ok
no leaks found
```

Suite measurements at unchanged/restored source:

- `mutation_engine`: 23 tests, 36.72 s test-body time inside `just ci`;
  64.0 s through the actual 150-second CI driver (including warm compilation
  and survivor accounting); final survivor sweep `found=0 remaining=0`.
- `supervision`: 26 tests, 131.60 s test-body time inside serialized `just ci`.
  It was NOT edited. This is not a separate hosted-driver budget proof.
- `supervision_dispatch_refusal`: 4 tests, 78.81 s test-body time inside
  serialized `just ci`; not edited.

The full aggregate ran exactly once, before source substitutions. Restored
focused tests and the complete mutation-engine driver ran afterward. No
runtime code is delivered that differs from that aggregate's source.

## Structural search evidence

These exact focused structural commands both returned exit 0 and one match:

```text
ast-grep run --lang rust --pattern 'fn effect_merge($$$ARGS) $$$BODY' --json=compact src/mutation.rs
ast-grep run --lang rust --pattern 'fn effect_post_merge_verify($$$ARGS) $$$BODY' --json=compact src/mutation.rs
```

Matches: `src/mutation.rs:3128-3242` and `src/mutation.rs:3246-3281`,
respectively. Earlier structural maps located the existing rehearsal tests
and the diagnosed-frontier condition; they were followed by narrow reads.
The initial map also named nonexistent `src/merge.rs` / `tests/merge.rs`,
and another named nonexistent `src/run.rs`; those paths emitted errors and
were not used as evidence. The verified functions are in `src/mutation.rs`.

## Delivery and explicit non-claims

The only delivered file is `.report-178.md`. The pinned base remained
`127ea44abd121a33baa3182ef0fb0cb243ef752a`; fetching `origin/staging` did not
change it. No branch refresh, rebase, or source fix was made. The final
handoff supplies the locally committed/pushed head and exact remote read-back.
No PR was opened; no issue was mutated; no push to staging was performed.

Not exercised or not delivered:

- Bounded reconciliation/retry and fresh independent re-certification of a
  moved-base candidate, or a product-owned merged integration head: (a) FAIL /
  owner-blocked, not a partial implementation presented as success.
- A policy-coherent squash `post_merge_verify`: the diagnostic reproduces
  its failure, not its repair.
- Automatic bounded retry of the refused `p6` mentioned in the issue.
- Any live acceptance-run recovery, actual forge merge, or hosted CI run.
- Mutation proof for an unreadable origin separately from the absent-ref
  route (the baseline/restored test covers both; the mutant stops at the
  first assertion).

Owner adjudication of #176 is required before the runtime change can be
implemented without silently changing merge policy or weakening verification.
