# Lane report — canter #261: a re-drive arms what it admits, and an operator control recovers an inert admitted run

Lane `issue-261` (the acceptance run's own worktree), branch `issue-261`, delivered as a PR to `staging`.

| | |
| --- | --- |
| Base started from | `592acfec6c556ed9e1edd6c81765389386fbe7ff` (= `origin/staging`, the run's recorded `issue_revision`) |
| Content commit | `48da6d20b4e584ba5f8f0689562c59a6dc11ee43` |
| This report | a CHILD commit of the content commit (a commit cannot name its own sha); the branch tip carries both |

## The defect (as the issue measured it on the live host)

`redrive_queue_submission` (`src/state.rs`) walked a submission's parked (`waiting`) items and
called `admit_queue_item_in_tx`. The submit path arms supervision in the SAME transaction as
the runs it names (`arm_supervision_in_tx`, `src/state.rs`), but the re-drive path never did:
the run it admitted carried **no supervision row at all**, nothing could drive it, and it held
one of the per-repository counted slots (`counted_lane_footprints`, instance statuses
`new`/`running`/`human_queue`/`blocked`) forever. Releasing the inert runs by hand was the only
remedy, because no operator surface could arm a run afterwards.

## The policy implemented (issue #261 "Required fix" 1), and why

**Arm what you admit — with the authorization the submission itself committed; refuse the whole
control typed when the submission has none.**

- The arming source, in order, is ONE fact: (1) the authorization block
  (`params.supervision`) of the submission's own committed `queue.submit` request — the row
  the operator presented `--supervise arm` on, and the same row a later admission already reads
  its dispatch context from; (2) failing that, the supervision row of an already-admitted run of
  the same submission — the SAME authorization the submit path armed that run with (the same
  substitution the #96 advance makes from the delivering run's row). No policy value, interval
  or deadline is ever invented by the caller.
- The admitted run is armed with the SAME bindings the submit path uses: the submission's
  approved digest (`authorization_digest`), its boundary phase (`approved_boundary`) and its
  state epoch (`run_generation`).
- A submission that committed **no `armed` authorization** (supervision disabled: the documented
  default) refuses the WHOLE re-drive typed — `refusal.queue.supervision_unarmed` — BEFORE any
  item is admitted, naming the missing authorization and the remedy (re-submit the parked items
  with `--supervise arm`). **The control never admits an inert run**: admitting one would convert
  the transient cap hold it exists to repair into a permanent one, which is exactly the defect.

**The bounded, audited operator control (requirement 2):** `supervision arm --run RUN_ID`
(new RPC method `supervision.arm`, document `hf-supervision-arm/v1`). It arms EXACTLY ONE
already-admitted run that carries no arming authorization, re-applying the authorization the
run's OWN committed submission presented (same digest/boundary/epoch binding), and commits the
arm together with its hash-chained `supervision.arm` audit row in ONE transaction (one
idempotency key = one effect). Typed refusals: `refusal.supervision.terminal_run` (a terminal
run), `refusal.supervision.already_armed` (a run whose row is `armed` — it is already driven),
`refusal.supervision.unbound` (no committed submission admitted the run), and
`refusal.supervision.unarmed` (the submission committed no `armed` authorization — arming would
invent one the operator never gave).

**Surfaced (requirement 3 / AC4):** `supervision.status` of an ADMITTED run that carries no
supervision row answers `hf-supervision/v1` with `supervision.state: "unarmed"`,
`evaluation.reason: "supervision.unarmed_inert"`, `eligible:false`, the detail ("nothing will
drive this run's next step and it holds its counted slot until it is armed or released") and the
remedy (`supervision arm --run …`) — so "admitted, unarmed" is distinguishable from "armed,
waiting" BEFORE the slot blocks a re-admission. An unknown run and a run no submission admitted
keep `state.not_found`.

No behavior change to the dispatch path; no new dependencies; no gate weakened.

## The change (content commit)

| file:line | change |
| --- | --- |
| `src/state.rs` (`submission_arming_authorization_locked`) | the arming source: the submission's committed `queue.submit` authorization block, else an already-armed admitted run of the same submission; `None` = the submission committed no `armed` authorization |
| `src/state.rs` (`redrive_queue_submission`) | the typed whole-control refusal when the arming source is absent; `arm_supervision_in_tx` in the SAME transaction for every `AdmissionDecision::Admitted` run, with the submission's digest/boundary/epoch |
| `src/state.rs` (`arm_admitted_run` + `SupervisionArm`) | the bounded, audited arm control's state transaction: run read, terminal/already-armed fences, admitting-submission link, re-applied authorization, `supervision.arm` audit row, ONE commit |
| `src/state.rs` (`admitted_submission_of`) | the admitting-submission read the unarmed read uses |
| `src/state.rs` (`supervision_wake_class`) | `supervision.arm` is a control-class wake, so the run's own driver checks it |
| `src/queue_executor.rs` (`codes::SUPERVISION_UNARMED`) | `refusal.queue.supervision_unarmed` |
| `src/supervision.rs` | `codes::ARM_*` / `codes::UNARMED_INERT`, `parse_arm_params`, `arm_params`, `arm_doc` (`hf-supervision-arm/v1`), `unarmed_doc` |
| `src/daemon.rs` | `supervision.arm` dispatch + `method_supervision_arm`; the `supervision.status` unarmed read |
| `src/schema.rs` | `supervision.arm` joins the closed RPC method set |
| `src/commands.rs` | `supervision arm` parsing/usage/executor; the surface doc amended (status stays read-only; arm is the ONE bounded control) |
| `docs/contracts/spec-daemon.md` | the normative contract for all of the above |
| `tests/queue_submit.rs` | witnesses below (one existing re-drive test updated to the issue's own policy: its submission committed no authorization, so the re-drive now refuses typed) |

## Witnesses (raw, named logs)

Library-level (`State`) over the product's own API, synthetic identities only:

| # | Witness | Log | Raw exit |
| --- | --- | --- | --- |
| W1 | `a_re_drive_arms_what_it_admits_with_the_submissions_own_authorization` — the parked item of an ARMED submission is admitted by the re-drive AND armed with the submission's own digest/boundary/epoch/policy; the driver's own reconciliation (`supervision::check_plan` + `commit_supervision_check`, the daemon driver's two calls) then commits a check for it with zero operator dispatch; exactly one `queue.redrive` audit record | `/tmp/c261-logs/focused-2.log` | ok |
| W2 | `a_parked_submission_is_re_driven_by_the_operators_bounded_control` (updated) — a submission with **no** arming authorization refuses the whole re-drive `refusal.queue.supervision_unarmed`, deterministic across a second invocation, creates NO run, flips no item and writes no `queue.redrive` record | `/tmp/c261-logs/focused-2.log` | ok |
| W3 | `the_operator_arm_control_recovers_an_inert_admitted_run_and_is_audited` — an admitted, explicitly-disabled (inert) run is armed by the control; the row reads `armed` with the submission's digest; exactly ONE `supervision.arm` audit record; a second arm of the same run refuses `refusal.supervision.already_armed` | `/tmp/c261-logs/focused-2.log` | ok |
| W4 | `the_arm_control_names_each_run_it_refuses` — an unarmed submission's run refuses `refusal.supervision.unarmed`; a terminal run refuses `refusal.supervision.terminal_run`; a malformed target refuses `usage.supervision.target` | `/tmp/c261-logs/focused-2.log` | ok |
| W5 | `the_arm_control_refuses_typed_on_every_run_that_must_not_be_armed` (daemon-level: the real CLI against the fixture daemon) — `supervision arm` on a run with no arming authorization exits non-zero with `refusal.supervision.unarmed` and writes no row | `/tmp/c261-logs/supervision-arm-1.log` | ok |
| W6 | `supervision_is_disabled_by_default_and_a_foreign_target_refuses` (daemon-level, updated to the #261 AC4 contract) — an admitted run with no supervision row reads `supervision.state: "unarmed"` / `evaluation.reason: supervision.unarmed_inert` / `eligible:false` across two reads, and no row is ever invented | `/tmp/c261-logs/supervision-arm-1.log` | ok |

## Gates (raw exits; the repo's own recipes)

| # | Command | Log | Raw exit |
| --- | --- | --- | --- |
| G1 | `cargo fmt --check` | `/tmp/c261-logs/focused-3.log` | 0 |
| G2 | `cargo test --locked --test queue_submit` | `/tmp/c261-logs/focused-3.log` | 0 (19 passed / 0 failed) |
| G3 | `cargo clippy --locked --all-targets -- -D warnings` | `/tmp/c261-logs/clippy-1.log` | 0 |
| G4 | `RUST_TEST_THREADS=1 just ci` — the headline aggregate (fmt-check → check → lint → test → doc → build-release → security) | `/tmp/c261-logs/ci-2.log` | 0 — 50 suites, **881 passed / 0 failed**; `security`: scanner self-test 21/21 OK, public-tree scan clean, `cargo deny` advisories/bans/licenses/sources ok, `cargo audit` 119 crates, `gitleaks` `no leaks found` |
| G5 | `cargo test --locked --test supervision` at the delivered tip (adds W5) | `/tmp/c261-logs/supervision-arm-1.log` | 0 (26 passed / 0 failed) |

G4 ran at `c4d6a4b` (one commit below this report's tip); the tip adds ONE daemon-level
witness to that same suite and the report text — no production code — and G5 is its green
run. A later round can re-run the aggregate at an even later head if it moves again.

## Mutation RED/GREEN (the restored blob's sha256 proved == `HEAD`)

| # | Mutation | Witness | RED | Restored → GREEN | Restored sha256 |
| --- | --- | --- | --- | --- | --- |
| M1 | the `arm_supervision_in_tx` call removed from `redrive_queue_submission` (the arm the issue asks for, absent) | W1 | `exit=101`: `panicked at tests/queue_submit.rs:2182: the re-driven run is ARMED — never an inert admission` (`/tmp/c261-logs/mut-1-red.log`) | `exit=0` — 19 passed / 0 failed (`/tmp/c261-logs/mut-1-green.log`) | `src/state.rs` = `ea1b0b7fa960ce0bd9cd39aece3585ca55f06a3ce44995e760503ea497bcaf56` == `git show HEAD:src/state.rs` |

M1 is the AC1 proof that the new witness BITES: with the arm removed the parked item is
still admitted (the pre-fix shape) and the witness fails on exactly that — an inert
admission is caught, not tolerated. The mutation was never committed; the restored bytes
are byte-identical to the committed blob.

## NOT EXERCISED

- No live canter run was driven on this host (the lane does not drive its own run); the
  re-drive is witnessed at the library level over the product's own `State` API, and the daemon
  RPC surfaces (the `supervision.arm` refusal + the unarmed `supervision.status` read) by W5/W6
  against a spawned fixture daemon and the real CLI binary.
- A daemon-level POSITIVE arm (an inert run whose submission IS armed) is only constructible
  from pre-fix history: every admission path now arms, so the fixture cannot build that state
  without hand-editing durable rows. It is witnessed at the library level (W3), including the
  audited row; the daemon leg covers the refusal + the read.
- The `queue status` per-item supervision annotation (the second half of requirement 3's
  "`queue status` / `supervision status`") is served by the `supervision status` read; the queue
  document itself was left as shipped.
- The real `openssl`/`gh`-dependent release legs were not exercised beyond what `just ci`
  covers.
