# .report-279-r2.md — canter #279's acceptance run: the stored pre-#269 binding is witnessed live

Lane: `issue-279` — canter queue submission `qs_06e5dd78637691a1`, item
`jirathip-dev/canter#279`, run `run-028cf38986310446`, worktree scope
`worktrees/issues/279`. Pinned revision (the submission's `issue_revision`):
`1392f973061a779bc88484935c7891d72658a677` — identical to the lane worktree
HEAD and to `origin/staging` at the time of writing. This PR's delta is this
report only: the issue's required fix is already implemented, merged and
installed at the pinned revision, so the honest delivery record is the LIVE
acceptance evidence the first lane expressly deferred (§1). No production code
is changed; `Refs #279`.

## 1. Preflight: the fix is contained at the pinned revision

- Delivery: PR #281 "A stored pre-#269 admission binding dispatches again
  (Refs #279)" — head branch `impl-279`, pushed head
  `e1a2839a7aaf32d2de4d9cec0907aed399a37d1f`, merged to `staging`
  2026-09-25T01:09:29Z as the squash merge commit
  `6bb321d647a15f7cc063a100c58ea273f5857e44` (single parent `d4b77f4`).
- `git merge-base --is-ancestor 6bb321d647a15f7cc063a100c58ea273f5857e44 HEAD`
  — raw exit `0` (contained).
- `git log --oneline origin/staging..HEAD | wc -l` — `0` before this report's
  own commit (the branch carried nothing of its own).
- `git diff 6bb321d...:src/config.rs HEAD:src/config.rs` — empty: the fix arm
  is still byte-identical to the merged one, and no later commit touched
  `src/config.rs`. The file's sha256 equals the green blob the first lane
  recorded (`685150e54e5a70a6d326efd4d0ac5f630e58916ddf36af34a464082ff6ddc25e`),
  and all three #279 test entry points are present at HEAD (`src/config.rs`,
  `src/mutation.rs`, `tests/supervision_dispatch_refusal.rs`).
- The first lane's committed evidence `.report-279.md` §6 names what it could
  not prove: "the live audit rows after the change, their supervision class
  transition, and the live cap freeing (AC1/AC3 are the acceptance run's to
  witness)". This lane IS that acceptance run. §2 and §3 below are those
  witnesses, taken read-only (SQLite `mode=ro`; `canter run status`,
  `canter supervision status`, `canter queue preview`, `canter queue status`
  reads). Nothing was released, retried or dispatched by this lane, and no
  operator control ran against any run.

## 2. AC1 — the two stored-binding runs' own supervision dispatches again

The defect's witness is the runs' OWN dispatch of their parked steps. Read-only
audit rows (durable store), before and after:

```
1690 | 2026-09-25T01:08:23Z | supervision.dispatch_refused | run-3c68e508443d62c0:p6-258:refusal.profile.binding:reason:refusal.profile.binding: the profile binding requires skills (an array; may be empty)
1691 | 2026-09-25T01:09:45Z | supervision.dispatch_refused | run-b57a1cabee4f3774:p6-261:refusal.profile.binding:reason:refusal.profile.binding: the profile binding requires skills (an array; may be empty)
1692 | 2026-09-25T01:11:00Z | supervision.dispatch_refused | run-3c68e508443d62c0:p6-258:refusal.profile.binding:reason:refusal.profile.binding: the profile binding requires skills (an array; may be empty)
1693 | 2026-09-25T01:11:01Z | supervision.dispatch_refused | run-b57a1cabee4f3774:p6-261:refusal.profile.binding:reason:refusal.profile.binding: the profile binding requires skills (an array; may be empty)
1695 | 2026-09-25T01:11:19Z | mutate.review_evidence                 | jirathip-dev/canter:run-3c68e508443d62c0:p6-258
1699 | 2026-09-25T01:11:21Z | mutate.review_evidence                 | jirathip-dev/canter:run-b57a1cabee4f3774:p6-261
1701 | 2026-09-25T01:11:22Z | outcome.mutate.review_evidence          | ik_run-b57a1cabee4f3774-p6-261-1790298681
```

`1690`–`1693` are the last `refusal.profile.binding` rows for either run; the
next rows record the runs' own supervision dispatching exactly those steps
(`1695`/`1699`, the `review_evidence` apply of `p6-258`/`p6-261`), with no
operator control between them.

**The held retries were SPENT by that dispatch** — the remedy the issue proved
"cannot complete" now does, and the whole bounded budget burns:

```
run-3c68e508443d62c0  p6-258  attempt 1  rt_a7d0e073eb85e3b2  authorized 2026-09-23T10:43:38Z  consumed 2026-09-25T01:11:19Z
                                attempt 2  rt_410ac7fb002d6615  authorized 2026-09-25T01:12:40Z  consumed 2026-09-25T01:12:40Z
                                attempt 3  rt_6d9ee1d36b3315a8  authorized 2026-09-25T01:15:17Z  consumed 2026-09-25T01:15:17Z
run-b57a1cabee4f3774  p6-261  attempt 2  rt_c6946cb6d6b0e504  authorized 2026-09-24T03:33:50Z  consumed 2026-09-25T01:11:21Z
                                attempt 3  rt_9d79114fe98224df  authorized 2026-09-25T01:12:56Z  consumed 2026-09-25T01:12:56Z
```

Both held authorizations had sat unconsumed for a day and a half before the fix
reached the running installation, and the same supervision then re-drove each
step twice more (`01:12:40`/`01:12:56`, `01:15:17`); with the fix not yet
installed every one of those dispatches was the refused one. The runs' own
records say so: `canter run status --run …` (raw exit `0` each) reports the
`p6` step's last failure as exactly the fix's own refusal code before the
change, and `canter supervision status --run …` (raw exit `0` each) shows the
attempt cursors ending in the runs' NEXT parked state (§6).

Zero operator control across the advance — audit rows for either run since the
merge, by action (the `supervision.dispatch_refused` rows here are the runs' own
recorded refusals; they include later cap-contention rows unrelated to the
binding):

| action | rows |
| --- | --- |
| `mutate.review_evidence` | 151 |
| `outcome.apply` | 146 |
| `host.proof.renewal` | 142 |
| `supervision.dispatch_refused` | 12 |
| `outcome.mutate.review_evidence` | 5 |
| `grant.rotation` | 2 |
| any `run.dispatch` / `run.retry` / `run.release` / `run.resolve` / `run.reevaluate` / `run.retire-lane` row for either run | **0** |

## 3. AC3 — the repository slot is free, with zero operator control

`canter queue preview --repository jirathip-dev/canter --harness … --host mac
--issue 279=1392f973061a779bc88484935c7891d72658a677 --caps 8/3/2 --json`
(raw exit `0`; effect-free read) — the admission read an operator would take:

- `concurrency.caps` = `{global 8, repository 3, harness 2}`;
  `concurrency.running` = `{repository: 1, total: 1}` — the counted set for
  this repository is ONE lane (`jirathip-dev/canter#279`,
  `run-028cf38986310446`, scope `worktrees/issues/279`): two repository slots
  are free, and the read carries NO `refusal.admission.cap_repository` hold.
  The only holds are this bare preview's own attestation holds
  (`preview.host_unavailable`, `preview.occupancy_unknown`), which are about
  host/occupancy attestation, never about the cap or the runs' bindings.
- The two former dead runs hold no counted slot: both now classify as terminal
  by outcome (`canter run status --run …`, raw exit `0` each):
  `escalation.run.owner_decision` — "parked on `refusal.run.retry_bound` … its
  bounded-retry budget is spent (3 of 3 used) … the run is not an eligible
  park". No `run release`, no `run retire-lane`, no state edit was used to get
  there, and zero `run.*` operator rows exist for either run (§2).
- The submission's own approved admission document re-renders bit-identically
  at this revision: the preview's `digest` equals the submission's recorded
  `digest` `1a327467fb10e58f50459dbfadbb334b7731623cfe588e07b303bc277f0a9f9d`.
- A fresh run for this repository WAS admitted after the fix: this lane's own
  submission (`qs_06e5dd78637691a1` → `run-028cf38986310446`), and
  `canter queue status --submission qs_06e5dd78637691a1` reads
  `admission: {admitted: 1, refused: 0, waiting: 0}` (raw exit `0`).

## 4. AC2/AC4 re-verified at the pinned revision

The #279 behavioural witnesses, re-run at the pinned revision
(`CARGO_TARGET_DIR` lane-local; every raw exit captured before its log was
read):

| Command | Raw exit | Result |
| --- | --- | --- |
| `cargo test --locked --lib a_stored_pre_269_binding_is_dispatchable_and_present_values_stay_strict` | `0` | `test result: ok. 1 passed; 0 failed` |
| `cargo test --locked --lib a_stored_pre_269_reviewer_binding_passes_the_review_contract` | `0` | `test result: ok. 1 passed; 0 failed` |
| `cargo test --locked --test supervision_dispatch_refusal a_stored_pre_269_binding_is_dispatched_by_the_run_s_own_supervision -- --exact` | `0` | `test result: ok. 1 passed; 0 failed` (8.05s) |

Discrimination probe — does the arm bite at THIS revision? The absent-`skills`
arm was replaced by the pre-fix refusal (literal replace asserted to match
exactly once; `src/config.rs` sha256
`685150e54e5a70a6d326efd4d0ac5f630e58916ddf36af34a464082ff6ddc25e` →
`6ff2e4ee96d1efaa9e722babaa879352a016982c706e7314fb10596398655f02`), the same
three tests then FAIL with the typed refusal, and the restored file's sha256
equals the pre-mutation value again, with the post-restore GREEN run recorded:

```
RED  cargo test --locked --lib a_stored_pre_269_…strict  -> exit 101
     a stored pre-#269 binding stays dispatchable: Binding("the profile binding requires skills (an array; may be empty)")
RED  cargo test --locked --lib a_stored_pre_269_…contract -> exit 101
     a stored pre-#269 reviewer binding is accepted: EffectOutcome { status: "refused",
     code: Some("refusal.profile.binding"), message: Some("the profile binding requires skills (an array; may be empty)"), … }
RED  cargo test --locked --test supervision_dispatch_refusal … --exact -> exit 101
     the run's own supervised dispatch was refused: [("run-…:p1:refusal.profile.binding:reason:refusal.profile.binding:
     the profile binding requires skills (an array; may be empty)", "2026-09-25T17:06:25Z")]
GREEN strict -> exit 0        GREEN reviewer -> exit 0        GREEN supervision -> exit 0
restored sha256 = 685150e54e5a70a6d326efd4d0ac5f630e58916ddf36af34a464082ff6ddc25e  (equal to the pre-mutation value)
```

No `git reset`, `git checkout` or `git stash` was used; the worktree was clean
before and after (`git status --porcelain` empty).

## 5. Gates

`just --list` was run once; the canonical recipe is `just ci`
(fmt-check → check → lint → test → doc → build-release → security).

| Command | Raw exit | Log |
| --- | --- | --- |
| `CARGO_TARGET_DIR=<lane-local, outside the worktree> just ci` at the content head (the tree this report is committed on top of) | `0` | `just-ci-content-head.log` — 53 test suites, `0` failures, all seven recipes green, `gitleaks … no leaks found` |

The delivered tip differs from that gated content head ONLY by this report file,
so the tree-reading legs are re-run at the tip and their raw exits are recorded
in the PR body (the report cannot cite a run that happens after its own commit
without moving the head again). Two earlier aggregate attempts from this lane
are DISCARDED and NOT cited: the first built its `cargo doc` output INSIDE the
worktree's ignored `target/`, where `gitleaks dir .` — the recipe scans the
working directory, not the index — reports ONE false positive,
`square-access-token` in `target/doc/search.index/33250da97a.js`, a generated
search-index blob; the hosted `secret-scan` job scans a fresh checkout with no
build output, and a local scan with that generated directory absent reports
none. A second attempt moved that already-built target directory outside the
worktree, which leaves stale absolute binary paths embedded in the
already-compiled test executables (`env!("CARGO_BIN_EXE_canter")`) and fails
`tests/checkpoint.rs` at spawn; it was discarded and the tree rebuilt cleanly
into the lane-local target dir. Host load (`sysctl -n vm.loadavg`) when the lane
started: `{ 16.38 13.75 11.29 }`; at the end of the probe: `{ 22.72 34.26 36.11 }`
(other lanes were running). No gate was piped through `grep` as a pass/fail
test.

## 6. What was NOT verified

- Hosted CI on this PR head (the workflows run on the PR; the local aggregate
  is what this report records).
- The independent exact-head review verdict — the fleet's review leg is that
  gate.
- The two runs' NEXT transition after their bounded budgets were spent: both
  read `terminal-escalation` / `refusal.run.retry_bound` with no exposed control
  ("the owner decides"). That is the sibling closed-remedy class the issue
  itself cites (#250, #236) and is explicitly NOT this defect.
- Live dispatch of the two runs was never performed by this lane: the runs,
  their lanes and their retry rows were fenced off; the rows quoted are what
  their own armed supervision did.

## 7. Boundary

Read-only throughout: SQLite opened `mode=ro`; `canter` run/queue reads only;
no `run.*` control, no `mutate.*` apply key, no `state.db` write, no lane
touched, no operator key in the audit for the clearing. This report names no
host paths, no private repository names, no credentials, and no provider/model
policy.
