# Issue #154 — fix-round 1 delivery index

Refs #154. PR #157, branch `impl-154`, target `staging`.
Reviewed code: `f9bf402e1ab1046c1ee311417e73f1bf2bd8c05f`.
Regression candidate: `86a3994441b941072ad1a4aa8e5b2f4b665405b9`.

## Changes

Only the pane-execution test changes: malformed worktree metadata now covers
missing identity, wrong repository root, wrong checkout, unlinked worktree and
empty repository name. It asserts typed refusal, exactly one rollback close,
post-close workspace readback, no remaining workspace/pane and no agent launch.
The production validator was already correct; production source is unchanged.

## Corrected delivery claims

> "Register pane lanes… plus the report commit, on branch impl-154"

RETRACTED as a delivery claim at the reviewed head: there was no report commit.

> "AC6 raw herdr readback evidence in the lane report"

RETRACTED as a discoverable/committed delivery claim at the reviewed head.
Prior raw artifacts were found outside the checkout. This round ran NEW evidence
rather than treating those predecessor claims as verified execution.

The remote PR body was not edited: this lane's policy reserves GitHub writes to
the orchestrator. Apply the supplied `pr-body-fix1.md` and verify the readback.
The old malformed-identity coverage disclosure is superseded by the witness and
mutation proof below. No historical report or review was rewritten.

## Live evidence and its storage boundary

`driver.rs` calls the production `mutation::execute_step` for `harness_start`,
with the real Herdr CLI and daemon, in a linked worktree of an isolated named
acceptance clone. It does not fabricate responses or manually register a lane.
A real idle agent and workspace have the deterministic issue/role/round names.
Raw workspace/agent readbacks agree with the returned step outcome's identities.
A separate invocation using another lane token refuses with
`refusal.lane.name_collision` (raw exit 4), naming the occupied name.
A separate retry succeeds with `reused=true` and identical workspace/pane/agent
identity; the full workspace-id sets prove zero new workspaces across collision
and retry. The run-created clone-root group and single linked lane workspace
were both removed with readback; all pre-existing workspaces were preserved.
The existing primary implementation lane was neither adopted nor renamed.

The external bundle is named `impl154-fix1-evidence`; its complete lane report is
`REPORT-154-FIX1.txt`. The operator handoff supplies its absolute location.
It contains full, unmodified stdout/stderr and raw exits, drivers, manifest,
workspace-id comparisons, and the explicit not-exercised list.

IMPORTANT: this tracked document is an index, not raw Herdr evidence. The full
raw workspace output contains private host paths and other live identities;
AGENTS.md's public-data boundary forbids committing it. No redacted data is
misrepresented as raw. Local AC6 execution is evidenced; public raw-artifact
publication and the PR-body edit remain orchestrator/owner actions. This report
does not claim those two delivery actions are complete.

## Required gates

All six ran once at the regression candidate, with a dedicated external Cargo
target, four build jobs and serial Rust tests. Named logs are in the bundle.
- `just build-release`: raw exit 0, timeout=false; `gate-build-release.log`.
- `just check`: raw exit 0, timeout=false; `gate-check.log`.
- `just test`: raw exit 0, timeout=false; `gate-test.log`.
- `just lint`: raw exit 0, timeout=false; `gate-lint.log`.
- `just fmt-check`: raw exit 0, timeout=false; `gate-fmt-check.log`.
- `just ci`: raw exit 0, timeout=false; `gate-ci.log`.

`just test`: 721 passed, 0 failed, 0 ignored.
`just ci`: 721 Rust tests passed, 0 failed, 0 ignored;
canonical aggregate including documentation and required security checks.

## Discriminating regression

Command for control, mutation and restored control:
`cargo test --locked --test herdr_pane_execution`.

- Control: raw exit 0, 25 passed; `mutation-control.log`.
- Bypass all of `verify_workspace` by inserting `if true { return Ok(()); }`
  as its first statement: raw exit 101, 24 passed, one failed;
  `mutation-red.log`. Only
  `malformed_workspace_identity_is_refused_and_rolled_back` fails, on actual
  `succeeded` versus expected `refused` for missing worktree metadata.
- Byte-identical restore: raw exit 0, 25 passed;
  `mutation-restored-green.log`. The scratch copy has its own Cargo target;
  the implementation worktree was never mutated.

This is a coverage fix: the new test is GREEN on the reviewed correct code,
then RED on its bypass, not a claim of a production bug fixed in this round.
Structural inspection used `ast-grep run --lang rust --pattern
'fn malformed_workspace_identity_is_refused_and_rolled_back() { $$$BODY }'
tests/herdr_pane_execution.rs`: exit 0, one function match (`ast-witness.log`).

## Not exercised

No provider task, full acceptance workflow, complete live p8, live durable
journal readback, concurrent-creator race, generation turnover, Linux run or
Corral screenshot. Existing supervision tests cover journal persistence with
fixture Herdr only. No sweep, other-fleet cleanup, database edits, host install,
profile changes, GitHub API writes, issue close, merge or protected-branch push.
Hosted CI at the final delivery head is not claimed verified by these local gates.
Docs: existing contract docs remain accurate; only this evidence index is new.

## SHA256 pins for the external artifacts

| Artifact | SHA256 |
| --- | --- |
| `REPORT-154-FIX1.txt` | `07dd1c674a508eab4ce247a4c496f68518b510053b234d796dd0697b6534bf7f` |
| `evidence-sha256.json` | `c8dc086e386dda2ef4cc1ea7d9805dde90aec2c74df45e048e0f28c97d8be9f1` |
| `driver.rs` | `5539cc342e8a97d0973175b2c41ce077bd3a726c14044ab6bd1f8fd8caa75373` |
| `run-live.py` | `a427e2cf36bebeffd6e098f0c34323bac28d4805843e33781cea1fa12af0c94c` |
| `live-workspaces-created.stdout` | `0ad0db98797d6746106f00af422daa07b499069d6a6d2a525042abd97a04173f` |
| `live-agent-created.stdout` | `0b6a076e4f3e4f1908ef2877897417a84abe7fad09187b8f40b2dc5d7cd1365f` |
| `live-duplicate.stdout` | `0b369f6567019e7c4012b922ebed1b527f853557510bf3266fe8762eece04abc` |
| `live-retry.stdout` | `22c20675a344ab5a6c6636a35ef794930c0b07b80c63c4674198b21638b3ee4d` |
| `live-workspaces-retry.stdout` | `0ad0db98797d6746106f00af422daa07b499069d6a6d2a525042abd97a04173f` |
| `live-comparison.json` | `58bfe40f3e02b65f11b5a890f37909214487d02e95e3fe3a168690980e9c6019` |
| `mutation-red.log` | `6450bdaca3401ff085e372e377facae91430ab8cec2873a1c02818f4f46591b4` |
