# Issue #132, round 8 — the reviewed published-ref cleanup proof, replayed onto the run's current integration base

Lane `impl-132` (the acceptance spine's own implementer dispatch, run queued
from `request-132k.json`, step `p4-132`), worktree `issues-132`, branch
`issue-132`, based on the run's recorded integration base
`6f334a2727ef240a5311851f78ea82e4465fb1c6` (`origin/staging` at dispatch time;
carrying #197).

## What this round delivers, and why it is the bounded change

Issue #132's own text names the shape that must hold: *"the run's `merge` step
must never leave the integration checkout diverged from the published
integration ref, and the run must be able to complete on a repo whose policy is
squash."* The merge-side half is already on this base (#156/#168/#178/#182, and
#196 lands and publishes under the plan's `merge_policy`). The still-unlanded
half is the `cleanup` side: **when the sanctioned landing happens on the forge,
the checkout's own integration ref never sees it, so `cleanup` must prove the
landing against the FETCHED, verified PUBLISHED ref — and only a checkout
strictly behind it may certify from that view.** That content is independently
PASSed in the review lineage (`REVIEW-183-4dc2ae22`, re-verified as
`REVIEW-183-c05cc99`) and is replayed here onto the rebuilt base — the review
lineage's own refresh shape, never a re-design:

- `LandedProof::Published { published_head, merge_base }` — the third landed
  proof (ancestry / local content / published content), made against the
  fetched published head.
- `landed_in_published` — reads the published head
  (`published_integration_head`), refuses when the checkout's own ref IS the
  published view (no self-certification), FETCHES and verifies it
  (`fetched_published_head`), refuses whenever the local ref is not a strict
  ancestor of it (an unpublished or diverged local move is never certified),
  then proves the branch head by ancestry against the fetched head, or — for
  the squash landing, which never is an ancestor — by exact content on every
  path the branch changed relative to the fork point.
- `effect_cleanup` — when the local proof refuses
  `refusal.cleanup.unmerged`, tries the published route; if that view adds no
  proof, the ORIGINAL local refusal is returned unchanged. The salvage record
  of a published proof names `published_head` (and `merge_base` when the proof
  was by content) while keeping the checkout's own `integration_head`;
  deletion uses `-D`.
- `landed_in_integration` now uses the base's shared fail-closed comparison
  helpers (`changed_paths_between`, `differing_paths_between`, landed with
  #196) instead of the round-5 private helper, so the local and the published
  proofs cannot drift: one NUL-delimited, `--literal-pathspecs`, no-rename
  listing side, one comparison side, one empty-delta confirmation.

Everything else in the issue's text is already satisfied by the base and is
not re-implemented here: the policy input (`merge_policy: squash | ff`), the
read-only-rehearsal refusal polarity with `MERGE_NOT_FF` naming the real cause
("an unpublished or diverged local view is never reconciled" / "a squash
result is not an ff merge"), the published-ref certification in
`effect_merge`, and the #178 reconciliation loop.

## Replay equivalence (byte-level)

Every replayed byte is identical to the reviewed head `c05cc991…` — the six
reviewed files hash equal to it (sha256 of the file contents):

| File | sha256 |
| --- | --- |
| `src/mutation.rs` | `7da7fa91b905cd1c4de0e85d2808bcb78fb1124ddb6be715eb44e3ec0c7dbe55` |
| `tests/mutation_engine.rs` | `921fc0f018997e153914e3bb1a4e45a505c8d140f3e023af452a8fc4b8fb5bcd` |
| `CHANGELOG.md` | `6bcec695b232c3577b489718b628e140e8b8508f3244e112535b6be288aac2d5` |
| `docs/contracts/spec-lifecycle.md` | `fb9b952c6406479e9a338467d96933ced9fdead0ab82ac3eb3ee3a3daeb1bb78` |
| `docs/contracts/spec-plans.md` | `5cf7bbc06df8451a8e12b51e1416ac330bccd602f62aa4f73697676878fde83e` |
| `.report-132-r7.md` | `c16d34ff521457fa80c5361d5e9520b0ecee0d282bfba66dece345f9456d07b3` |

The replay commit's own patch equals the reviewed diff
(`git diff 4a13a0992d0681344b1ac012d47cae7546b916ed..c05cc991…`) for all six
files after normalizing `index` lines (`08-replay-equivalence.log`); the only
diff this round carries beyond the reviewed content is this report file
(`.report-132-r8.md`). The previous round's report is carried unchanged, so
its narrative still names its own base (`4a13a099…`) and heads.

## Delta

| File | Change |
| --- | --- |
| `src/mutation.rs` | +151/−64 (published landed proof, cleanup fallback/salvage/`-D`, shared-helper reuse in the local proof) |
| `tests/mutation_engine.rs` | +130 (one real-daemon witness, both legs) |
| `docs/contracts/spec-lifecycle.md` | +16/−2 (published-ref landed proof, salvage/deletion wording) |
| `docs/contracts/spec-plans.md` | +7/−3 (cleanup route against the fetched published head) |
| `CHANGELOG.md` | +20 |
| `.report-132-r7.md` | the round-7 report, carried byte-identical |
| `.report-132-r8.md` | this report |

## The witness (real daemon, real git trees, synthetic bare remote)

`cleanup_certifies_a_squash_landing_visible_only_on_the_published_ref`:

1. A reviewed delivery is pushed to the bare remote and a SECOND clone
   performs the policy squash there (`merge --squash` + commit + push), so
   the published ref moves while the integration checkout's own ref never
   sees the landing (asserted).
2. `cleanup` (step `x1`, real daemon RPC) then returns `removed:true`, the
   salvage names `landed_by:"content"` and the exact `published_head` the
   proof was made against, keeps the checkout's own `integration_head`, the
   checkout's own ref is still at its base (never moved), the worktree is
   gone and the lane branch is deleted.
3. Second leg: the same published landing while the checkout's own ref
   carries an UNPUBLISHED local move — `cleanup` refuses
   `refusal.cleanup.unmerged`, the lane worktree survives, and the branch
   read-back (`git branch --list issue-123`) confirms `+ issue-123` after
   the refusal.

## Evidence (raw exits, logs under `/tmp/canter-132-r8/`)

| # | Command | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `cargo check --locked --all-targets` (`01-check.log`) | 0 | clean |
| 2 | `cargo test --locked --test mutation_engine cleanup_certifies_a_squash_landing_visible_only_on_the_published_ref -- --exact --nocapture` (`02-green-published-witness.log`) | 0 | 1 passed; 0 failed (9.18 s) |
| 3 | same witness with **only** `src/` reverted to the run's base (`git stash push -- src/`; base `src/mutation.rs` sha256 `6652a9a4291504b74c018d850c11c935f0e41963633d06b812ab8a85d0cf295b`) (`05-red-witness-without-fix.log`) | 101 | FAILED against `refusal.cleanup.unmerged` — "1 of the 1 path(s) it changed … are not content-identical there (first [\"lane.txt\"])": the local view alone cannot see the published landing, exactly the pre-fix shape |
| 4 | restore (`git stash pop`; fixed `src/mutation.rs` sha256 `7da7fa91b905cd1c4de0e85d2808bcb78fb1124ddb6be715eb44e3ec0c7dbe55`) | 0 | index back to the fixed bytes |
| 5 | `RUST_TEST_THREADS=1 just ci` — one complete invocation (`07-just-ci.log`) | 0 | `fmt-check` → `check` → `lint` → `test` → `doc` → `build-release` → `security`: 44 suites `test result: ok`, 782 tests passed, 0 failed; the new witness ok inside `mutation_engine` (32 passed); scanner self-test + public-tree scan + cargo-deny + cargo-audit + gitleaks `no leaks found` |

## Adjudication notes, disclosed

- This round is a replay, not a re-authoring: the reviewed round-7 content
  (`c05cc991…`, lineage `4dc2ae22`/`d756a22`/`532458c`) is carried
  byte-for-byte onto the run's recorded base `6f334a27…` (#197). No production
  byte is authored by this round.
- Not verified here: real-forge (GitHub API) squash behaviour — the fixture
  uses a synthetic bare remote as the forge stand-in, by repository contract.
  The Linux-only non-UTF-8 filename leg does not execute on APFS (unchanged
  from the reviewed round; the portable U+FFFD fence covers both comparison
  sides).
- Residual (unchanged from the reviewed round, out of scope here):
  `post_merge_verify` and the standalone `branch_delete` effect still prove
  against the checkout's own integration ref.
