# Lane report — issue #245 (`canter queue intake`)

Round: acceptance run `run-4a4d15a67be20eea`, step `p4-245` (the implementer
step). The run's own worktree is `worktrees/issues/245` on branch `issue-245`,
pinned base `277fdd94565f47a6ea14cabf64f4f06daf81f927` (= the live
`origin/staging` head at intake).

Delivered head: `25b604f9209113f63e61db55ad33840a09c2ba03` (the candidate
`26d87f5` plus the shipped-entry-point witness; both pushed to
`refs/heads/issue-245` and consumed by PR #253, base `staging`).

## What was delivered

`canter queue intake` — intake as a first-class command instead of an agent.

| File | Change |
| --- | --- |
| `src/intake.rs` (new) | the decision core: selection, revision resolution, dedupe, bounds, digest, rendering (`hf-intake/v1`), closed refusal vocabulary, six unit tests |
| `src/commands.rs` | `QueueAction::Intake` + `QueueIntakeArgs`, `parse_queue_intake` / `parse_pin_argument`, `execute_queue_intake`, the `queue intake` usage line |
| `src/observe.rs` | `ls_remote_head` (the remote's own integration head) and a public `diagnostics` |
| `src/lib.rs` | module registration |
| `docs/contracts/spec-intake.md` (new) | the label contract, the ordering rule, the revision rule, the bounds, dedupe, digest discipline, dry run, observability and the closed refusal vocabulary (AC7) |
| `README.md`, `CHANGELOG.md` | the command surface row and the changelog entry |
| `tests/queue_intake.rs` (new) | the shipped-entry-point witness (below) |

Contract in one line: selection is the repository's own issue state (open
issues carrying the ready label, never a closed issue, never an unlabelled
issue, never a pull request), ordered by ascending issue number, each revision
resolved by the documented rule (a tracker-declared `--pin N=HEX40` wins;
otherwise the integration head read from the remote), an owned/queued issue
never re-submitted, items beyond the declared bound waiting, an unresolvable
revision refusing typed, and the rendered document + per-item grants +
submission going through the SAME `queue preview` → `grant issue` →
`queue submit` surfaces the operator path uses.

## Witnesses (raw exits)

| # | Command | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `cargo check --locked --all-targets` | 0 | compiles, no warnings (`02-check.log`) |
| 2 | `cargo test --locked --lib intake` | 0 | `6 passed; 0 failed` — ordering+bound, pin-wins, dedupe, typed refusal, digest determinism, item rendering (`15-intake-lib.log`) |
| 3 | `cargo test --locked --test queue_intake` | 0 | `2 passed; 0 failed` — the shipped binary (`16-intake-cli.log`) |
| 4 | `cargo fmt --check` | 0 | clean (`12-fmt-check.log`) |
| 5 | `cargo clippy --locked --all-targets -- -D warnings` | 0 | clean (`14-lint.log`) |
| 6 | `python3 scripts/check-public-tree.py .` | 0 | the tracked index passes the privacy scanner (`05-scanner.log`, `17-scanner.log`) |

Witness 3 is the load-bearing one; it drives the REAL binary with a controlled
environment (fake `gh` on PATH, a local bare origin carrying `staging`, the
fixture HOME/XDG state home, a real recorded state store):

- **dry run** — two invocations render ONE digest; the selection is ascending
  issue number with the closed issue, the unlabelled issue and the pull
  request excluded; the item beyond the declared bound reports `waiting`; the
  state store hashes identically before and after and no document is written
  (the journal lives inside that store, so an unchanged store is an unchanged
  journal).
- **revision rule** — the integration head is the default revision for both
  ready issues; with no published integration branch the invocation exits
  non-zero with the typed `refusal.intake.revision` and writes neither the
  store nor a document.

## Gate adjudication

- The canonical aggregate `just ci` was started at the delivered head
  (`19-just-ci.log`); its raw exit is reported in the final message rather
  than asserted here, and the per-leg runs above are the pre-aggregate proof.
- `just ci` is fail-fast across recipes: `fmt-check`, `check`, `lint` and the
  two touched suites were run individually (rows 2, 1, 4, 5, 3) so a later
  leg's flake cannot hide an earlier leg's result.

## Disclosed gaps

- The commit leg (non-`--dry-run`) delegates to `execute_queue_preview` →
  `execute_grant_issue` per item → `execute_queue_submit`, i.e. the reviewed
  existing surfaces; it is wired and type-checked but its end-to-end run
  creation against a LIVE daemon (AC1's "creates one run per admitted item") is
  NOT witnessed by a test in this delivery — the witnesses above cover the
  decision path, determinism and mutation-freedom. Stated plainly rather than
  claimed.
- The run's own recorded base is the current integration head, so the diff
  against `origin/staging` is the lane's own delta (no intervening merged work
  is spanned).
- `git fetch origin` prints the repository's benign
  `! [rejected] v0.1.0 -> v0.1.0 (would clobber existing tag)` and exits 1;
  the following push is unaffected (`08-push.log`).
