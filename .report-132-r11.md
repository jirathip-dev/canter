# Issue #132, round 11 — the delivered published-ref cleanup proof carried at integration base `e1a8c29`, re-verified

Lane: canter's own implementer dispatch (acceptance run `run-a1eb1f68dc9f2976`,
step `p4-132`), worktree `issues-132`, branch `issue-132`, recorded integration
base `e1a8c2951a91ac8ac86b8f64155615da6a3f201a` (the current `origin/staging`;
carrying #202 via PR #205).

Carried delivery: content commit `7c9c907571bedb307cc92c13aca87a4303ef2228`
and round-10 report commit `024783d42f6f416c22882dd3af25146c641bc03d`. This
report's own commit follows `024783d`, so it can name it; the head this branch
now carries is the commit that adds only this file.

## What this round delivers, and why it is the bounded change

The run's recorded base equals the base the round-10 candidate was rebuilt
onto, so a replay would re-derive a byte-identical diff for no new fact. No
re-derivation is performed: the delivered commits are carried as-is, so the
SHAs the review lineage names stay in the branch's history, and this round adds
only this report and re-verifies the delivery at the carried head.

The delivered content is the published-ref `cleanup` proof — the half of issue
#132 that was not already in the base:

- `LandedProof::Published { published_head, merge_base }` — the third landed
  proof (ancestry / local content / published content), made against the
  fetched published head.
- `landed_in_published` — reads the published head, refuses when the checkout's
  own ref IS the published view (no self-certification), fetches and verifies
  it, refuses whenever the local ref is not a strict ancestor of it (an
  unpublished or diverged local move is never certified), then proves the
  branch head by ancestry against the fetched head, or — for the squash
  landing, which never is an ancestor — by exact content on every path the
  branch changed relative to the fork point.
- `effect_cleanup` — when the local proof refuses
  `refusal.cleanup.unmerged`, tries the published route; if that view adds no
  proof, the ORIGINAL local refusal is returned unchanged. The salvage record of
  a published proof names `published_head` (and `merge_base` when the proof was
  by content) while keeping the checkout's own `integration_head`; deletion
  uses `-D`.
- The base's shared fail-closed comparison helpers (`changed_paths_between`,
  `differing_paths_between`, landed with #196) are used by both the local and
  the published proofs, so those proofs cannot drift.

Everything else in the issue's text is already satisfied by the base and is not
re-implemented here: the closed policy input (`merge_policy: squash | ff`), the
read-only-rehearsal refusal polarity with `MERGE_NOT_FF` naming the real cause,
`effect_merge`'s published-ref certification with the #178 reconciliation, and
`post_merge_verify`'s content fact for a squash landing.

## Provenance and re-verification at dispatch

- The run recreated the lane branch at its recorded base (no commits); this
  lane re-materialized the delivered head: `git rev-parse HEAD` equals
  `git ls-remote origin refs/heads/issue-132` =
  `024783d42f6f416c22882dd3af25146c641bc03d`, and
  `git merge-base --is-ancestor e1a8c29 HEAD` exits 0.
- Byte-identity of the eight delivered paths against the round-10 table
  (`shasum -a 256`, same values):

  | File | sha256 |
  | --- | --- |
  | `src/mutation.rs` | `b4ff7a49b78366494510a376408c9d6ab1d9c98a31dbca9b2dcf9db3dbd026e4` |
  | `tests/mutation_engine.rs` | `45c4c58c829ba31d536e20b1eb65e56034290b6923734a215ae12598cc34be0b` |
  | `CHANGELOG.md` | `6bcec695b232c3577b489718b628e140e8b8508f3244e112535b6be288aac2d5` |
  | `docs/contracts/spec-lifecycle.md` | `0a07ddd46925dba7a4b66acfe31d529c044759bdcaf811558f754675c12b23f7` |
  | `docs/contracts/spec-plans.md` | `5cf7bbc06df8451a8e12b51e1416ac330bccd602f62aa4f73697676878fde83e` |
  | `.report-132-r7.md` | `c16d34ff521457fa80c5361d5e9520b0ecee0d282bfba66dece345f9456d07b3` |
  | `.report-132-r8.md` | `e1dd06f97531c859fa1b308580b22e6a9ef4794a0aa541636b9ff7bd408fe16f` |
  | `.report-132-r9.md` | `93312eaeb0b6e99b4de47e2486bd2df876a6f4efc3b7198903e911a583ef7564` |

- Independent verdict at the carried head: `REVIEW-183-024783d4` — PASS naming
  `024783d42f6f416c22882dd3af25146c641bc03d` exactly, base `e1a8c29…`, with no
  blocking findings (lineage: `REVIEW-183-1ba80d60`, `REVIEW-183-4dc2ae22`,
  `REVIEW-183-c05cc99`, `REVIEW-183-d756a22`, `REVIEW-183-532458c`).

## Gates (raw exits; named logs)

At the carried content commit `7c9c907` — round 10's aggregate record, quoted
from `.report-132-r10.md` and its named logs:

| # | Command | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `just ci` (one invocation) | 101 | `fmt-check` / `check` / `lint` green; the test step ran 39 suites: 38 ok (743 passed) then an untouched-suite fixture race in `tests/supervision.rs` (no assertion failure) |
| 2 | that selection, `--test-threads=1` | 0 | 1 passed; 0 failed (3.09 s) |
| 3 | `just doc && just build-release && just security` (one invocation) | 0 | docs + release build green; scanner self-test + scan + supply-chain checks green; `gitleaks directory scan … no leaks found` |

At this round's head this dispatch runs `just fmt-check`, `just security` and
the full aggregate against the committed tree; their raw exits are recorded in
the lane's final dispatch report.

## Not delivered / not claimed

- No production behaviour change in this round; no re-design and no replay
  re-derivation — the delta is this report file only.
- The live end-to-end acceptance spine (`p1`..`p8`, zero operator keys) is the
  orchestrator's to drive: this lane did not dispatch, pause, retry, release or
  mutate any run, daemon or state, and did not touch `staging`.
- Hosted CI for this branch is reported by the pull request, not by this
  report.
