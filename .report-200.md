# impl-200 — a certified delta head must not be able to move (Refs #200)

Candidate commit: `4025007ec1c087cb354ea4b9dcc43e1014ad20ec`
Base: published `origin/staging` = `c3f3d09feb370d58240adffa4a5860ae8ac9cf2a` (verified with
`git ls-remote origin refs/heads/staging` before the work started; the worktree HEAD equalled it).
Branch: `impl-200`. No PR opened; `staging` untouched.

## Read-only reproduction of the defect (live sandbox, nothing dispatched or reset)

`run-85a856d6b9e9e7d0` (issue #132) at the p6 frontier, read through
`state.db` (read-only), the daemon log and the journal mirror:

- `p4-132` (prompt) resolved in 5 s with `harness_state: "working"` — the prompt is
  delivery-scoped; the worker was mid-turn.
- `p5-132` was claimed `08:39:14Z` and resolved `08:59:27Z` with
  `result.head = 38fb0929364eb76e75262121ef3f126fddc2c14b` (`changed_files` = the six
  candidate paths) — **certified while the same pane worker was still mid-turn**.
- The same worker committed `0d5e851b79d593a9031eb80c58adf2be30365201` (`.report-132-r8.md`)
  at `08:59:53Z`, **26 s after collection**; the lane checkout has been past the
  certificate ever since.
- `p6-132` then refused `refusal.evidence.verdict_stale` ×3
  (`13:04:41Z`, `13:05:42Z`, `13:07:43Z`) and **consumed all three bounded retries**
  (`run_retries` `rt_b94ef98d2a4a1a20`, `rt_384df05a5a4ee186`, `rt_5a921b84619845b9`)
  on a step that can never succeed, then parked with the budget spent.

That is the #147 class at the p4→p5 boundary: a head read while the worker is still
mid-turn can still move.

## What changed

| file | +/− | change |
| --- | --- | --- |
| `src/mutation.rs` | +84/−22 | `poll_pane_worker`: a delta head is certified only at a **genuinely settled** pane turn — the stop state must be confirmed across two consecutive read-backs, exactly the rule the empty-delta path already used. A delivery read while the worker is still live, like an empty delta while it is live, stays a WAIT/re-check (bounded by the step deadline → `effect.worker_timeout`). A collection failure that is not emptiness (a refusal of the collection itself) still returns immediately: it certifies no head. No deadline, cadence or budget changed. (a)/(c) unit witnesses live in this file's test module. |
| `src/supervision.rs` | +83/−0 | `dispatch_intent`: a recorded `refusal.evidence.verdict_stale` diagnosis is an **impossible** step, not a retryable one — the certificate is a recorded fact and the checkout's movement is external, so a re-dispatch refuses identically. Excluded from the bounded-retry arm: never re-dispatched, bounded retries stay **unspent**; the frontier parks typed (`needs-attention` / `supervision.step_diagnosed` / the recorded code). Retryable diagnoses (`refusal.worktree.exists`, …) are untouched — #179/#180 unchanged. The rule witness (parked frontier, zero retries, typed detail; a retryable code still retries) lives in this file's test module. |
| `src/daemon_retry_tests.rs` | +121/−0 | Daemon-level AC2 witness + a `seed_attempt` fixture helper. |
| `tests/supervision.rs` | +44/−4 | The daemon-level pane-collection fixture: the delivery now lands **mid-turn** and the worker settles; the witness asserts the collection certified only at the settled turn, that the mid-turn read really happened (poll log), and that the certified head is the lane's final settled HEAD (read from the recorded response). |
| `tests/review_dispatch.rs` | +20/−5 | The moved-head review step refuses **deterministically**: two dispatches of the same moved head refuse identically, the message names both heads, and the reviewer is never started. |
| `docs/OPERATIONS.md` | +1/−0 | Operator remedy row for `refusal.evidence.verdict_stale`. |
| `docs/contracts/spec-daemon.md` | +22/−6 | The confirmed-stop certification rule and the moved-head park (unspent retries). |
| `docs/contracts/spec-review-evidence.md` | +4/−1 | The deterministic moved-head refusal and its park rule. |

Fences honoured: no `main`, no release work, no gate bypass, no dependency change, commit
says `Refs #200` (never `Fixes`/`Closes`/`Resolves`), no PR, `staging` untouched, the live
sandbox was read-only and `run-85a856d6b9e9e7d0` / `run-c604f142afd5774c` / lane
`issue-132` were not touched.

## Gates — raw exits (candidate `4025007`)

`just --list` was read first; each recipe ran as one invocation with its own log.

| command | raw exit | log |
| --- | --- | --- |
| `just fmt-check` | 0 | `01-just-fmt-check.log` |
| `just check` | 0 | `01-just-check.log` |
| `just lint` | 0 | `01-just-lint.log` |
| `just test` | 0 | `01-just-test.log` |
| `just doc` | 0 | `01-just-doc.log` |
| `just build-release` | 0 | `01-just-build-release.log` |
| `just security` | 0 | `01-just-security.log` |
| `just ci` (one aggregate invocation) | 0 | `02-just-ci.log` |

`just test`: **789 passed / 0 failed** across 44 test binaries (unit + integration +
doc-tests), including `tests/supervision.rs` (the touched serial suite) and
`tests/review_dispatch.rs`. Host load during the runs: 6–14 on 10 cores.
`just security` ran gitleaks over the checked-out tree (`no leaks found`).

## Mutation battery (RED/GREEN, restored bytes proven)

Both mutations were applied to the **committed** candidate, each witness run raw
(no pipes), then the exact source bytes were restored from the committed blob.

**M1 — restore "certify on the first returned outcome"** (`src/mutation.rs`, the pre-fix
block re-inserted ahead of the confirmed-stop check):
- mutated sha256 `9f3de8a73c052e7ccf50218df38112493516002b27fb91d728969781593d9734`
- (a) `collection_certifies_the_final_settled_head_never_an_earlier_read` → **RED, exit 101**:
  `left: "aaaa…" right: "bbbb…"`, "the certified head is the final settled head"
- (c) `collection_delivery_while_live_waits_and_is_collected_once_settled` → **RED, exit 101**:
  `left: 3 right: 5`, "the delivery read while working (read 3) never certifies"
- daemon fixture `collection_waits_for_pane_delivery_without_operator_dispatch` →
  **RED, exit 101**: `left: "working" right: "done"`, "the delta is certified only at the
  worker's SETTLED turn"
- log: `10-mutation-M1-certify-first-outcome.log`
- restored sha256 `4c6e1fb92d1463935a1597d8ed8e23303fe033cfb37cadb41856276978810388`
  (**identical to the committed blob**) and `git status --porcelain` empty.

**M2 — drop the moved-head exclusion** (`src/supervision.rs`, one condition removed):
- mutated sha256 `2e754053fdab7e20bd519f85d02b177de257397cd4f64e081108bf67fd07d246`
- `a_moved_head_diagnosis_parks_the_frontier_without_burning_a_bounded_retry` → **RED, exit 101**
  ("an impossible step is never re-dispatched")
- `a_moved_head_diagnosis_never_spends_a_bounded_retry` → **RED, exit 101**
  ("the recorded moved-head refusal parks the frontier")
- log: `11-mutation-M2-retry-moved-head.log`
- restored sha256 `0d1c03659338412521ea22cff70201b7c85beff7be0e8885966f062234678ca3`
  (**identical to the committed blob**) and `git status --porcelain` empty.

**GREEN after restore** (`12-restored-green-lib.log`): `collection_` 5 passed, exit 0;
`a_moved_head_diagnosis` 2 passed, exit 0.

## Acceptance-criteria map

- **AC1** — `poll_pane_worker` (unit (a)/(c) witnesses + the daemon collection fixture).
- **AC2** — `dispatch_intent` exclusion (rule witness + daemon dispatch witness: no
  re-dispatch, `run_retries` empty, stale intent refused `refusal.run.retry_required`,
  typed `needs-attention`/`step_diagnosed` + the code).
- **AC3** — unchanged head check: the reviewer is still refused on a head that does not
  match the certificate (deterministic, both heads named, reviewer never started); nothing
  is re-pointed or invented.
- **AC4** — (a)/(c) above, (b) the deterministic typed refusal + the parked frontier,
  (d) M1/M2 RED.
- **AC5** — no deadline widened, no cap/gate changed; retryable diagnoses still take their
  bounded retries (witnessed both in the rule witness and in the untouched
  `repeated_refusal_exhausts_three_retries_and_fences_the_frontier`); #152/#192 fixtures
  unchanged and green.

## Not verified / disclosed gaps

- No **live** run was repaired, re-dispatched or reset: the wedged
  `run-85a856d6b9e9e7d0` is forensics and still refuses `verdict_stale` (with its three
  retries already spent by the old binary).
- A full daemon end-to-end *moved-head review* fixture (a real review step whose lane
  checkout moves) was **not** built: AC2 is witnessed at the supervision rule level plus
  the daemon dispatch path with a seeded recorded refusal — the same recorded shape the
  live `state.db` shows — and the effect-level refusal is witnessed in
  `tests/review_dispatch.rs`. The reviewer leg's own dispatch is exercised by the existing
  #193 fixtures.
- `poll_pane_worker` treats any state outside `idle|done|blocked` (including `unknown`) as
  not settled: such a collection now waits out the step deadline and parks as
  `effect.worker_timeout` instead of certifying. That is the bounded, typed alternative,
  and it is a behaviour change for that (previously certifying) case.
