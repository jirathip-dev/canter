# Issue #132, round 15 — the reviewed published-ref cleanup proof, replayed onto integration base `67b51b7`

Lane: canter's own implementer dispatch (run `run-f50b96a2f79ed9f8`, step
`p4-132`, submission `qs_d9a863cb6e9eb4bf`), worktree `issues-132`, branch
`issue-132`, based on the run's recorded integration base
`67b51b7f29ed9bc64bd8768906b6a1856793db03` (`origin/staging` at dispatch
time; carrying #219 via PR #220).

Commit graph: base `67b51b7` -> content head `6ed6dfb` ("Cleanup certifies a
squash landing visible only on the published ref; replayed onto integration
base 67b51b7 (Refs #132)") -> the five report-only commits the round-14 head
carried (`.report-132-r10.md` `4e88a42`, `.report-132-r11.md` `05235e4`,
`.report-132-r12.md` `8c2f49b`, `.report-132-r13.md` `03b9a11`,
`.report-132-r14.md` `8b6ba9f`, byte-identical to their copies on the
previous head) -> this report's own commit. This report's own commit follows
the carried head, so it can name it.

## What this round delivers, and why it is the bounded change

Issue #132's own text names the shape that must hold: *"the run's `merge` step
must never leave the integration checkout diverged from the published
integration ref, and the run must be able to complete on a repo whose policy
is squash."*

The merge-side half is in the base (#156/#168/#178/#182; #196 lands and
publishes under the plan's `merge_policy`, with the published ref always the
certified target, never a stale local view; #219/#220 publishes through the
route the topology declares — this repository's route is the pull request —
and fast-forwards the checkout so no later step reads a stale local view).
The still-unlanded half is the `cleanup` side: when the sanctioned landing
happens on the forge (the PR is squash-merged), the checkout's own integration
ref never sees it, so `cleanup` must prove the landing against the FETCHED,
verified PUBLISHED ref — and only a checkout strictly behind it may certify
from that view. That content is independently PASSed in the review lineage
(`REVIEW-183-024783d4` PASS at `024783d42f6f416c22882dd3af25146c641bc03d`;
lineage `REVIEW-183-f4ea6876` naming `f4ea6876cb0ac8d39729f1a4bbfabe4eb2250a2b`
exactly, `REVIEW-183-1ba80d60`, `REVIEW-183-4dc2ae22`, `REVIEW-183-c05cc99`,
`d756a22`, `532458c`) and is replayed here onto the rebuilt run base — the
lineage's own refresh shape, never a re-design:

- `LandedProof::Published { published_head, merge_base }` — the third landed
  proof (ancestry / local content / published content), made against the
  fetched published head.
- `landed_in_published` — reads the published head
  (`published_integration_head`), refuses when the checkout's own ref IS the
  published view (no self-certification), FETCHES and verifies it
  (`fetched_published_head`), refuses whenever the local ref is not a strict
  ancestor of it (an unpublished or diverged local move is never certified),
  then proves the branch head by ancestry against the fetched head, or — for
  the squash landing, which never is an ancestor — by exact content on every
  path the branch changed relative to the fork point.
- `effect_cleanup` — when the local proof refuses `refusal.cleanup.unmerged`,
  tries the published route; if that view adds no proof, the ORIGINAL local
  refusal is returned unchanged. The salvage record of a published proof names
  `published_head` (and `merge_base` when the proof was by content) while
  keeping the checkout's own `integration_head`; deletion uses `-D`.
- `landed_in_integration` uses the base's shared fail-closed comparison
  helpers (`changed_paths_between`, `differing_paths_between`, landed with
  #196) instead of the round-5 private helper, so the local and the published
  proofs cannot drift.

Staging has moved one commit since the round-14 replay base (`fc5c88c` ->
`67b51b7`: #219 via PR #220). No production behavior beyond the replay is
added in this round. The round-10..round-14 report files are carried from the
previous head byte-identically; prior reports are byte-unchanged.

## Replay integrity — the reviewed content, byte-for-byte, on the new base

All comparison proofs are hunk-body comparisons (`index` lines and `@@`
headers excluded because offsets move), each a raw `diff` whose exit was 0
with an empty diff file (named by basename; logs in the lane artifact
directory for this round):

| # | comparison (log basename) | result |
| --- | --- | --- |
| 1 | this delta vs the previous content head's delta, the five code/doc paths (`01-delta-equiv.diff`) | IDENTICAL — 476 lines each |
| 2 | ride-along, `src/mutation.rs`: `diff(previous content head -> this content head)` vs `diff(fc5c88c -> 67b51b7)` for the same path (`02-ridealong-mutation.diff`) | IDENTICAL — 763 lines each |
| 3 | ride-along for `docs/contracts/spec-plans.md` (`03-ridealong-plans.diff`) | IDENTICAL — 44 lines each |
| 4 | ride-along for `tests/mutation_engine.rs` (`04-ridealong-tests.diff`) | IDENTICAL — 324 lines each |

sha256 (16 hex shown) of the eight content-head files against the previous
content head `92e50f4` (round-14 head `8ac50c1a`):

| path | this content head | previous content head | state |
| --- | --- | --- | --- |
| `CHANGELOG.md` | `6bcec695b232c357` | `6bcec695b232c357` | byte-identical |
| `docs/contracts/spec-lifecycle.md` | `0a07ddd46925dba7` | `0a07ddd46925dba7` | byte-identical |
| `.report-132-r7.md` | `c16d34ff521457fa` | `c16d34ff521457fa` | byte-identical |
| `.report-132-r8.md` | `e1dd06f97531c859` | `e1dd06f97531c859` | byte-identical |
| `.report-132-r9.md` | `93312eaeb0b6e99b` | `93312eaeb0b6e99b` | byte-identical |
| `src/mutation.rs` | `5cc4703adf77d837` | `6a1e75f591af7f36` | differs by the base's own #219/#220 text (proof 2) |
| `docs/contracts/spec-plans.md` | `7dd23842bcf25988` | `550a2b199c0ddaa2` | differs by the base's own #219/#220 text (proof 3) |
| `tests/mutation_engine.rs` | `002dba1d1d021ad5` | `45c4c58c829ba31d` | differs by the base's own #219/#220 text (proof 4) |

The three differing files differ exactly where the base's own #219/#220 work
rides along; proofs 2–4 show `diff(previous content head -> this content head)`
is byte-identical to `diff(fc5c88c -> 67b51b7)` for those paths, hunk for hunk
— a clean three-way cherry-pick (exit 0, no conflict, no base text dropped).
The five carried report files are byte-unchanged: `.report-132-r10.md`
`07f413d69aae55af`, `.report-132-r11.md` `3e67191baf22aa80`,
`.report-132-r12.md` `b1ca2df618176d97`, `.report-132-r13.md`
`ae436fd77888a023`, `.report-132-r14.md` `add131965a1c6f8b`.

The content commit stat is 8 files, 706(+)/69(-) — the same count as the
previous content head and the originally reviewed commit; the full delivery
delta against the new base is 13 files, 1507(+)/69(-).

## Verification (raw exits, named logs)

All runs in the lane worktree at the head above, `CARGO_TARGET_DIR` outside
the checkout; logs in the lane artifact directory (named by basename below).
Method: raw redirect to a named log, raw exit recorded, no gate piped through
a filter.

| # | Command (log basename) | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `just fmt-check` (`01-fmt-check.log`) | 0 | clean |
| 2 | `cargo check --locked --all-targets` (`02-check.log`) | 0 | clean |
| 3 | the delta's own witness solo (`03-witness-solo.log`) | 0 | 1 passed; 0 failed (10.64 s) |
| 4 | mutation probe m1 — the strict-behind guard forced to `if false` (`05-mut-1.diff`, `06-witness-red.log`) | 101 | RED at the guard's own property, see below |
| 5 | post-probe restore + witness GREEN on the restored bytes (`07-witness-green-restored.log`) | 0 | `src/mutation.rs` sha256 == `git show HEAD:` blob (`5cc4703adf77d837`); `git status` empty; 1 passed (10.33 s) |
| 6 | `just lint` (clippy `-D warnings`, all targets) (`08-lint.log`) | 0 | clean |
| 7 | `just ci` attempt 1 (`09-ci.log`) | 101 | `test` step: `host_proof_renewal::a_submit_time_proof_renews_at_dispatch_time_and_the_fanout_steps_are_reached` — its own 60 s bounded frontier wait expired with the frontier at `p2-5` (host load 15–40). Rows 8–9 are the adjudication |
| 8 | same selection solo at the head (`10-host-proof-solo.log`) | 101 | same signature (`tests/host_proof_renewal.rs:395`, frontier `p2-5`, 62.10 s) under the same load |
| 9 | same selection with the delta's source reverted to base `67b51b7`, byte-exact restore after (`14-host-proof-base-probe.log`) | 0 | 1 passed (4.70 s); the same selection re-run at the head under lighter load (`15-host-proof-at-head-rerun.log`) → 0, 1 passed (3.62 s). The test's 60 s bound races the supervisor's own 60 s tick; the identical failure is already on record for this lineage (`.report-132-r12.md`, same test, `:395`, frontier `p2-5`) |
| 10 | `just doc` (`11-doc.log`) | 0 | clean |
| 11 | `just build-release` (`12-build-release.log`) | 0 | clean |
| 12 | `just security` (`13-security.log`) | 0 | scanner self-test OK; public-tree scan clean; cargo-deny advisories/bans/licenses/sources ok; cargo-audit clean; gitleaks directory scan … no leaks found |
| 13 | `just ci` attempt 2 (`17-ci-attempt2.log`) | 101 | `test` step: `review_dispatch` 14 passed / 3 failed — each `adapter.timeout` where `effect.review_timeout`/`refused` was expected (4.55 s), the base's own #214/#215 parallel-execution deadline race, in suites this delta does not touch. Rows 14–15 are the adjudication |
| 14 | `cargo test --locked --test review_dispatch` solo at the head (`18-review-dispatch-solo.log`) | 0 | 17 passed; 0 failed (6.26 s) |
| 15 | same suite with the delta's source reverted to base, byte-exact restore after (`19-review-dispatch-base-probe.log`) | 0 | 17 passed; 0 failed (5.96 s). The three aggregate failures appear only under the aggregate's parallel execution, on both invocations — the suite's own deadline race, not this delta |

Neither `just ci` invocation went green end to end: both stopped in the
`test` step on suites this delta does not touch, and each failing selection is
adjudicated above (solo re-run and/or base-content probe). Every other local
suite is green in at least one raw invocation. Hosted CI on the pushed head is
the authoritative aggregate for the full chain.

## Mutation probe m1 (discrimination, not just a green run)

With the strict-behind guard in `landed_in_published` forced to `if false`
(the difference in `05-mut-1.diff`), the witness exits 101 at its own
diverged-view assertion: the cleanup CERTIFIES the diverged unpublished local
view (`removed:true`, `landed_by: content`) instead of refusing it. Restoring
the bytes exactly (`git checkout -- src/mutation.rs`, sha256 re-verified
against the committed blob, `git status` empty) turns the same witness green
again (`07-witness-green-restored.log`, exit 0). The guard therefore carries
the property it claims.

## Not exercised in this lane (stated, not implied)

- The live forge route itself belongs to the run's own `p7-132` (this lane
  pushes its branch and carries the open pull request's head; it does not
  merge anything).
- Hosted CI on the pushed head is the authoritative aggregate for the full
  gate chain; the local raw exits above are this lane's own record.
- No live workflow execution, release execution or real harness session is
  part of this delivery; all witnesses use synthetic fixtures and fake
  executables only (repository contract).

Refs #132.
