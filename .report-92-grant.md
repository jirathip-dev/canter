# .report-92-grant.md — issue #92: a SUPPORTED production grant-issuance path

Lane: `issue-92-grant-issuer` · base `origin/staging` `5de41a0` · candidate head
`dfa856b` ("Grant issuance: the supported production mint path (Refs #92)").
The delivered head adds this report file only; the gates below ran against
`dfa856b`, and the tracked-tree scanners were re-run at the delivered head
(section 10).

Fences respected: `src/tui/**` and `src/redact.rs` untouched; no schema or
migration change (state schema stays v12, chain `m0001..m0012`); no new
dependency; the closed RPC set grew by exactly the ONE method issuance needs.

## 1. The gap this closes (AC1)

Traced at the base: `State::issue_grant` (`src/state.rs`) had test-only
callers; `RPC_METHODS` (`src/schema.rs`) carried `grants.list` /
`grants.revoke` but no issuance method; the CLI had no `grant` verb. So
`canter board --plan FILE --grant REF=GRANT_ID` and the `apply` RPC bound to
grant ids that no supported caller could obtain.

The shipped mint path:

- **`grants.issue`** (closed method set `36 -> 37`): `params.grant` carries
  one `hf-grant/v1` document, `params.idempotency_key` the automation key.
  The document is validated BEFORE any journaling; an already-expired window
  refuses `refusal.grant.expired`; a production-class binding (phase
  `production`, or a `production`/`release` capability) refuses
  `refusal.policy.production_confirmation` (production authority stays
  human-only and this surface carries no confirmation channel). The intent
  (`mutate.grant.issue`) is journaled before the single row insert, and the
  response is the minted document in contract shape (`caps` as an array).
- **`canter grant issue --request FILE --issue N --expires-in SECS`** is the
  supported CLI caller over that method. Every field is DERIVED — repository
  identity and the issue's exact 40-hex acceptance revision from the reviewed
  bound-input document (`canter queue preview --out`), `workflow_hash` from
  that document's `workflow.hash`, `policy_hash` from its
  `role_config.revision` re-observed against the CURRENT configuration,
  phase/caps from its boundary, scope `worktrees/issues/N`, the live daemon
  epoch, and the explicit window; the grant id is content-addressed
  (`gr_` + sha256 of the canonical binding). Nothing is typed by hand and the
  CLI refuses a daemon response that does not match the requested binding.

No test-only seeding and no direct state write exists anywhere in the mint
path: it is the CLI -> socket -> daemon -> one row insert, and a mint without
a live daemon refuses `daemon.absent` (proved in `tests/grant_issue.rs`,
which also asserts the store stays empty).

## 2. Binding proof (AC2)

End-to-end mint on the real host against the REAL acceptance revision of
`jirathip-dev/canter#92` (read through the read-only plan; run log
`run/logs/e2e-92.log`, isolated root, section 7):

```
GRANT_ID=gr_ffcfaf2b366ef40e
{"caps":["read","worktree","spawn","prompt","review","merge","cleanup"],
 "created_at":"2026-09-13T15:52:33Z","expires_at":"2026-09-13T16:52:33Z",
 "grant_id":"gr_ffcfaf2b366ef40e",
 "issue":{"number":92,"revision":"20d0afb063ea4f73f1400d47f6d2d2843bc6ad0c"},
 "phase":"merge",
 "policy_hash":"4159d0ea4181ad726dbabf0612a8afd10115d4a2f433abb6435e7fe823d805d9",
 "repository":"jirathip-dev/canter","schema":"hf-grant/v1",
 "scope":"worktrees/issues/92","state_epoch":1,
 "workflow_hash":"94b4778d372acd08a9d5ae85c88c0b0154bad24bdc57cb0f575f57e035458dd3"}
```

The response document validates as `hf-grant/v1` (the family validator, pinned
in `cli_mint_is_daemon_issued_and_reads_back_with_the_reviewed_binding`), and
`grants.list` reports the same grant id/repository/issue revision/expiry/epoch
over the socket. The same test asserts the workflow hash equals the reviewed
document's, the policy hash equals the re-observed role-configuration
revision, the caps equal the reviewed boundary caps, and `state_epoch` equals
the daemon's live epoch; the issued intent (`mutate.grant.issue`) is present in
`journal.tail`, i.e. the intent precedes the row.

"Grants remain daemon-issued and daemon-consumed": issuance is only reachable
over the socket; consumption is unchanged (`queue submit` re-verifies status /
epoch / expiry / repository+issue+revision / workflow hash / scope under its
guard, then the run binds the grant — the dispatched run in section 7 reports
`grant_id gr_ffcfaf2b366ef40e`).

## 3. Expiry proof (AC2) — `refusal.grant.expired`

- Refused at mint: a document whose `expires_at` is not in the future is
  refused with `refusal.grant.expired` BEFORE the claim (no grant row, no
  journal claim) —
  `an_already_expired_document_is_refused_at_mint_before_any_claim`.
- Refused at consumption (the typed code the contract names): a grant minted
  with `--expires-in 1`, consumed after it passed, yields an item verdict
  `refused` with reason `refusal.grant.expired` and no dispatched run —
  `an_expired_window_stops_being_consumable_with_the_typed_code`.

## 4. Epoch proof (AC2) — grants die with their epoch

- A presented document whose `state_epoch` is not the live epoch is refused
  `state.epoch_mismatch` with no row (`a_stale_epoch_document_is_refused_at_mint`).
- A state-epoch rotation (the real `backup.create` + `restore.begin` path,
  epoch 1 -> 2) invalidates the minted grant: it leaves the active set
  (`grants.list` empty) and its issue refuses as `refusal.grant.inactive`
  (`an_epoch_rotation_invalidates_the_minted_grant`).
- The live epoch is bound at mint time: `grant issue` reads the daemon epoch
  and a moved epoch refuses (`state.epoch_mismatch`); the re-observed role
  revision is checked before the wire, and a moved configuration refuses
  `refusal.profile.revision` with nothing minted
  (`a_moved_role_revision_refuses_before_the_mint`).

## 5. Authorization unchanged (AC3)

Issuance is not authorization and nothing on this surface authorizes a run:

- The board keeps the authorization point: `canter board --plan FILE --grant
  REF=GRANT_ID` (with the reviewed digest approval) is where a run is
  authorized; the mint response's own hint says so, and `board --grant`
  parsing/arming guards are untouched (`src/tui/**` byte-unchanged).
- Unknown-keyboard/back/cancel behavior is untouched (no `src/tui/**` edit);
  the CLI mint has no `--yes`-style blanket flag, no digest bypass and no
  prompt: the only inputs are the reviewed document, `--issue`, `--expires-in`
  and an optional idempotency key.
- A production-class binding refuses typed on both layers (CLI + daemon) —
  probe P2 disables both layers and the wire test goes RED, so the fence is
  real, not a single-layer gesture.

## 6. Atomicity, idempotency, crash points (AC4)

- Intent journaled before the effect (section 2); the claim machinery is the
  shared one (`mutate.grant.issue`).
- Exactly-once per idempotency key: same request id + key replays the recorded
  response byte-for-byte; a different request id reusing the key refuses
  `state.claim_reused`; the same binding with a fresh key refuses
  `state.grant_exists` (content-addressed id) — no second authorization for
  one reviewed run.
- Crash points (both tested with real daemon children, `CANTER_CRASH_POINT`):
  - `grants.issue.after-intent` (interrupted before the insert): after the
    restart the reconcile readback is `never committed`, `grants.list` is
    empty (no partial grant), and a retry with a fresh key mints cleanly.
  - `grants.issue.after-commit` (interrupted after the insert): after the
    restart the readback is `committed before the interrupt`, exactly ONE
    grant exists, and re-minting the same binding refuses
    `state.grant_exists`.
- A second daemon on the same state/socket is still refused (exit 1,
  `daemon.busy`) while the first daemon keeps minting
  (`a_second_daemon_is_still_refused_while_issuance_is_available`).

## 7. End-to-end reachability (AC5)

Driver: `run/e2e/e2e-board-grant.sh` (+ `run/e2e/pty_drive.py` for a real PTY)
with the release binary built from `dfa856b`; log `run/logs/e2e-92.log`.
Isolation: a dedicated root with its own state home and socket, and HOME left
real so the read-only `gh` observation keeps the invoking environment's auth.
The operator's live daemon/state was never touched (an early run that omitted
the state override was refused by the live lock — `another daemon is already
running: another daemon holds the lock` — and the script now isolates state
before anything else).

1. Real revision: `canter plan jirathip-dev/canter 92` -> acceptance revision
   `20d0afb063ea4f73f1400d47f6d2d2843bc6ad0c` (gh read).
2. Reviewed bound-input document: `canter queue preview ... --out request.json`
   -> exit 0, digest `449b290cb4c25fe47250af389649241dffdbd29d058c71e1572f558f7e673c39`.
3. **Mint through the new supported surface**: `canter grant issue --request
   request.json --issue 92 --expires-in 3600` -> exit 0, grant
   `gr_ffcfaf2b366ef40e` (binding in section 2). A second mint of the same
   binding -> exit 1 `state.grant_exists`.
4. **The engine performs ONE bounded already-approved work item**:
   `canter queue submit --grant 92=gr_ffcfaf2b366ef40e --caps 4/2/2 ...` ->
   exit 0, submission `qs_35b7f83f88165bc0`, `admission admitted 1 refused 0
   waiting 0`, item `jirathip-dev/canter#92` **admitted** with work item
   `wi_c6fc6eb3128e5b2f` and run `run-dfaf65f2986f2268`; the run reports the
   minted grant and the real role binding (`role_config.key fleet-impl`,
   revision `4159d0ea…`). The submission document itself states the boundary:
   no workflow step has been executed (step execution stays with the merged
   `apply` machinery, which no CLI subcommand exposes).
5. **The board authority path over a REAL PTY** (`jp<CR><SPACE><CR>q` into the
   operator console): `canter board --plan request.json --caps 4/2/2
   --host-available yes --harness-lanes 0 --grant 92=gr_ffcfaf2b366ef40e
   --resume run-dfaf65f2986f2268=b5ed0742…` -> exit 0; the captured frames
   contain the board, the preview and the authorization scope, and the console
   dispatched submission `qs_13e152722ab2e29c` (`admitted 1`), which the daemon
   recorded: the run went `paused -> running` (resume digest consumed). The
   console is the authority that produced this effect; the script only sent
   keystrokes.
6. Pause / restart / next-work: `run pause` committed the safe boundary with
   its engine-minted resume digest `b5ed07424f366a13c2788a1f3cef8c5e2e7448d4800ceb49e781f0dbdbc80921`;
   a SIGTERM + restart re-read the same state (`run status` paused, same
   digest; `queue status` identical), i.e. the committed work item and its
   cursor survive the restart.

Not performed (disclosed): no workflow STEP was executed and nothing was
spawned, killed, merged or pushed by this chain; the "work item" performed here
is the durable run dispatch plus the board-authorized resume, which is the
reach the current product surfaces have. The physical/host acceptance of a real
harness lane is out of this lane's scope (as the issue records for #90).

## 8. Discrimination probes (tests must bite)

`run/probes/grant-issue-probes.py` mutates the shipped source, runs the named
focused test (RED must name the test and report FAILED, never a bare exit),
restores the file from its pre-probe bytes, re-runs it (GREEN) and verifies the
restored sha256 equals the original. Run at `dfa856b` (summary:
`run/logs/probes/summary.txt`):

| probe | mutation | RED | GREEN |
| --- | --- | --- | --- |
| P1 mint expiry fence | `if false && is_expired(...)` in `grants.issue` | rc 101, test FAILED | rc 0 |
| P2 production fence (both layers) | CLI + daemon production guard -> `false` | rc 101, test FAILED | rc 0 |
| P3 epoch fence | `if false && grant.state_epoch != epoch` (`state.rs`) | rc 101, test FAILED | rc 0 |
| P4 issued-document round-trip | minted `created_at` rendered wrong | rc 101, test FAILED | rc 0 |

Restored bytes were identical in all four (original sha256s: `src/daemon.rs`
`a2e590d469372c5b439e2b6247b7a2372882db65e852a9a30f0488fd01927c32`,
`src/commands.rs`
`253af09f2c973da97e231a4fd9beba14035177f1bedeb6ef31e8e0502c8b6f12`,
`src/state.rs`
`6c0d97cbd80c11dfc90c755c6c7bcdaad4e9cfcb244062fe96eef0e84cec833d`), and
`git diff --exit-code` was 0 after the run, so the probe table describes the
committed head.

## 9. Raw gate exits at the candidate head

Runner: `run/logs/gates.txt` (this script echoes each raw exit INTO the log);
`CARGO_TARGET_DIR` was OUTSIDE the worktree for every cargo recipe (the
in-tree rustdoc-scanner interaction of earlier lanes is avoided), and
`test-build-archive.py` ran with it UNSET plus the same-head release binaries at
the standard path (its documented layout).

Per recipe, host environment: `fmt-check 0` · `check 0` · `lint 0` ·
`test 101` (one pre-existing host-environment test, section 11) · `doc 0` ·
`build-release 0` · `security 0` (scanner self-test `Ran 21 tests ... OK`,
public-tree scan clean, cargo-deny 0, cargo-audit 0, gitleaks `no leaks found`).

The exact aggregate: `just ci` = **101** in the host environment (stops at the
same one test) and `just ci` = **0** in the CI-shaped environment (no
discovered canter config — the same shape hosted runners have);
`just test` = **0** there with the full suite green (lib 285 passed, every
integration target ok, incl. the new `grant_issue` 14 passed).

Standalone gates: `check-contract-fixtures.py` **0** ("69 manifest
expectation(s) held (69 rows)"), `test-check-public-tree.py` **0**,
`check-public-tree.py .` **0**, `test-build-archive.py` **0** ("all checks
passed": alias member + provenance pins + offline SBOM + byte-identical
rebuild).

`test-build-archive.py` both ways (the brief's known artifact): green in
isolation (above) AND green inside a measured parallel-load window
(`run/logs/test-build-archive-load.log`: 8 pinned CPU burners on a 10-core
host, recorded `vm.loadavg` `{ 45.71 94.73 186.35 }` during the run) — same
raw exit 0, every check line PASS. The historical diagnostic-free one-line
`FAIL:` was NOT reproduced under this load in this lane, so no bare-`FAIL`
adjudication was needed; the load-window run is recorded for completeness
rather than claimed as a reproducer.

Focused at the head: `cargo test --locked --test grant_issue` **0**, 14 passed;
`cargo test --locked --test cli_smoke` **0**, 7 passed.

## 10. Delivered-head scans

After the report commit the tracked-tree scanners were re-run at the delivered
head (results recorded in the lane's final message): `check-public-tree.py .`,
`test-check-public-tree.py`, and gitleaks over a clean `git archive` export.

## 11. Pre-existing host-environment test artifact (not caused by this diff)

`commands::tests::missing_config_is_a_typed_config_error` assumes no canter
config is discoverable (`$HOME/.config/canter/config.toml`); this host HAS one
(587 B, created by the earlier #92 acceptance run), so `plan widgets 1` takes
the repository-resolution path and exits 2 instead of 5. Proven pre-existing by
running the SAME test at the BASE commit (`5de41a0`, scratch checkout
`/tmp/canter-92-base`) in the same environment: `test result: FAILED. 0 passed;
1 failed` (`/tmp/canter-92-base-test.log`, exit 101) — identical failure, so it
is a host-environment artifact, not a regression. Hosted CI (no such config)
runs it green: the CI-shaped local run above is green, and the test passes
locally once the discovered config is hidden (`XDG_CONFIG_HOME` = empty dir).

## 12. Not verified here / follow-ups

- Hosted CI and the independent review/merge are the orchestrator's next steps;
  nothing here claims them.
- `apply` STEP execution (spawn/prompt/Git effects) was not exercised — the
  dispatch/resume reach in section 7 is the boundary of the current surfaces.
- Out-of-scope observation (pre-existing, untouched): the `grants.list`
  projection renders `caps` as a JSON string while the minted document (and the
  `hf-grant/v1` validator) carry an array; the new surface returns the contract
  shape, and the list projection is left for a follow-up so no merged reader
  changes here.

## 13. Fix round 92-grant-R1 — verdict, three defects, all fixed (head `c28f278`)

Round contract: `.fix-r1.md` (addenda §7/§8). Verdict head `d55aeb2` failed the
orchestrator's battery with `cargo test --locked --lib` raw_exit=101
(`commands::tests::missing_config_is_a_typed_config_error`, panicked at
`src/commands.rs:6150:9`, `left: 2, right: 5`; 284 passed / 1 failed). Working
through the round surfaced TWO ambient-config couplings and ONE real product
hole; all three are fixed, and the two environment ones are proven pre-existing
at base `5de41a0`.

### 13.1 Defect 1 — the lib suite read the developer's ambient config

Reproduced at `d55aeb2` in the host environment (exactly the reported failure,
exit 101) and green with discovery rooted at an empty home
(`run/logs/fixr1-head-hostenv.log`, `run/logs/fixr1-head-emptyxdg.log`). The
same test at BASE `5de41a0` in the same environment fails identically and
passes with an empty home (`run/logs/fixr1-base-lib-hostenv.log`,
`run/logs/fixr1-base-lib-emptyxdg.log`), so this is env coupling, not the
grant-issuer diff: this lane's shell exports
`XDG_CONFIG_HOME=$HOME/.config`, and that directory holds a real
`canter/config.toml` (587 B, mtime 21:39:30 — written ~18 minutes BEFORE this
lane's session started at 21:57) whose `[daemon] socket` points at a sibling
lane's live acceptance socket. The test asserted the no-config branch while
discovery read that file. The product contract itself is intact at the head:
under an empty config home the release binary returns exit 5 with
`error.code = config.not_found` for `plan widgets 1`, `status` and
`config validate`; with the ambient config `plan widgets 1` keeps its
pre-existing usage path (exit 2).

Fix (commit `1c36d24`): `src/config.rs` gains a `#[cfg(test)]`-gated,
thread-local config-home override (`with_config_home`) consulted by
`config_path_in`; `missing_config_is_a_typed_config_error` keeps its assertion
EXACTLY (exit 5, `Kind::Error`, valid envelope, `config.not_found`, still the
discovery branch) and now roots discovery in a home that holds no config, and
is EXTENDED to the three commands its comment names.

### 13.2 Defect 2 — `rename_compat`'s daemon fixture inherited `XDG_CONFIG_HOME`

The first re-run made the lib suite green but `just ci` in the host environment
was still red at `tests/rename_compat.rs::pre_rename_state_tree_is_adopted_in_place_and_reused`
(`a daemon already serves /tmp/canter-accept/canter.sock`; the daemon refused
because the ambient config's explicit `[daemon] socket` replaced the DERIVED
socket that test exists to pin). BASE `5de41a0` fails identically in the same
environment (`run/logs/fixr1-base-rename-compat.log`). Fix (commit `36d3b97`):
the fixture pins its own empty config home (`XDG_CONFIG_HOME=<fixture>/c`), so
the adoption rule is proven regardless of ambient state.

### 13.3 Defect 3 — the grant id included the window (a real product hole)

The CI-shaped re-run at `36d3b97` failed
`tests/grant_issue.rs::a_crash_after_the_commit_keeps_exactly_one_grant`
(`no duplicate mint`: exit 0 where the typed `state.grant_exists` refusal was
expected). Not a flake: the content-addressed grant id included the requested
`expires_at`, so the same-binding refusal only held while both mints landed in
the same wall-clock second — a re-mint one second later computed a different id
and inserted a SECOND active grant for the same reviewed binding. Fix (commit
`6f8ebda`): the id's identity is the REVIEWED BINDING (repository, issue number
+ acceptance revision, workflow hash, policy hash, phase, scope, caps, live
state epoch); `created_at` and `expires_at` are deliberately not part of it, so
a re-mint with ANY window refuses `state.grant_exists` and the first minted
window stands. No schema change, no migration, no pre-check race (the refusal
is still the UNIQUE constraint); the minted document still carries the window
(AC2). `CHANGELOG.md` and `docs/contracts/spec-daemon.md` record the rule
(commit `c28f278`). Both same-binding refusals in the suite now re-mint with a
DIFFERENT window (7200 s vs 3600 s) and assert the live population stays exactly
one, and `run/logs/fixr1-window-determinism.log` runs each of them five times:
10/10 green.

### 13.4 Bite proofs (each RED → byte-exact restore → GREEN)

- `run/logs/fixr1-probe.log` — disabling the `#[cfg(test)]` seam puts the
  ambient config back in scope: `src/config.rs` sha256 `b38a747f…` →
  mutated `484b5067…` → RED rc 101 (`test result: FAILED`, panicked) → restored
  sha256 `b38a747f…` (matches original) → GREEN rc 0. `PROBE_RESULT=PASS`.
- `run/logs/fixr1-id-probe.log` — putting the window back into the id preimage
  (signature + preimage + call site, i.e. the pre-fix material):
  `src/commands.rs` sha256 `ac944cd4…` → mutated `fa178db2…` → RED rc 101 on
  `replay_returns_the_recorded_outcome_and_a_second_mint_never_duplicates` →
  restored sha256 `ac944cd4…` (matches original) → GREEN rc 0.
  `PROBE_RESULT=PASS`.

### 13.5 Shipped-behaviour parity (`cfg(test)` seam compiles out)

9 CLI cases × {fixed head, base `5de41a0`}: identical raw exits AND
byte-identical stdout sha256 per case — `plan widgets 1` / `status` /
`config validate` in both config states, an invalid document, an explicitly
missing `--config` path, and `--version`. `PARITY_RESULT=PASS (0 differing
cases)` in `run/logs/fixr1-parity.log` (+ `run/logs/parity/*.out`). One
documented normalization: the live `timings_ms:` wall-clock measurement line is
replaced before hashing (it is a measurement of the invocation, not output).

### 13.6 Gates at the frozen head (`c28f278`, clean tracked tree)

`run/logs/fixr1-gates.txt` (raw exits echoed into the log):

- `cargo test --locked --lib` — **0**, `285 passed; 0 failed` (the full count).
- focused targets — **0**: `grant_issue` 14, `cli_smoke` 7, `daemon_rpc` 13,
  `checkpoint` 7, `queue_cli` 2.
- `just ci` — **0** in the host environment (the environment that flagged the
  regression) AND **0** in the CI-shaped environment (no discovered config).
- whole suite in the host environment (`run/logs/fixr1-full-host-env.log`) —
  **0**: 38 targets ok, 0 FAILED, 620 tests passed.
- `scripts/check-contract-fixtures.py` — **0** ("69 manifest expectation(s)
  held (69 rows)"); `scripts/test-check-public-tree.py` — **0**;
  `scripts/check-public-tree.py .` — **0**; `scripts/test-build-archive.py`
  (isolated, `CARGO_TARGET_DIR` unset, same-head release binaries at the
  standard path) — **0** ("all checks passed").
- AC5 re-run at the frozen binary (`run/logs/e2e-92.log`, `SCRIPT_EXIT=0`,
  fresh ids): mint `gr_a7c4f999c3574c99` (binding as in section 2), a second
  mint refuses `state.grant_exists`, `queue submit` `qs_9ae31ba189431be9`
  admits one item (`run-2d579b0c98ccd15f`, `wi_c6fc6eb3128e5b2f`), `run pause`
  yields the resume digest `3f730c6f…`, a restart reads the identical paused
  state, and the real-PTY board authorises `qs_9427051700eceef0` and the run
  goes `paused -> running` (`BOARD_EXIT=0`, `RUN_STATUS_AFTER_BOARD_EXIT=0`).

### 13.7 Disclosures

- The ambient config file and its live acceptance socket belong to a sibling
  lane's run (created before this lane started) and were NOT modified or
  deleted; the suite is hermetic instead, so the same machine can now run
  `just ci` green with that state present — and, equally, green at base's
  behaviour for the shipped surface (parity above).
- Section 11 still stands as the record of round 1 (host-environment artifact);
  this round removes the coupling rather than the assertion.
- Deliberate boundary of the new identity rule: a revoked or consumed grant
  keeps occupying its reviewed binding within its epoch, so minting a fresh
  window for the SAME binding requires an epoch rotation (the documented
  invalidation mechanism). A re-mint is never a second concurrent
  authorization.
- AC5's boundary is unchanged: the submission statement itself records that no
  workflow step has been executed — the reachable "one bounded already-approved
  work item" is the durable run dispatch plus the board-authorized resume.
- Everything else in sections 12 (hosted CI, independent review/merge, and the
  `apply`-step reach) remains unverified here.

