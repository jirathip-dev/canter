# Fix round 1 — the CI suite matrix enumerates the new intake surface test; both recorded failures resolve (Refs #258)

- Repository: `jirathip-dev/canter` (public)
- Certified head reviewed (verdict fail): `dcb5c0ed5adeaa5f646aef38152f0a8d115edf71`
- Integration base: `9bdff98dedab9526ebbdeaa9d05efe1447f5f17b`
- Content commit carrying the repair (child of the certified head): `e61ee11bcaf728bc2b92dfdb836763fb2427ab60`
  — "The CI suite matrix enumerates the new intake surface test (Refs #258)":
  one row, `"--test queue_intake_surface"`, added to the closed
  `rust-ubuntu-suite` matrix in `.github/workflows/ci.yml` in ascending
  suite order.
- Feature branch: `issue-258` (this report commit rides on top of `e61ee11`)
- Rounds used: 1 of 3

## The two recorded failures, and what resolves them

Both recorded checks are the same defect seen from two sides: the delivery's
new integration suite `tests/queue_intake_surface.rs` is auto-discovered by
the CI driver but never joined the closed suite matrix.

1. `ci_matrix_completeness_guard_base_green_head_red` — the repository's own
   guard (`scripts/test-ci-test-driver.py`,
   `InvocationTests.test_matrix_enumerates_every_suite_once`: "matrix must
   match every `tests/*.rs` plus lib/bins/doc") is green at the integration
   base and red at the reviewed head, with exactly one row missing.
   Reproduced in this round from three trees, raw exits below: base
   `9bdff98` OK exit 0; reviewed head `dcb5c0e` FAILED exit 1 with the
   recorded payload; repaired head `e61ee11` OK exit 0.

2. `hosted_ci_rust_ubuntu_build_red_delta_attributable` — at the
   pre-repair delivery head `c4029a0` the hosted `rust-ubuntu-build` leg
   failed at the step "Test driver regression checks" with the identical
   payload (run `35802181000`, job `106994790236`:
   `AssertionError: Element counts were not equal: First has 0, Second has
   1: '--test queue_intake_surface'`), and every step behind it ("Package
   test build", "Upload test build", "Docs", "Release build") was skipped.
   At `e61ee11` both hosted runs are green, attempt 1 of 1 (census below):
   `rust-ubuntu-build` step "Test driver regression checks" success, and
   the newly enumerated cell `rust-ubuntu (--test queue_intake_surface)`
   itself runs and passes.

## Evidence at the content commit `e61ee11`

```
python3 scripts/test-ci-test-driver.py   (tree 9bdff98, integration base)  0   Ran 15 tests ... OK
python3 scripts/test-ci-test-driver.py   (tree dcb5c0e, reviewed head)     1   FAILED (failures=1)
                                                                               Element counts were not equal:
                                                                               '--test queue_intake_surface'
python3 scripts/test-ci-test-driver.py   (tree e61ee11, repaired head)     0   Ran 15 tests ... OK
python3 scripts/test-ci-test-scope.py    (tree e61ee11)                    0   Ran 18 tests ... OK (skipped=14)
```

The base tree was materialized from the repository itself
(`git archive 9bdff98 | tar -x`); the reviewed-head and repaired-head legs
ran in the lane checkouts named above. Logs (`guard-base.log`,
`guard-dcb5c0e.log`, `guard-e61ee11.log`, `scope-e61ee11.log`) live outside
the tracked tree with this round's artifacts.

## Hosted CI read-back

- Repaired head `e61ee11`: ci run `35803392722` — success, 51/51 jobs,
  1 attempt: `rust-ubuntu-build` (all steps success, including "Format
  check", "Locked check (all targets)", "Clippy (warnings denied)", "Build
  test executables", "Test driver regression checks", "Docs", "Release
  build"), every `rust-ubuntu (<suite>)` cell success including
  `--test queue_intake_surface`, `rust-macos` success, `policy` success,
  aggregate `rust-ubuntu` success. Security run `35803392704` — success
  (`supply-chain`, `secret-scan`).
- Pre-repair head `c4029a0`: ci run `35802181000` — failure, the
  `rust-ubuntu-build` red recorded by the reviewer; security success.

## This round's own delta and scope

The certified head `dcb5c0e` is an ancestor of the delivered head; no
reviewed commit was amended or rewritten (verified with
`git merge-base --is-ancestor`). The branch's lineage shows the one-row
repair `c4029a0` → `e61ee11` created before this round's instruction was
recorded (review resolution `2026-09-23T00:47:25Z`; repair commit
`2026-09-23T00:44:48Z`), so the repair content was already carried on the
branch when this lane started. This round therefore VERIFIED the repair
against both recorded checks instead of re-authoring it, made no further
code change (none was warranted — the one row is the whole fix, and the
suites it enumerates are green), and its own tracked-tree delta is this
report file only.

- `.report-258.md` (the reviewed delivery's evidence) is unchanged by this
  round: sha256 `ce00309b7d8c652b387c9786d401b9c9fe56b7815d300e88edfaef630983413a`.
- No `src/` file, no test file, no `.github/` file is touched by this
  round's commit.

## Local gate verification for this round

This round's commit is docs-only (one new file, no code, no workflow
change), so the tracked-tree scanners and the two CI driver script gates are
the applicable local gates; they are re-run against the delivered head's
bytes (staged index before the commit, re-run at the commit) and their raw
exits are carried with the lane delivery:
`python3 scripts/test-check-public-tree.py`,
`python3 scripts/check-public-tree.py .`,
`gitleaks dir --no-banner --redact --exit-code 1 .`,
`python3 scripts/test-ci-test-driver.py`,
`python3 scripts/test-ci-test-scope.py`.

## Disclosed gaps

- The repair commit `e61ee11` predates this round's dispatch; this round's
  own authored delta is the report file. The verification legs above are
  this round's own measurements against the delivered head.
- No complete local `just ci` aggregate was re-run in this round: the
  commit under it is docs-only and the code content is byte-identical to
  `e61ee11`, whose full hosted CI (including the two script gates, run
  from the hosted runner) is green and censused above. Any aggregate run at
  the report commit would re-certify that same code content.
- The hosted runs for THIS report commit are discovered after the push with
  `gh run list --repo jirathip-dev/canter --branch issue-258`; this report
  predates them, so that census is carried with the lane delivery rather
  than asserted here.
