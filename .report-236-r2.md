# Issue #236 — fix round 2, the naming half: the refusal producer names the lanes that occupy the cap

Round answers the reviewer verdict, verbatim:

> VERDICT: FAIL — AC2's naming requirement is not delivered on every refusal
> producer: check_fanout_admission (src/lifecycle.rs, the site the issue names)
> [plus one more].

Base under repair: `2931485` (round-1 head, PR #237). This round's code commit:
`8e06159` (10 files, +509/-170 against `2931485`). Nothing of the
retire-lane-records half of #236 is touched.

## 1. The naming is part of the refusal the producer raises

`src/lifecycle.rs`:

- `LaneFootprint:343-356` carries the lane's identity facts — `issue_number`
  and `identity` (the recorded run / instance id). `0`/empty means the caller
  recorded none; nothing is invented.
- `lane_occupants():368-394` (`LANE_OCCUPANTS_MAX` at `361`) is the ONE authoritative occupancy rendering:
  `repository#issue (run on scope)`, bounded by `LANE_OCCUPANTS_MAX = 32` (the
  same window the preview's `concurrency.lanes` rendering uses —
  `src/queue_preview.rs:63-66` now aliases that constant) with an explicit
  `, +N more` remainder.
- `occupant_clause():399-405` adds ` (N active lanes: names)` (harness axis:
  `attested lanes`) ONLY when the axis is actually occupied, so an axis no lane
  occupies keeps the refusal text it always had.
- `check_fanout_admission():530-626` (the three refusals at `561`, `579`, `598`) — the global, per-repository and
  per-harness refusals each carry the count, the declared cap and the occupants
  of that axis (global: every counted lane; repository: the counted lanes of the
  proposed repository; harness: the counted lanes on that harness key). The
  counted-lane set, the caps, the decision order and the monorepo-overlap check
  are unchanged.

Occupant identities are whatever each caller actually records (`as available`):

| refusal producer | count | cap | identities named |
| --- | --- | --- | --- |
| daemon `apply` gate — durable rows (`src/daemon.rs:1307-1413`, lanes at `1372-1398`) | yes | yes | run id, issue, declared worktree scope |
| daemon `lane.start` gate — caller-attested lanes (`src/daemon.rs:7568-7689`, lanes at `7636-7674`) | yes | yes | declared worktree scope |
| queue preview — durable rows (`src/queue_preview.rs:1163-1282`) | yes | yes | run id, issue, declared worktree scope |
| durable waiting verdict — durable rows (`src/state.rs:692-797`, `hold()` at `752-797`) | yes | yes | run id, issue, declared worktree scope |

The durable per-harness waiting verdict names its attested count only: harness
occupancy is client-attested and no per-harness lane rows are recorded, so that
one axis has no identity to name (`src/state.rs:780-796`). The daemon gate and
the preview do name the harness key on that axis.

## 2. Every consumer renders that one fact

No surface re-derives the occupant string:

- daemon RPC payload — the gate's own `err.message` (`src/daemon.rs:1406-1413`,
  `7678-7685`).
- durable waiting verdict -> `queue status --json` (`items[].message`,
  `advance.held.message`) and the board's `waiting [{code}] {message}`
  (`src/tui/operator.rs:1438` renders that recorded message unchanged).
- `queue status` human text — each item's and the held row's own message
  (`src/queue_executor.rs:1778-1783`, `1807-1811`); before this round the human
  text showed only the refusal code.
- `queue preview` — hold messages in `--json` (`holds[].message`) and, new this
  round, the named holds in the human text (`src/commands.rs:5235-5255`).
- `src/state.rs:692-797` derives both counted axes from ONE counted lane set
  (ordered by run id, so one durable state renders one message) instead of a
  per-axis SQL query; the private occupant renderers round 1 had added in
  `src/state.rs` and `src/queue_preview.rs` are gone. One renderer, four
  consumers.

## 3. Witnesses (RED/GREEN, raw exits)

New or extended, each against a real code path:

1. `src/lifecycle.rs:1288` `cap_refusals_name_the_count_the_cap_and_the_occupying_lanes`
   — TWO occupying lanes per axis; asserts count, declared cap and both lanes'
   identities on the global, per-repository and per-harness refusals, and that a
   lane on another harness is never attributed to this one.
2. `src/lifecycle.rs:1385` `a_cap_refusal_is_unchanged_when_no_lane_occupies_the_repository`
   — a lane-free repository raises byte-identically the refusal it always raised;
   lanes of another repository never exhaust its axis.
3. `tests/start_adopt.rs:1190` — over a real daemon socket, the `lane.start`
   refusal in the RPC payload names both attested lanes.
4. `tests/queue_submit.rs:1143` — the durable waiting verdict names the holder:
   count, cap, issue, run id and declared worktree.
5. `tests/queue_cli.rs:362` — the CLI human text of `queue status` carries the
   producer's own message for every item, not only `--json`.
6. `tests/commands.rs:650` — the CLI human text of `queue preview` carries each
   hold's code and the producer's own message.

RED (mutation probes; every mutation reverted by `cp` and verified byte-for-byte
by `shasum -a 256`):

| probe | mutation | command | raw exit |
| --- | --- | --- | --- |
| the producer names no occupant | `occupant_clause` returns the empty string (the pre-#236 text) | `cargo test --locked --lib cap_refusal` | 101 (`/tmp/canter-236f1/red-lib-cap_refusal.log`) |
| the consumers name no occupant | `render_human` message lines removed; the preview human holds removed; the durable repository verdict back to a bare count | `cargo test --locked --no-fail-fast --test queue_cli --test queue_submit --test commands` | 101 (`/tmp/canter-236f1/red-consumers-nff.log`) |

Restore proof: `/tmp/canter-236f1/mutback/before.sha256` vs `after.sha256`
(three files) and `mutback/queue_submit.before.sha256` vs `.after.sha256` are
identical; no probe marker remains in the tree.

GREEN (raw exits):

- `cargo test --locked --lib cap_refusal` — exit 0
  (`/tmp/canter-236f1/green2-lib-cap_refusal.log`): 3 witness tests pass.
- `cargo test --locked --no-fail-fast --test queue_preview --test queue_cli
  --test queue_submit --test commands --test start_adopt --test queue_advance
  --test run_control` — exit 0 (`/tmp/canter-236f1/green2-consumers.log`):
  86 tests pass across the touched surfaces.

## 4. One example refusal line (raw)

Printed by witness 4 in a `--nocapture` run (`/tmp/canter-236f1/green-example-line.log`):

```
the per-repository concurrency cap (2) is reached (1 active lanes: example-org/widgets#7 (run-owned-wire-0001 on worktrees/issues/7)); the item waits
```

## 5. Gates — `just ci` and the pre-existing red on this host

`just ci` = fmt-check -> check -> lint -> test -> doc -> build-release -> security.

| run | raw exit | log |
| --- | --- | --- |
| `just ci` (aggregate, fail-fast) | 101 (failed at `test`) | `/tmp/canter-236f1/just-ci.log` |
| `just fmt-check` | 0 | `/tmp/canter-236f1/per-recipe-exits.log` |
| `just check` | 0 | same |
| `just lint` | 0 | same |
| `just test` | 101 | same (same pre-existing class, below) |
| `just doc` | 0 | same |
| `just build-release` | 0 | same |
| `just security` | 0 | same — `gitleaks 8.30.1` is installed and the recipe ran it; a missing gitleaks is a failure, not a skip |

The `test` step is intermittently red on this host for two load/wall-clock
sensitive classes, and the failing member MOVES between runs; the base is red
the same way. Failure sets, with raw exits:

| head | command | raw exit | failing tests |
| --- | --- | --- | --- |
| `2931485` (this round's base) | `cargo test --locked --no-fail-fast` | 101 | 3 (all `tests/review_dispatch.rs` timeout chains), 46 suites: 815 passed / 3 failed — `/tmp/canter-236f1/base-full-tests.log` |
| `1f47577` (staging, pre-#236) | `cargo test --locked --test review_dispatch` | 101 | the same 3 — `/tmp/canter-236f1/staging-review_dispatch.log` |
| `8e06159` (this round) | `cargo test --locked --no-fail-fast` | 101 | 1 DIFFERENT test (`tests/grant_issue.rs` 1-second expiry window: the 2s-later mint took longer than the window under load), 46 suites: 819 passed / 1 failed — `/tmp/canter-236f1/head-full-tests.log`; the 3 review_dispatch tests PASSED in this run |
| `8e06159`, isolated | `cargo test --locked --test grant_issue an_expired_window_stops_being_consumable_with_the_typed_code` | 0 | the grant-expiry test passes alone — `/tmp/canter-236f1/rerun-grant-expiry-isolated.log` |

Test-count arithmetic: base 815 + 3 = 818 total -> head 819 + 1 = 820 total
(delta +2 = this round's two new unit tests; no suite silently lost tests).

Classification: contention/wall-clock, not regression. The failure set changed
members between runs at the same code and includes base-only failures; the host
was heavily loaded throughout (1-min load average 16-75, ~13% idle at the peak,
other fleet/tool processes running), and both classes are sub-2-second timing
budgets (`effect.review_timeout` vs `adapter.timeout`; a 1-second grant window).
This round's diff touches no timing, adapter, mutation, review-dispatch or
grant code, and every suite that covers the diff is green. The pre-existing red
is NOT fixed here (out of scope).

Environment note: the lane build-cache volume `/Volumes/NVMe2TB` (external) went
away mid-round — the lane's `target` symlink is dangling from that point on. The
completion run above therefore used a temporary internal-disk
`CARGO_TARGET_DIR`; no tracked file was changed for that, and the `just ci` and
per-recipe runs listed here ran before the volume went away.

## 6. What this round does NOT claim

- The retire-lane-records half (#236 items 1/3: a run's lane records retire with
  it, and the bounded operator control) is untouched and still open.
- The durable per-harness waiting verdict cannot name harness occupants: no
  per-harness lane rows exist (occupancy is client-attested), so only the count
  is available on that one axis.
- The `lane.start` path's attested lanes record repository, harness key and
  scope only, so its refusal names the declared worktree and no run id (there is
  none on that path).
- The pre-existing `just test` red (timeout-class tests above) is not fixed.
