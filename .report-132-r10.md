# Issue #132, round 10 — the reviewed published-ref cleanup proof, replayed onto integration base `e1a8c29`

Lane: canter's own implementer dispatch (run `run-32f4bc3df0565189`, step
`p4-132`), worktree `issues-132`, branch `issue-132`, based on the run's
recorded integration base `e1a8c2951a91ac8ac86b8f64155615da6a3f201a`
(`origin/staging` at dispatch time; carrying #202 via PR #205).

Content head: `7c9c907571bedb307cc92c13aca87a4303ef2228`. This report's own
commit follows it, so it can name it.

## What this round delivers, and why it is the bounded change

Issue #132's own text names the shape that must hold: *"the run's `merge` step
must never leave the integration checkout diverged from the published
integration ref, and the run must be able to complete on a repo whose policy is
squash."* The merge-side half is already in the base (#156/#168/#178/#182, and
#196 lands and publishes under the plan's `merge_policy`). The still-unlanded
half is the `cleanup` side: **when the sanctioned landing happens on the forge
(the PR is squash-merged), the checkout's own integration ref never sees it, so
`cleanup` must prove the landing against the FETCHED, verified PUBLISHED ref —
and only a checkout strictly behind it may certify from that view.** That
content is independently PASSed in the review lineage
(`REVIEW-183-4dc2ae22`, re-verified as `REVIEW-183-c05cc99`; latest
`REVIEW-183-1ba80d60` names the reviewed head exactly) and is replayed here onto
the rebuilt base — the review lineage's own refresh shape, never a re-design:

- `LandedProof::Published { published_head, merge_base }` — the third landed
  proof (ancestry / local content / published content), made against the
  fetched published head.
- `landed_in_published` — reads the published head
  (`published_integration_head`), refuses when the checkout's own ref IS the
  published view (no self-certification), FETCHES and verifies it
  (`fetched_published_head`), refuses whenever the local ref is not a strict
  ancestor of it (an unpublished or diverged local move is never certified),
  then proves the branch head by ancestry against the fetched head, or — for
  the squash landing, which never is an ancestor — by exact content on every
  path the branch changed relative to the fork point.
- `effect_cleanup` — when the local proof refuses
  `refusal.cleanup.unmerged`, tries the published route; if that view adds no
  proof, the ORIGINAL local refusal is returned unchanged. The salvage record
  of a published proof names `published_head` (and `merge_base` when the proof
  was by content) while keeping the checkout's own `integration_head`;
  deletion uses `-D`.
- `landed_in_integration` now uses the base's shared fail-closed comparison
  helpers (`changed_paths_between`, `differing_paths_between`, landed with
  #196) instead of the round-5 private helper, so the local and the published
  proofs cannot drift: one NUL-delimited, `--literal-pathspecs`, no-rename
  listing side, one comparison side, one empty-delta confirmation.

Everything else in the issue's text is already satisfied by the base and is
not re-implemented here: the policy input (`merge_policy: squash | ff`), the
read-only-rehearsal refusal polarity with `MERGE_NOT_FF` naming the real cause
("an unpublished or diverged local view is never reconciled" / "a squash
result is not an ff merge"), the published-ref certification in `effect_merge`,
and the #178 reconciliation loop.

## Replay equivalence (hunk-level)

The replayed diff (`git diff e1a8c29 7c9c907` restricted to the eight delivered
paths) and the reviewed diff (`git diff 2d3d1ef8 1ba80d60` on the same paths),
with `index` lines and `@@` hunk headers removed, are BOTH 906 lines and
byte-identical (`diff` exit 0) — no reviewed hunk is altered in the replay.

The tree delta against the reviewed tree is exactly the intervening #205 work:
`git diff 1ba80d60 7c9c907 -- src/mutation.rs tests/mutation_engine.rs` hunk
bodies are byte-identical to `git diff 2d3d1ef8 e1a8c29` hunk bodies (218 lines
each, `diff` exit 0). So `7c9c907`'s tree = the reviewed tree + #202/#205,
exactly.

| File | sha256 at this head | vs reviewed head `1ba80d60` |
| --- | --- | --- |
| `src/mutation.rs` | `b4ff7a49b78366494510a376408c9d6ab1d9c98a31dbca9b2dcf9db3dbd026e4` | differs — #205 touched it; hunk bodies identical |
| `tests/mutation_engine.rs` | `45c4c58c829ba31d536e20b1eb65e56034290b6923734a215ae12598cc34be0b` | differs — #205 touched it; hunk bodies identical |
| `CHANGELOG.md` | `6bcec695b232c3577b489718b628e140e8b8508f3244e112535b6be288aac2d5` | byte-identical |
| `docs/contracts/spec-lifecycle.md` | `0a07ddd46925dba7a4b66acfe31d529c044759bdcaf811558f754675c12b23f7` | byte-identical |
| `docs/contracts/spec-plans.md` | `5cf7bbc06df8451a8e12b51e1416ac330bccd602f62aa4f73697676878fde83e` | byte-identical |
| `.report-132-r7.md` | `c16d34ff521457fa80c5361d5e9520b0ecee0d282bfba66dece345f9456d07b3` | byte-identical |
| `.report-132-r8.md` | `e1dd06f97531c859fa1b308580b22e6a9ef4794a0aa541636b9ff7bd408fe16f` | byte-identical |
| `.report-132-r9.md` | `93312eaeb0b6e99b4de47e2486bd2df876a6f4efc3b7198903e911a583ef7564` | byte-identical |

The prior rounds' reports are carried byte-identical, so their narratives still
name their own bases and heads.

## The witness (real daemon, real git trees, synthetic bare remote)

`tests/mutation_engine.rs::cleanup_certifies_a_squash_landing_visible_only_on_the_published_ref`
(this round's replay carries it; the suite is also exercised by `just ci`):

1. A reviewed delivery is pushed to the bare remote and a SECOND clone
   performs the policy squash there (`merge --squash` + commit + push), so the
   published ref moves while the integration checkout's own ref never sees the
   landing (asserted).
2. `cleanup` (real daemon RPC) then returns `removed:true`, the salvage names
   `landed_by:"content"` and the exact `published_head` the proof was made
   against, keeps the checkout's own `integration_head`, the checkout's own
   ref is still at its base (never moved), the worktree is gone and the lane
   branch is deleted.
3. Second leg: the same published landing while the checkout's own ref is
   AHEAD of (an unpublished local move) it refuses `refusal.cleanup.unmerged`
   and deletes nothing.

## Gates (raw exits; named logs — filenames only)

One full aggregate invocation plus the remaining recipes, all raw exits quoted
from the named logs (filenames only; the host carried other lanes' work
throughout at 1-minute load 36–47):

| # | Command | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `just ci` (one invocation) — log `just-ci.log` | 101 | `fmt-check` / `check` / `lint` green. The test step ran 39 suites: **38 ok (743 passed)** then `tests/supervision.rs::collection_invalid_base_refuses_before_any_worker_poll` panicked `read_to_string(&log).unwrap()` → `Os { code: 2, kind: NotFound }` at `tests/supervision.rs:3659`, after its `effect.worker_timeout` assertion had already held: under the load the fixture's injected one-second deadline expired before the fake worker subprocess wrote its first poll row. No assertion failed, and this delta does not touch `tests/supervision.rs`. |
| 2 | `cargo test --locked --test supervision -- collection_invalid_base_refuses_before_any_worker_poll --test-threads=1` (serialized re-run of that selection) — log `supervision-focused.log` | 0 | 1 passed; 0 failed (3.09 s) |
| 3 | `just doc && just build-release && just security` (one invocation) — log `gates-doc-build-security.log` | 0 | `cargo doc --locked --no-deps` generated the docs; `cargo build --release --locked` compiled the release binary; scanner self-test + scan + supply-chain checks green, `gitleaks directory scan … no leaks found` |

Focused witness evidence:

| # | Command | Raw exit | Result |
| --- | --- | --- | --- |
| 4 | `tests/mutation_engine.rs` inside row 1's test step | (row 1) | the suite is green: `ok. 33 passed; 0 failed` (26.65 s), and its result list includes this round's replayed witness `cleanup_certifies_a_squash_landing_visible_only_on_the_published_ref ... ok` |

### Adjudication

- Row 1's single failure is in an UNTOUCHED suite (`tests/supervision.rs`; the
  delta's own diff is the eight paths above) and is a fixture-timing shape
  (subprocess start vs a 1 s injected deadline under load), with no assertion
  failure; row 2 re-runs that exact selection serialized and green in 3.09 s.
- `tests/mutation_engine.rs` — the suite that carries the replayed witness —
  is green inside the same aggregate (row 4).
- The delivered head adds only this report file to the content commit; a
  `just fmt-check` + `just security` re-run at the delivered head is recorded
  in the lane's final report.

## Not delivered / not claimed

- No production behavior change beyond the replayed, previously reviewed
  published-ref cleanup proof; nothing is re-designed in this round.
- The live end-to-end acceptance spine (`p1`..`p8`, zero operator keys) is the
  orchestrator's to drive: this lane did not dispatch, pause, retry, or mutate
  any run, daemon, or state, and did not touch `staging`.
- Hosted CI for this branch is reported by the pull request, not by this
  report; the local aggregate's raw exits above (and any load-flake
  adjudication) are quoted from the named logs.
