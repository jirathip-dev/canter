# .report-270-r2.md — canter #270's delivered pass bound, re-witnessed at the r2 drive base

Lane: `issue-270` — canter queue submission `qs_5ac6cd96e13bf6d1`, item
`jirathip-dev/canter#270`, run `run-95b8ad129ad152fa`, worktree scope
`worktrees/issues/270`. Pinned revision (the submission's `issue_revision`):
`b753fa3214eccc63456e2b66b43114d31689b230` — identical to this lane worktree's
HEAD and to `origin/staging` at the time of writing. This PR's delta is this
report only: the issue's required fix is already implemented, merged and
installed at the pinned revision, so the honest delivery record is the
re-verification of its witnesses at that revision (§2–§4). No production code
is changed; `Refs #270`.

## 1. Preflight — the fix is contained at the pinned revision

- Delivery: PR #273 "Bound a supervision pass: a sleeping lane call is
  abandoned typed and named on the surface (Refs #270)" — head branch
  `impl-270`, pushed head `6a6530d39a9fc8a5b0ef366853c7034cd25b1939`, merged to
  `staging` 2026-09-24T14:05:21Z as the squash merge commit
  `4fc473cc8213aa45b2707ff1a73a2868b0a2b226` (single parent
  `86e8579c1ec0db6598b60089d595821e573ccacf`).
- `git merge-base --is-ancestor 4fc473cc… HEAD` — raw exit `0` (contained).
- Tree identity: `git rev-parse 4fc473cc^{tree} origin/impl-270^{tree}` — both
  `e65b7bc5cbd8d32079f07407ec99301a78117456`, so the squash carries exactly the
  delivered tree; the merge lost nothing.
- `git log --oneline origin/staging..HEAD | wc -l` — `0` before this report's
  own commit: the branch carried nothing of its own.
- The r1 lane's report `.report-270.md` is tracked at HEAD, and every piece of
  the fix is present at HEAD: `src/supervision.rs` (`PASS_DEADLINE_SECS`,
  `dispatch_bounded`, the per-effect `canter-supervision-effect` thread),
  `src/adapters/herdr_lane.rs` (the gone-checkout guard; the file is unchanged
  since the merge — `git log 4fc473cc..HEAD -- src/adapters/herdr_lane.rs` is
  empty), `src/adapters.rs` (`CODE_INCOMPLETE_IDENTITY`, its guard and unit
  witnesses), `tests/supervision_pass_deadline.rs` (the daemon-level suite) and
  its `rust-ubuntu-suite` matrix entry in `.github/workflows/ci.yml`.
- The issue's recovery comment narrowed the witness shape: the measured sleep
  inside `close_lane_workspace` was finite (~17 minutes) rather than an absolute
  deadlock, so the witness is a close-lane call that sleeps *past the deadline*
  (§2, §3), not one that never returns.

## 2. The delivered witnesses re-run at the pinned revision (GREEN)

All runs in this lane worktree at the pinned revision, `CARGO_TARGET_DIR`
lane-local (outside the checkout); each raw exit captured before its log was
read:

| Command | Raw exit | Result |
| --- | --- | --- |
| `cargo test --locked --lib a_close_lane_call_that_sleeps_past_the_pass_deadline_is_abandoned_typed` | `0` | `test result: ok. 1 passed; 0 failed` (3.88 s) |
| `cargo test --locked --lib a_gone_lane_worktree_resolves_typed_without_any_herdr_call` | `0` | `test result: ok. 1 passed; 0 failed` |
| `cargo test --locked --lib a_lane_worktree_that_exists_still_reaches_the_substrate` (positive control) | `0` | `test result: ok. 1 passed; 0 failed` |
| `cargo test --locked --test supervision_pass_deadline -- --test-threads=1` | `0` | `test result: ok. 1 passed; 0 failed` (11.20 s) |

What each witness proves, at this revision:

- **AC1a** (unit): the REAL `close_lane_workspace` runs against a fake `herdr`
  whose lane row sleeps 3 s, dispatched through `SupervisorCore::dispatch_bounded`
  with a 1 s deadline; the call returns in < 1500 ms (the pre-fix shape held the
  whole sleep on the driver thread), the surface reads `stalled` naming the
  run/`p8`, `abandoned_passes == 1`, and the substrate saw exactly one close.
- **AC1b** (daemon): run A's driver-dispatched lane registration sleeps 8 s; run
  B's committed `evaluation.checks` ADVANCES while run A still reads
  `driver.state == stalled`, and B's `evaluation.last_check.at` is not older
  than the stalled pass's own `started_at`. A stall is only ever reported while
  the effect is unresolved, so a tick observed under it landed on the stuck pass.
- **AC2**: `daemon status` reads `freshness: stalled` (never `fresh` over the
  stall), `supervision.state: stalled`, `supervision.pass.run`/`step`
  (`p2`)/`age_secs >= cadence`, `abandoned_passes >= 1`; `supervision.status`
  carries the same document as `driver`; the human line names run, step, age,
  bound and the abandoned count.
- **AC3**: the substrate's sleeping lane row ran exactly ONCE
  (`herdr-slow.txt` = 1 line), run A's `p2` attempt ledger holds exactly one
  attempt (no double close), and the stall clears when that effect resolves —
  the effect keeps its own apply claim on its own thread.
- **AC3b** (the issue's live specimen): with the registered checkout directory
  GONE, `close_lane_workspace` refuses `refusal.identity.incomplete` naming the
  missing checkout and `retire_lane_workspace` is the bounded no-op — with the
  fake `herdr` never invoked. Positive control with the same call and an
  EXISTING checkout: the workspace read-back does run, so the guard cannot hide
  a live lane.

## 3. Discrimination probe — the witnesses bite at THIS revision

Two mutations on the committed tree (the content head above), each applied by
literal replace asserted to match exactly once, restored by copy (no
`git reset`, `git checkout` or `git stash`), sha256 recorded on both sides:

- MUTATE 1 — the deadline wait is no longer bounded by the pass deadline:
  `match receiver.recv_timeout(bound) {` →
  `match receiver.recv_timeout(Duration::from_secs(600)) {` in
  `dispatch_bounded` (the pre-fix shape). `src/supervision.rs` sha256
  `1270fd64678409353b2efcd6e80ae5213ac66b38a433b063094c9de799443b49`
  → `5e02d935f020dddce4dba3c578ab2308dc769784d529d27ec276f28d6bdb78e1`.
  - **RED** — raw exit `101`: `the pass must not wait for a close that sleeps:
    waited 3.5749205s`.
- MUTATE 2 — the gone-checkout guard is defeated: `if !worktree.is_dir() {` →
  `if false {` in `close_lane_workspace` (a gone target is ADDRESSED again, the
  pre-fix shape). `src/adapters/herdr_lane.rs` sha256
  `853e1bf8b8f84e29801eda9b128a834d974742b55aa361849544218051ba8579`
  → `0edf6bb4919d93d62947ad0cacb9255999c4bf84ac9a61fd4e1677bfeab927bb`.
  - **RED** — raw exit `101`: `assertion left == right failed /
    left: "refusal.unavailable.herdr"` (the refusal expected is
    `refusal.identity.incomplete`).
- RESTORE + byte proof: `1270fd64…` and `853e1bf8…` again — byte-identical to
  the committed content; `git status --porcelain` empty.
- GREEN again at the restored content: both witnesses raw exit `0`.

Raw exits and logs: `probe-raw-exits.txt`, `probe-shas.txt`,
`probe-green-*/probe-red-*/probe-green2-*.log`.

## 4. Acceptance criteria

| AC | How it is satisfied at this revision | Witness |
| --- | --- | --- |
| AC1 — a pass that blocks beyond its deadline is abandoned typed and the driver thread continues; the OTHER runs' ticks advance while it is stuck | §2 rows 1 (the deadline bound returns, typed surface) and 4 (run B's committed checks advance under run A's reported stall); §3 MUTATE 1 proves the bound bites | raw exit `0` / `0`; probe RED `101` |
| AC2 — the stall is visible on the product surface, naming the stalled pass and its run/step, before an operator notices frozen `last_check` stamps | §2 row 4: `freshness: stalled`, `supervision.state/pass.run/step/age_secs/abandoned_passes` | raw exit `0` |
| AC3 — no double-close and no lost claim; the abandoned effect resolves exactly once, typed | §2 rows 4 (substrate row once, one `p2` attempt, stall clears on resolution), 2 (gone-checkout refusal typed, no substrate call) and 3 (positive control) | raw exits `0` / `0` / `0`; §3 MUTATE 2 RED `101` |
| AC4 — `Refs #270` wording; no direct push to `main`; no gate bypass; no host paths, private repository names, profile names or model names in added lines | commit message says `Refs #270`; PR targets `staging`; `just ci` is the repository aggregate, nothing skipped; the scanner legs of `just security` re-run at the tip cover this file itself | §5 gates table |

## 5. Gates (raw exits, named logs)

`just --list` was run once; the canonical aggregate is `just ci`
(fmt-check → check → lint → test → doc → build-release → security).

| # | Command | Raw exit | Log |
| --- | --- | --- | --- |
| 1 | `just ci` (full aggregate, attempt 1) at the content head | **101** at `test` / `tests/mutation_engine` | `logs/just-ci-content-head.log` |
| 2 | `cargo test --locked --test mutation_engine -- --test-threads=1` (attempt-1 adjudication) | **0** — `41 passed; 0 failed` (305.93 s) | `logs/mutation-engine-serial.log` |
| 3 | `cargo test --locked --test mutation_engine` (parallel re-run) | **101** — the same single test | `logs/mutation-engine-parallel.log` |
| 4 | `cargo test --locked --test mutation_engine disconnected_apply_is_resolved_after_its_effect_deadline_without_restart` (that test alone, parallel) | **0** — `1 passed` (3.19 s) | `logs/mutation-engine-single.log` |
| 5 | `just fmt-check` / `just check` / `just lint` at the content head | **0 / 0 / 0** | `logs/leg-fmt-check.log`, `logs/leg-check.log`, `logs/leg-lint.log` |
| 6 | `just doc` / `just build-release` / `just security` at the content head (after the test-leg flake) | **0 / 0 / 0** — scanner self-test `Ran 21 tests … OK`; `advisories ok, bans ok, licenses ok, sources ok`; gitleaks `no leaks found` | `logs/leg-doc.log`, `logs/leg-build-release.log`, `logs/leg-security.log` |
| 7 | `python3 scripts/test-ci-test-driver.py` | **0** — `Ran 19 tests … OK (skipped=1)` | `logs/ci-test-driver.log` |

**Attempt 1 adjudication (disclosed, not hidden):** the aggregate died in its
fail-fast `test` leg at `tests/mutation_engine`, test
`disconnected_apply_is_resolved_after_its_effect_deadline_without_restart`,
panic `effect never started` (mutation_engine.rs:1147 — the fixture's effect is
launched by the scenario daemon and the test waits up to 10 s for its marker).
That is the suite's documented pre-existing fixture-timing flake class on this
host, not lane code — nothing in this lane touches `src/state.rs`,
`src/mutation.rs` or the apply path (the delivered diff is a report file): the
suite is green serialized at the SAME head (row 2: 41/41, 306 s) and the failing
test is green run alone in parallel (row 4, 3.19 s). Rows 3–4 reproduce the
serialized/parallel split so the adjudication is not a single green sample.
Host load during the aggregate's test leg: 1-minute averages ~19–25 (other
fleets); at the adjudication and rows 5–7: ~7–15.

The delivered tip differs from that gated content head ONLY by this report file,
so the tree-reading legs (`just security`, `cargo fmt --check`,
`cargo check --locked --all-targets`) are re-run at the tip and their raw exits
are recorded in the PR body (this report cannot cite a run that happens after
its own commit without moving the head again). No gate was piped through `grep`
as a pass/fail test; every raw exit above was captured from the command itself
before its log was read.

## 6. What was NOT verified

- **No single `just ci` invocation ran fully green in one go on this host.**
  The one full aggregate (row 1) died at the `test` leg on the documented
  `mutation_engine` fixture flake named above; its earlier legs
  (`fmt-check`/`check`/`lint`) were green in that same run, and the remaining
  legs were run individually at the same head and are green (rows 2, 5–7). The
  flake class is pre-existing (it is named in `.report-270.md` §3, the r1 lane's
  own record, with the same panic text) and it is not reachable from this
  diff — but the honest statement is that one green full-aggregate invocation
  was not obtained here.
- Hosted CI on this PR head (the workflows run on the PR; the local aggregate
  is what this report records).
- The independent exact-head review verdict — the fleet's review leg is that
  gate.
- The live specimen itself was not touched: the wedged daemon the issue measured
  is fenced off from this lane, and nothing here restarts or mutates any live
  run. The fix prevents the next wedge; it does not retroactively unwedge one.

## 7. Boundary

Read-only throughout the repo side: no `main`, no direct push to `staging`, no
gate bypass, no force-push. No canter run control ran (no release/pause/resume/
retry/reevaluate/retire-lane/dispatch, no `queue submit`/`redrive`, no new run);
the queue/run/audit facts quoted above are SQLite `mode=ro` and `canter …`
read-only reads. This report names no host paths, no private repository names,
no profile names, no credentials and no provider/model policy.
