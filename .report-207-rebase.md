# impl-ordering — rebase of #207's certified-head fence onto the current `staging` (Refs #207)

Rebase record and supersession of the branch's stale pre-rebase report. This
file is the CURRENT record for the branch `impl-ordering`; the earlier
`.report-ordering.md` is kept for its audit trail under a supersession banner.

## Delivery (read back FROM ORIGIN)

| fact | value |
| --- | --- |
| branch | `impl-ordering` (PR #213, base `staging`) |
| pre-rebase branch tip, local AND remote | `0afc0d5581a1d33e8d709b028ae405e50bb6385c` (remote read with `git ls-remote origin refs/heads/impl-ordering` before any edit: identical) |
| pre-rebase merge base | `e1a8c2951a91ac8ac86b8f64155615da6a3f201a` |
| new integration head (base of the rebase) | `7b43ce9bc490ac9af94374ba0b22af455b536397` (`git fetch origin staging` + `git rev-parse origin/staging`) |
| rebased FIX commit | `850fa821c66bb3639d997a8c4da3acb8db2a7ab0` — the ORIGINAL message of `026eac9`, verbatim |
| `git merge-base HEAD origin/staging` after the rebase | `7b43ce9bc490ac9af94374ba0b22af455b536397` — EQUAL to the recorded integration head |
| pushed head read back FROM ORIGIN (the rebased fix commit) | `850fa821c66bb3639d997a8c4da3acb8db2a7ab0`. Raw read-back: `git push --force-with-lease=impl-ordering:0afc0d5581a1d33e8d709b028ae405e50bb6385c origin impl-ordering` printed `+ 0afc0d5...850fa82 impl-ordering -> impl-ordering (forced update)` (exit 0); `git ls-remote origin refs/heads/impl-ordering` then printed `850fa821c66bb3639d997a8c4da3acb8db2a7ab0<TAB>refs/heads/impl-ordering`, equal to the local `HEAD` at that moment (`.lane-logs/push-fix.log`) |
| this report commit (the branch's new tip) | it DESCENDS from the read-back head above (a plain fast-forward follows, no second force); a commit cannot name its own sha, so the tip's origin read-back is the value the lane's closing message reports — never invented here |

`git diff --name-only origin/staging...HEAD` is exactly the branch's own file
set (the same seven files as before the rebase), so the rebase moved no
scope:

```
docs/OPERATIONS.md
docs/contracts/spec-daemon.md
docs/contracts/spec-review-evidence.md
src/mutation.rs
tests/queue_advance.rs
tests/review_dispatch.rs
```

## Rebasing and the conflict map (reproduced, not trusted)

`GIT_EDITOR=true git rebase origin/staging` stopped with EXACTLY THREE
conflicting files, one hunk each — the brief's trial map reproduced — while
`src/mutation.rs`, `src/supervision.rs`-side changes, `docs/contracts/spec-daemon.md`
and `tests/queue_advance.rs` auto-merged. No additional conflict was found.

Both commits replayed; nothing was squashed, dropped or reworded: the fix
commit keeps its original subject and body, and its rebased sha is
`850fa821c66bb3639d997a8c4da3acb8db2a7ab0`.

### 1. `docs/OPERATIONS.md` (hunk at what is now line 495)

- staging (`HEAD` side) rewrote the `refusal.evidence.verdict_stale` repair
  cell to name the #241 behaviour: "Repair the lane checkout to the certified
  head, then authorize that exact step (`run retry`): the run's own armed
  supervision consumes the authorization by re-dispatching it (issue #241),
  or dispatch it yourself (`run dispatch`)".
- the branch added the #207 fence clause to the same cell.

**Resolution — both survive** at `docs/OPERATIONS.md:495` (one row, no
duplication): staging's #241 remedy is kept verbatim, and the branch's two
#207 additions ride in it — the widened parenthetical "(the lane checkout
moved past the certified head — **before the reviewer started, or while the
review was open**)" and the sentence "**The reviewed work is fenced for the
whole review window (issue #207)**: a commit that lands mid-review is refused
at consumption exactly like one that predates the reviewer, and the delivery
must re-enter review." Neither issue's text was dropped; the row still parks
typed with retries UNSPENT (#200) and still refuses `refusal.run.retry_required`.

### 2. `docs/contracts/spec-review-evidence.md` (hunk at what is now line 119)

- the branch replaced the numbered item 4 ("the engine then consumes the
  verdict the REVIEWER writes") with its fence item.
- staging's side had inserted its OWN item 3 (#210: the reviewer leg binds its
  own lane checkout) and its own item 4 (the reviewer is started through the
  same role-bound adapter), pushing consumption to item 5.

**Resolution — re-seated, not merged textually.** staging's items 1–4 survive
verbatim (`docs/contracts/spec-review-evidence.md:87`, `:92`, `:99`, `:115`).
The fence item is re-seated as item **5** at
`docs/contracts/spec-review-evidence.md:119` with the branch's text intact
(it still cites the start-side certified-head check as "item 2", which is
still the start-side check), and consumption is renumbered to item **6** at
`docs/contracts/spec-review-evidence.md:127`. The branch's intent is preserved
exactly once — the checkout is read again after the verdict arrives and before
anything is recorded, refusing `refusal.evidence.verdict_stale` naming both
heads — and no numbering collision or duplicated step remains:
`1,2,3(#210),4,5(#207),6`.

### 3. `tests/review_dispatch.rs` (large hunk: staging's substrate vs the branch's appended witnesses)

- staging ADDED the #210 multi-workspace substrate: `FAKE_HERDR_LANES`
  (`tests/review_dispatch.rs:721`) with `row_of_id` (`:729`), `agent_row`
  (`:734`), `id_of_agent` (`:740`), `id_of_pane` (`:746`), the `LaneFixture`
  (`:889`) and the whole pane-substrate witness block.
- the branch's change to this file was APPEND-ONLY: two witnesses after the
  last pre-existing test (`+151 −0`).

**Resolution — staging's substrate byte-for-byte, the branch's witnesses
re-applied on top.** The resolved file is staging's blob with the branch's
151-line block appended; `git diff origin/staging -- tests/review_dispatch.rs`
is therefore `151 insertions, 0 deletions`:
`151	0	tests/review_dispatch.rs`

- #210's substrate is intact and never weakened: all four shell helpers, the
  `FAKE_HERDR_LANES` constant and the `LaneFixture` are staged-verbatim (the
  file region above the appended block is byte-identical to
  `origin/staging:tests/review_dispatch.rs`), and every #210 witness still
  runs: `the_reviewer_leg_binds_its_own_lane_while_the_run_lane_stays_registered`
  (`:1185`), `a_moved_reviewer_lane_refuses_before_the_reviewer_starts`
  (`:1313`), `a_retired_generations_reviewer_lane_is_reclaimed_while_a_live_one_is_never_adopted`
  (`:1349`).
- the branch's two #207 witnesses are re-applied VERBATIM (their 151-line
  block is byte-identical to the branch's own block: the appended region's
  sha256 equals the same region at `026eac9`) at
  `a_commit_landing_while_the_review_is_open_forces_re_entry_not_consumption`
  (`:1967`) — the AC3 fence witness, which commits into the run's lane while
  the reviewer's window is open — and
  `a_verdict_naming_a_pre_collection_head_of_the_same_branch_is_refused`
  (`:2048`) — the AC2 ordering witness.
- no branch-side helper conflicted functionally with #210's: the branch's
  witnesses use the headless `Fixture` (the bare-subprocess substrate), whose
  lane checkout IS the review step's evidence anchor
  (`src/mutation.rs:5409`, `:5490`), which is exactly the worktree the fence
  re-reads. Both suites' assertions hold at this head (see the gate table and
  the focused serialized run).

### Auto-merged files — verbatim preservation proved

For the three files the rebase auto-merged, every line the branch ADDED is
present at the new head, and neither file lost a line to the rebase:

| file | branch delta | added lines present at the new head | deletions |
| --- | --- | --- | --- |
| `src/mutation.rs` | +26 −0 | 26/26 | none |
| `tests/queue_advance.rs` | +362 −0 | 362/362 | none |
| `docs/contracts/spec-daemon.md` | +14 −6 | 14/14 | none extra |

(The #207 clause on the "moved certified head" contract is at
`docs/contracts/spec-daemon.md:817`; the AC1 witness at `tests/queue_advance.rs:987`.)

## Gates at the rebased head (raw exits; named logs)

Every log below lives in the lane worktree's untracked `.lane-logs/` (lane
artifacts, never committed); the aggregate log is at the worktree root, as the
brief names it. No gate is piped through `grep` to become a PASS/FAIL.

| # | recipe / command | raw exit | duration | log |
| --- | --- | --- | --- | --- |
| 1 | `RUST_TEST_THREADS=1 cargo test --locked --test review_dispatch` (focused: the resolved file, both #207 witnesses) | **0** (23 passed / 0 failed) | 32.46 s | `.lane-logs/focused-review_dispatch.log` |
| 2 | `RUST_TEST_THREADS=1 just ci` (HEADLINE aggregate, serialized) | **0** | 22 m 39.454 s (`real`; 50 `test result: ok` lines, 0 FAILED) | `.gate-ci-serialized.log` |
| 3 | `just ci` (parallel; disclosure run) | **101** | 4 m 25.095 s (`real`) | `.gate-ci-parallel.log` |
| 4 | `just security` at THIS report commit (the report files are tracked, so the scanner + gitleaks phase must see them; run once this commit exists) | raw exit recorded in `.lane-logs/security-at-report-commit.log` and reported in the lane's closing message | — | `.lane-logs/security-at-report-commit.log` |
| 5 | `just --list` (canonical recipe list, read once before any gate) | 0 | — | `.lane-logs/just-list.log` |

`just ci` = `fmt-check → check → lint → test → doc → build-release → security`
(the repository's own aggregate; no CI chain was reconstructed).

### The parallel `just ci` red — disclosed and adjudicated, never suppressed

Same recipe, same bytes, default test threads: **raw exit 101**,
`real 4 m 25.095 s`, log `.gate-ci-parallel.log`. The recipe failed in the
`test` phase alone (`error: recipe \`test\` failed on line 26 with exit code
101`), so that invocation never reached `doc`, `build-release` or `security` —
it is a PARTIAL aggregate and is not a verdict on those phases; 36 suite
results were `ok` before it stopped.

One suite failed: `tests/review_dispatch.rs` — 20 passed / 3 failed — and all
three failures are the DOCUMENTED pre-existing wall-clock race class the brief
names (seconds-scale fixture budgets vs worker-turn-class waits), never a #207
witness:

| failing test | seat | raw assertion |
| --- | --- | --- |
| `a_reviewer_that_writes_no_verdict_parks_the_frontier_with_a_typed_timeout` | `tests/review_dispatch.rs:541` (staging's own test) | `left: Some("adapter.timeout")` vs `right: Some("effect.review_timeout")` |
| `a_re_attempted_review_step_consumes_the_late_verdict_without_re_prompting_the_leg` | `tests/review_dispatch.rs:1497` (staging's own test, #214) | same pair |
| `a_review_row_the_agent_never_took_is_never_a_proven_delivery` | `tests/review_dispatch.rs:1704` (staging's own test) | `status "ambiguous"` + `adapter.timeout` where `"refused"` is expected |

Attribution evidence (each with its own raw exit and named log):

1. **The failing tests are staging's own, and this branch cannot reach them.**
   The branch's delta on that file is append-only (`151 0`); the first 1964
   lines of the rebased file are staging's blob BYTE FOR BYTE (sha256
   `1036b34748878bcd5e429153d991493d221d14f56e67e16f84d9c472aa1e9d5b` on both
   sides), and all three failing tests exist in
   `origin/staging:tests/review_dispatch.rs` exactly as at this head
   (`.lane-logs/control-base-restore-proof.log`).
2. **Serialized, the very same bytes are green** — row 2 above ran that
   suite's 23 tests inside a complete `exit 0` aggregate, and row 1 ran the
   suite alone serially: 23 passed.
3. **Same mode, this head, quiet host:** `cargo test --locked --test
   review_dispatch` (default threads) → **raw exit 0**, 23 passed, 6.72 s
   (`.lane-logs/head-parallel-rerun.log`).
4. **Same-mode base-content control** (the brief's requested attribution
   proof): the six touched source/doc files were swapped to `origin/staging`'s
   blobs (`git checkout origin/staging -- src/mutation.rs
   tests/review_dispatch.rs tests/queue_advance.rs docs/OPERATIONS.md
   docs/contracts/spec-daemon.md docs/contracts/spec-review-evidence.md`), the
   test binary was proved RECOMPILED (`Compiling canter v0.1.0` in the log),
   and the same parallel command ran on base content → **raw exit 0**, 21
   passed (base content carries two fewer tests: this branch's appended
   witnesses), `.lane-logs/control-base-parallel-review_dispatch.log`. Every
   file was then restored from `HEAD` and the restore proved byte-identical
   (file sha256 == `git show HEAD:<file> | shasum -a 256` for all six, and
   `git status` clean apart from the untracked logs):
   `.lane-logs/control-base-restore-proof.log`.

Conclusion: the parallel red is the load-window property of that suite that
the brief describes (filed as a follow-up from #228's review), not a
regression of this diff — it is absent at the base bytes and at this head's
bytes in the same window, and both are green serialized. **Nothing in the
fixtures or the deadlines was touched**: this branch's delta on the file is
the append-only block above.

## AC4 — the mutation RED/GREEN table (witnesses that bite)

Each mutation was applied to `src/mutation.rs` at the rebased head, the
witness was run SERIALIZED (`-- --test-threads=1`; one focused execution per
hypothesis, raw exit captured, log named), the pristine blob was then restored
and the witness re-run.

| # | mutation applied | mutated file sha256 | witness (test) | RED | restored file sha256 | GREEN |
| --- | --- | --- | --- | --- | --- | --- |
| M1 | the **head-binding check** removed from `review_verdict_facts` (`src/mutation.rs:5838`): the `Some(value) if value == inputs.feature_head` guard and its typed `refusal.evidence.verdict_stale` arm deleted, so any named head passes | `bd785e4bbf7191ef57d4386d53c9118b75b94a6482a08e6879a9c9375af82e7e` | `a_verdict_naming_a_pre_collection_head_of_the_same_branch_is_refused` (`:2048`) | **exit 101** — the foreign-head verdict was silently CONSUMED: `left: "succeeded"`, `right: "refused"` (`.lane-logs/m1-red.log`) | `880344af60f81050be1bedd2fd756a97818946ce9c931b64096fe087aa976a5a` | **exit 0** — 1 passed (`.lane-logs/m1-green.log`) |
| M2 | the **fence** removed (`src/mutation.rs:5668`–`:5693`): the post-verdict `rev-parse` + the whole `fenced for the whole review window` refusal deleted | `d453acd42b792ab12637c1e044f8961c3d86ca18c98b934cb3cf4f5b9a1de5b4` | `a_commit_landing_while_the_review_is_open_forces_re_entry_not_consumption` (`:1967`) | **exit 101** — the mid-review commit was silently CONSUMED: `left: "succeeded"`, `right: "refused"` (`.lane-logs/m2-red.log`) | `880344af60f81050be1bedd2fd756a97818946ce9c931b64096fe087aa976a5a` | **exit 0** — 1 passed (`.lane-logs/m2-green.log`) |

The restored sha256 is the blob's own sha256 at the rebased head
(`git show HEAD:src/mutation.rs | shasum -a 256` = `880344af…a976a5a`), and
`git status --porcelain src/mutation.rs` is empty after each restore
(`.lane-logs/m1-restore-proof.log`, `.lane-logs/m2-restore-proof.log`).

## AC1 / AC2 / AC3 witnesses at THIS head

| AC | witness | seat at the rebased head | status |
| --- | --- | --- | --- |
| AC1 (the run's OWN reviewer fires at the certified head) | `the_run_dispatches_its_own_reviewer_at_the_certified_head` | `tests/queue_advance.rs:987` (auto-merged verbatim) | runs and passes in the aggregate and in the focused suite |
| AC2 (a verdict that does not name the run's bound head cannot satisfy p6) | `a_verdict_naming_a_pre_collection_head_of_the_same_branch_is_refused` | `tests/review_dispatch.rs:2048` | passes; RED under M1 |
| AC3 (a commit inside the review window is refused / forces re-entry) | `a_commit_landing_while_the_review_is_open_forces_re_entry_not_consumption` | `tests/review_dispatch.rs:1967` | passes; RED under M2 |

## AC5 — non-weakening at the rebased head

How this was checked:

1. **The delta is additions-only in code and tests.** `git diff --numstat
   origin/staging HEAD -- src tests` shows zero deletions:
   `src/mutation.rs +26 −0`, `tests/queue_advance.rs +362 −0`,
   `tests/review_dispatch.rs +151 −0`. The only deletions anywhere in the
   rebased delta are the two re-seated doc lines (`docs/OPERATIONS.md +1 −1`,
   `docs/contracts/spec-review-evidence.md +9 −1`) and
   `docs/contracts/spec-daemon.md +14 −6`, each accounted for above.
2. **No deadline, freshness bound or retry budget is widened.** Every added
   line matching a deadline/retry/budget shape was enumerated
   (`.lane-logs/ac5-nonweakening.log`): the only `deadline_secs` in the added
   code is a fixture-local `("deadline_secs", integer(60))` inside the added
   AC1 witness's own params document — a test's declared policy, not a bound
   the engine applies to any other step. No constant
   (`REVIEW_DEADLINE_DEFAULT_SECS`, `EFFECT_DEADLINE_CEILING_SECS`, retry
   budgets) is touched: the whole delta contains no `SECS`/retry constant
   edit.
3. **The preserved behaviours run at this head, unchanged.** Their suites are
   staged-verbatim and pass in the aggregate: `host_proof_renewal` (#199),
   `frontier_carry_forward` (#192), `supervision`, `supervision_collection`,
   `supervision_collection_b`, `supervision_dispatch_refusal` (#200/#202/#205),
   `queue_advance` (#195/#196) — 45 suite results, all `ok` in the serialized
   aggregate. Nothing in the branch weakens #200's "parks typed with retries
   UNSPENT" or #202/#205's freeze/bind: the fence ADDS a consumption-side
   refusal and removes none.
4. **#210 and #241 are intact**, cited by line above
   (`tests/review_dispatch.rs:721`…`889`; `docs/OPERATIONS.md:495`).

## The branch's stale report — superseded

`.report-ordering.md` (the rebased `0afc0d5` report) claimed "Candidate
commit: `026eac97947dcfdc9491b2ddbbdff499b975677c`", a head this branch no
longer has. It now carries a SUPERSEDED banner at its head that quotes that
claim, labels it RETRACTED and points here; its body is kept verbatim as the
pre-rebase record. Its pre-rebase gate narrative (six `just ci` attempts under
high host load, the `host_proof_renewal` load-window classification, the
base-vs-candidate A/B) is history at the OLD base and is NOT evidence for this
head: every gate claim that applies to the rebased head is in the table above.

## NOT EXERCISED

- **Hosted CI** — not run and not claimed: no PR was opened, edited or merged
  (the orchestrator owns the PR surface); this lane pushed the branch only.
- **Live workflow execution / release execution / real harness sessions** —
  never run. No run was dispatched, presented, paused, retried, released,
  resolved or superseded by this lane; every witness uses the repository's
  fake executables and synthetic fixtures. The live run named by #207 is not
  touched (nothing in this diff reads or writes it).
- **The pre-existing `review_dispatch` wall-clock race class** — the
  `deadline_secs: 1` fixtures that can record `adapter.timeout` where the
  assertion expects `effect.review_timeout` under parallel load. it did NOT manifest in the serialized aggregate (50 suite results `ok`, 0 FAILED) and is disclosed + adjudicated in the section above; no fixture or deadline was touched.
- **`just ci`'s release build and security phases are only as strong as this
  host's toolchain** — `gitleaks` v8.30.1, `cargo-deny`, `cargo-audit` were
  present and were invoked (a missing tool would have been a failure, not a
  skip).
- **`.lane-logs/`** — untracked lane logs (raw gate output) are left in the
  worktree and are NOT committed; `git status --porcelain` therefore lists
  that directory.
