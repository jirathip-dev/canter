# Issue #132, round 14 — the reviewed published-ref cleanup proof, replayed onto integration base `fc5c88c`

Lane: canter's own implementer dispatch (run `run-8b381dc5046b7f2b`, step
`p4-132`, submission `qs_47cc6bc3b529b4eb`), worktree `issues-132`, branch
`issue-132`, based on the run's recorded integration base
`fc5c88c3b60fb96788938f21a943829718500f8a` (`origin/staging` at dispatch
time; carrying #217 via PR #218).

Commit graph: base `fc5c88c` -> content head `92e50f4` ("Cleanup certifies a
squash landing visible only on the published ref; replayed onto integration
base fc5c88c (Refs #132)") -> the four report-only commits the round-13 head
carried (`.report-132-r10.md` `a85d258`, `.report-132-r11.md` `bd069d5`,
`.report-132-r12.md` `cb8634a`, `.report-132-r13.md` `602afd0`,
byte-identical to their copies on the previous head) -> this report's own
commit. This report's own commit follows the carried head, so it can name it.

## What this round delivers, and why it is the bounded change

Issue #132's own text names the shape that must hold: *"the run's `merge` step
must never leave the integration checkout diverged from the published
integration ref, and the run must be able to complete on a repo whose policy
is squash."*

The merge-side half is in the base (#156/#168/#178/#182; #196 lands and
publishes under the plan's `merge_policy`, with the published ref always the
certified target, never a stale local view). The still-unlanded half is the
`cleanup` side: when the sanctioned landing happens on the forge (the PR is
squash-merged), the checkout's own integration ref never sees it, so `cleanup`
must prove the landing against the FETCHED, verified PUBLISHED ref — and only
a checkout strictly behind it may certify from that view. That content is
independently PASSed in the review lineage (`REVIEW-183-024783d4` PASS at
`024783d42f6f416c22882dd3af25146c641bc03d`; lineage `REVIEW-183-f4ea6876`
naming `f4ea6876cb0ac8d39729f1a4bbfabe4eb2250a2b` exactly, `REVIEW-183-1ba80d60`,
`REVIEW-183-4dc2ae22`, `REVIEW-183-c05cc99`, `d756a22`, `532458c`) and is
replayed here onto the rebuilt run base — the lineage's own refresh shape,
never a re-design:

- `LandedProof::Published { published_head, merge_base }` — the third landed
  proof (ancestry / local content / published content), made against the
  fetched published head.
- `landed_in_published` — reads the published head
  (`published_integration_head`), refuses when the checkout's own ref IS the
  published view (no self-certification), FETCHES and verifies it
  (`fetched_published_head`), refuses whenever the local ref is not a strict
  ancestor of it (an unpublished or diverged local move is never certified),
  then proves the branch head by ancestry against the fetched head, or — for
  the squash landing, which never is an ancestor — by exact content on every
  path the branch changed relative to the fork point.
- `effect_cleanup` — when the local proof refuses `refusal.cleanup.unmerged`,
  tries the published route; if that view adds no proof, the ORIGINAL local
  refusal is returned unchanged. The salvage record of a published proof names
  `published_head` (and `merge_base` when the proof was by content) while
  keeping the checkout's own `integration_head`; deletion uses `-D`.
- `landed_in_integration` uses the base's shared fail-closed comparison
  helpers (`changed_paths_between`, `differing_paths_between`, landed with
  #196) instead of the round-5 private helper, so the local and the published
  proofs cannot drift.

Staging has moved one commit since the round-13 replay base (`5e02cf1` ->
`fc5c88c`: #217 via PR #218). No production behavior beyond the replay is
added in this round. The round-10..round-13 report files are carried from the
previous head byte-identically; prior reports are byte-unchanged.

## Replay integrity — the reviewed content, byte-for-byte, on the new base

All four comparison proofs are hunk-body comparisons (`index` lines and `@@`
headers excluded because offsets move), each a raw `diff` whose exit was 0
with an empty diff file (named by basename; logs in the lane artifact
directory for this round):

| # | comparison (log basename) | result |
| --- | --- | --- |
| 1 | this delta vs the round-13 delta (`01-delta-equiv.diff`, inputs `01a`/`01b`) | IDENTICAL — 906 lines each |
| 2 | the carried report commits vs the round-13 head's (`02-reports-equiv.diff`, inputs `02a`/`02b`) | IDENTICAL — 627 lines each |
| 3 | tree delta vs base delta: `diff(92e50f4..602afd0)` vs `diff(5e02cf1..fc5c88c)` (`03-tree-equiv.diff`, inputs `03a`/`03b`) | IDENTICAL — 368 lines each |
| 4 | ride-along, the two delivered files the base touched (`04-ridealong.diff`, inputs `04a`/`04b`) | IDENTICAL — 200 lines each |

sha256 (16 hex shown) of the eight content-head files against the round-13
content head `91e38b0`:

| path | this content head | round-13 content head | state |
| --- | --- | --- | --- |
| `tests/mutation_engine.rs` | `45c4c58c829ba31d` | `45c4c58c829ba31d` | byte-identical |
| `CHANGELOG.md` | `6bcec695b232c357` | `6bcec695b232c357` | byte-identical |
| `docs/contracts/spec-lifecycle.md` | `0a07ddd46925dba7` | `0a07ddd46925dba7` | byte-identical |
| `.report-132-r7.md` | `c16d34ff521457fa` | `c16d34ff521457fa` | byte-identical |
| `.report-132-r8.md` | `e1dd06f97531c859` | `e1dd06f97531c859` | byte-identical |
| `.report-132-r9.md` | `93312eaeb0b6e99b` | `93312eaeb0b6e99b` | byte-identical |
| `src/mutation.rs` | `6a1e75f591af7f36` | `6c977438f441f9fe` | differs by the base's own #217 text (proof 4) |
| `docs/contracts/spec-plans.md` | `550a2b199c0ddaa2` | `4de7b57ae01c7842` | differs by the base's own #217 text (proof 4) |

The two differing files differ exactly where the base's own #217 work rides
along; proof 4 shows `diff(round-13 content -> this content)` is byte-identical
to `diff(5e02cf1 -> fc5c88c)` for those paths, hunk for hunk — a clean
three-way cherry-pick (exit 0, no conflict, no base text dropped). The four
carried report files are byte-unchanged: `.report-132-r10.md`
`07f413d69aae55af`, `.report-132-r11.md` `3e67191baf22aa80`,
`.report-132-r12.md` `b1ca2df618176d97`, `.report-132-r13.md`
`ae436fd77888a023`.

The content commit stat is 8 files, 706(+)/69(-) — the same count as the
round-13 content head and the originally reviewed commit; the full delivery
delta against the new base is 12 files, 1309(+)/69(-).

## Verification (raw exits, named logs)

All runs in the lane worktree at the head above, `CARGO_TARGET_DIR` outside
the checkout; logs in the lane artifact directory (named by basename below).
Method: raw redirect to a named log, raw exit recorded, no gate piped through
a filter.

| # | Command (log basename) | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `just fmt-check` (`01-fmt-check.log`) | 0 | clean |
| 2 | `cargo check --locked --all-targets` (`02-check.log`) | 0 | clean (5.84 s, warm) |
| 3 | the delta's witness solo (`03-witness-solo.log`) | 0 | 1 passed; 0 failed (9.70 s) |
| 4 | mutation probe m1 — the strict-behind guard forced to `if false` (`05-mut-1.diff`, `06-witness-red.log`) | 101 | RED at the guard's own property, see below |
| 5 | post-probe restore + witness GREEN on the restored bytes (`07-witness-green-restored.log`) | 0 | `src/mutation.rs` sha256 == `git show HEAD:` blob; `git status` empty; 1 passed (9.33 s) |
| 6 | `just lint` (clippy `-D warnings`, all targets) (`08-lint.log`) | 0 | clean |
| 7 | `just security` (`09-security.log`) | 0 | scanner self-test OK; public-tree scan clean; cargo-deny advisories/bans/licenses/sources ok; cargo-audit clean (119 crate dependencies); gitleaks directory scan … no leaks found |
| 8 | `cargo test --locked --test mutation_engine` (`10-mutation-engine-full.log`) | 0 | 33 passed; 0 failed (21.89 s) |
| 9 | `cargo test --locked --test review_dispatch` (`11-review-dispatch.log`) | 101 | 14 passed; 3 failed — the base's own #214/#215 `deadline_secs: 1` races (`adapter.timeout`); see adjudication |
| 10 | that selection serialized (`12-review-dispatch-serialized.log`) | 0 | 17 passed; 0 failed (22.24 s) |
| 11 | that selection parallel again at the same bytes (`15-review-dispatch-parallel-rerun.log`) | 101 | 15 passed; 2 failed — same class (`tests/review_dispatch.rs:1422`, `:1573`) |
| 12 | base-content probe: `git checkout fc5c88c -- src/mutation.rs tests/mutation_engine.rs`, same selection (`13-review-dispatch-base-probe.log`, `16-review-dispatch-base-probe-2.log`) | 0 / 0 | 17 passed; 0 failed (5.29 s / 5.54 s) |
| 13 | post-probe restore (`14-probe-restore.log`, `17-restore-2.log`) | 0 | `src/mutation.rs` / `tests/mutation_engine.rs` sha256 equal the HEAD blobs; `git status --porcelain` shows only this report |

## Mutation proof (the guard is the discriminator)

The strict-behind guard in `landed_in_published`
(`if !is_commit_ancestor(ctx, &local_head, &fetched)`, `src/mutation.rs:5773`)
was rewritten to `if false` (exactly one occurrence). The delta's own witness
then fails at the guard's own property: the DIVERGED checkout is CERTIFIED
instead of refused — `"removed":true`, `"landed_by":"content"`,
`"published_head":"9d65eeb1…"` named, assertion "expected refused response" at
`tests/mutation_engine.rs:372`. Source restored byte-exactly (sha256
re-verified equal to the committed blob; `git diff` empty; `git status`
empty); the witness is GREEN on the restored bytes.

## Bounded aggregate this round (and why)

This round's lane ran under the run's own collection window: the run's
`p5-132` `collect_outcome` attempt was already claimed when the lane started
(`ik_run-8b381dc5046b7f2b-p5-132-1789833240`), and that attempt's ~1800 s
window closes at 23:24 local — a `worker_timeout` expiry would park the step
for an operator retry and forfeit the zero-operator acceptance. The lane
therefore ran the fastest complete gate set that fits the window (rows 1–13)
and left the full `just ci` aggregate to hosted CI at the delivered head,
which is the authoritative aggregate (round-13 precedent): runs
`35453613021` / `35453613108` were in progress for the previous push
`602afd0` when this report was written; the head `9e8c663` was green on
every check; the delivered head's own runs start with this round's push.

Disclosed limits: `just ci` was NOT invoked in full this round — the p5
collect window above bounded the lane to the gate set in rows 1-13, so there
is no raw `just ci` exit at this head; hosted CI at the pushed head is the
authoritative aggregate. `review_dispatch` is not claimed green locally; the
adjudication is in rows 9-12: the delta's diff contains NO line touching any
review/deadline/prompt/verdict path in `src/mutation.rs` (grep of the delta
diff is empty; the delta's only added production fn is
`landed_in_published`) and it does not touch `tests/review_dispatch.rs`,
which is the base's own file. The failures are the base's own #214/#215
witnesses: an adapter operation is reaped at its 1 s `deadline_secs` before
the reviewer leg registers, returning `adapter.timeout` ("the operation
exceeded its deadline and was cancelled") where the test expects a typed
`effect.review_timeout` / `refusal.prompt.undelivered`. Observed at this
head: 3 failed (row 9, parallel, load 30-50), 0 failed (row 10, serialized),
2 failed (row 11, parallel, load 8.38); with the delta's source reverted to
the base: 0 failed twice (row 12, parallel). I could NOT reproduce the
failure with base bytes in this window; the not-the-delta basis here is the
serialized-green run at the delta bytes, the empty review-path diff above,
and hosted CI green at code-identical content — not a same-window base-side
reproduction (round 13 recorded one; this round's window was too short to
keep probing).

## Not delivered / not claimed

- No forge-merge route is implemented here: the `p7` landing form on a
  PR-protected integration ref (forge landing vs local landing) is the
  owner's ruling recorded in this issue's comments. This lane neither
  implements it nor dispatches, retries, pauses, releases or merges any run.
- No daemon, state or run was mutated by this lane. The only side effects are
  the branch's commits, the branch push, and this PR update.
- Hosted CI for the pushed head is reported by the PR, not by this report.
- No new production behavior beyond the replay; the witnesses are the ones
  the reviewed head already carried.
- `review_dispatch` is NOT claimed green locally on this host; rows 9-12 are the recorded reason it is adjudicated as base-side, with the limits stated above.

## Residual tension (recorded, not resolved here)

Cleanup's published route certifies a landing the forge performed; it never
performs or claims a forge merge itself (`spec-lifecycle.md`, `spec-plans.md`
state this explicitly). A run whose `merge` step must land by push on a
PR-protected integration ref still cannot complete end-to-end without either
(a) the forge landing performed externally and then certified here, or (b)
the owner ruling that changes the landing form. Carried forward from round 13
(its own report file is byte-unchanged); recorded so round 14 is not read as
closing that question.
