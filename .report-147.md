# Refs #147 / Refs #164 — bounded pane collection and published lane bases

## R1 correction: the original delivery failed hosted CI

The earlier framing, "Final aggregate: `just ci` — raw exit 0, 794.98 s",
is RETRACTED as evidence of a mergeable delivery. That local four-thread run
really passed, but it did not exercise the hosted serial per-suite budget.
At delivered `a72a79be541e9c1af0c8d8685488129c1a44bdd2`, CI run
`35117154853` failed: supervision was killed at 150.1 s, raw exit 124.
The orchestrator measured 245.95 s serially; this lane's R1 baseline measured
199.56 s including Cargo startup (25 passed, exit 0, still over budget).

The budget failure came from cumulative wall-clock policy waits, not from
the printed `COLLECT_REFUSAL` line. That line belongs to a passing negative
witness for missing-base/retry preservation. R1 also adds an independent
invalid-base-before-worker-poll witness and preserves that refusal boundary.

The repaired serial suite passed all 26 tests: 109.60 seconds of test time,
113.4 seconds including compile/driver time under the unchanged 150-second
budget (exit 0, no survivors). Candidate `f0ddad43774832e74b354b31b37e709a06d27764`
also passed the complete serial `just ci` invocation: raw exit 0 in
852.83 seconds, 736 Cargo tests passed, 0 failed, 0 ignored. Supervision took
94.86 seconds within that aggregate. These are local results, not a substitute
for exact-head hosted CI.

See [.report-147-r1.md](.report-147-r1.md) for the replacement candidate,
raw aggregate result, discrimination evidence and the post-publication hosted
receipt. The original results below are historical local evidence, not a
claim that the old delivered head passed hosted CI.

## Historical round-zero delivery

Branch: `impl-147-wait`
Base: `018d2eb38bf4590509a6dc738e0ae89ff9e6e967`
Candidate: `5067426b5f1ccff42ffc32d293a44452bdd1f0fd`
The delivery commit adds this report only. The pushed delivery SHA is reported by the lane's final message and verified with `git ls-remote`.

## Scope and implementation

Read both issues and the latest bounded fix-contract comment on #147. The original issue's live-session and poisoning investigations are outside this brief's live-system fence.

Changed files:
- `src/mutation.rs`: wait for the run's pane worker before examining its delta; bounded collection deadline, default 1800 s / existing ceiling 3600 s. Checkout and lane creation resolve the observed base, otherwise the published origin ref, fetching missing objects without changing that base. Existing directories refuse `refusal.worktree.exists`, rather than resetting worker-owned content.
- `src/adapters.rs`: reuse Herdr agent lookup and lane/generation/worktree verification for collection observations.
- `src/daemon.rs`: resolve the run's bound session for collection; run the single collection claim off the supervisor thread, with a reservation before spawn to avoid duplicate in-flight dispatch.
- `src/supervision.rs`: in-flight collection is eligible `waiting-workers`; exhausted collection is ineligible `worker-timeout`, with `effect.worker_timeout` recorded as ambiguous. The existing attempted-step fence remains intact.
- `src/tui/supervision.rs`: include worker-timeout in the closed display vocabulary and alert tone.
- `tests/supervision.rs`: three daemon/RPC regressions driven by fake Herdr state. A fixture latch holds the worker across repeated reads and a supervisor tick, then commits from the bound worktree before reporting done. The ledger proves one attempt per step, zero operator keys, and no retries. A second witness stops empty; a third never stops and remains parked after later ticks.
- `tests/mutation_engine.rs`: three base regressions: stale local branch, moved origin after the recorded observation, and a published object missing locally; typed existing-lane refusal and unchanged lane content are checked. The prior replay witness now expects the new typed refusal, retaining its no-duplicate checks.
- `tests/executor_authoring.rs`, `tests/supervision_dispatch_refusal.rs`: publish the fixture's integration branch through a local origin, matching the newly required read-back prerequisite; no assertions removed.
- `docs/contracts/spec-daemon.md`, `docs/contracts/spec-plans.md`: document waiting, timeout, base, and deadline contracts.

No dependency, capability, admission, digest/idempotency, prompt-delivery, or published merge-certification gate changes. `Cargo.toml` and `Cargo.lock` are unchanged.

## Commands and raw results

All commands run from this branch's worktree. Build environment: `CARGO_BUILD_JOBS=4`; dedicated `CARGO_TARGET_DIR` outside the checkout. Mutation probes use `RUST_TEST_THREADS=1`. The final aggregate uses `RUST_TEST_THREADS=4`; earlier aggregates used 1. The two whole supervision suites were also exercised with `--test-threads=4`. Full gate runner deadline: 1800 s; each mutation subprocess deadline: 180 s. Logs below are raw local evidence artifacts, not committed host-bearing output.

`just --list` was invoked during initial discovery (once); its output identified the canonical recipes used below.

Verified focused runs:
- `cargo test --locked --test supervision collection_ -- --test-threads=1`: exit 0; 3 passed, 0 failed, 22 filtered. `.gate-restore-collect.log` (after all probes).
- `cargo test --locked --test mutation_engine worktree_base_ -- --test-threads=1`: exit 0; 3 passed, 0 failed, 19 filtered. `.gate-restore-base.log` (after all probes).
- `cargo test --locked --test executor_authoring`: exit 0; 15 passed, 0 failed. `.gate-executor-green.log`.
- `cargo test --locked --test mutation_engine plan_rpc_digest_binding_and_idempotent_replay`: exit 0; 1 passed, 0 failed. `.gate-duplicate-green.log`.
- `cargo test --locked --lib tui::supervision::tests::every_recorded_class_maps_onto_the_closed_vocabulary`: exit 0; 1 passed, 0 failed. `.gate-tui.log`.
- `cargo test --locked --test supervision --test supervision_dispatch_refusal -- --test-threads=4`: raw exit 101 overall; supervision itself 25 passed / 0 failed; dispatch-refusal fixture 1 passed / 3 failed before its expectation correction. `.gate-supervision-green.log`.
- `cargo test --locked --test supervision_dispatch_refusal -- --test-threads=4`: exit 0; 4 passed / 0 failed after correction. `.gate-refusal-green.log`.
- `just fmt-check`: exit 0; `.gate-fmt-retry-final.log`.
- `just lint`: exit 0. `.gate-lint-green.log`.
- `just fmt`: exit 0. `.gate-fmt-fetch.log`.
- `git diff --check`: exit 0.

Final aggregate: `just ci` — raw exit 0, 794.98 s, at candidate `5067426b5f1ccff42ffc32d293a44452bdd1f0fd`. Logs: `.gate-ci-verified.log` and `.gate-ci-verified.status`.

Raw receipt:

```text
command=just ci
exit=0
duration_secs=794.98
```

The aggregate executed `cargo fmt --check`, `cargo check --locked --all-targets`, `cargo clippy --locked --all-targets -- -D warnings`, `cargo test --locked`, `cargo doc --locked --no-deps`, `cargo build --release --locked`, then the complete security recipe. Summing the 42 Cargo result blocks in this log gives 734 passed, 0 failed, 0 ignored, 0 filtered. The scanner separately reports `Ran 21 tests` / `OK`. Security output includes `advisories ok, bans ok, licenses ok, sources ok` and `no leaks found`; cargo-audit completed successfully.

The delivery commit changes this report only; post-report `just fmt-check` and `just security` receipts and pushed-head verification are supplied in the final delivery message.


Earlier failures, not green claims:
- Initial `just check`: exit 101, adapter error visibility; fixed. `.gate-check-first.log`.
- Focused collection development runs: four exit-101 runs (private test accessor, rejected interval policy, unresolved checkout parameters, and a fixture assertion racing the released worker commit). `.gate-collection-{first,second,third,fourth}.log`. The final fixture uses supported evidence, valid policy, resolved checkout inputs, and checks the base before releasing its latch.
- Initial base witness: exit 101 because its helper expected RPC success for a typed refusal; changed to the error helper. `.gate-base-first.log`.
- `just fmt-check && just check && just lint && just test && just doc && just build-release && just security`: first invocation exit 101 at clippy (needless borrow; terminal output); fixed. Named-log invocations then exited 101 at the closed TUI vocabulary (`.gate-chain.log`, 300 passed / 1 failed), and at executor fixtures with no origin (`.gate-chain-green.log`, executor suite 4 passed / 11 failed). Both were fixed without relaxing production checks.
- First `just ci`: exit 101 at the old duplicate-worktree error expectation (mutation_engine 21 passed / 1 failed). `.gate-ci.log`; corrected in the candidate's final test-only commit.

- The next `just ci` returned exit 1 at fmt-check for the revised duplicate-error assertion (`.gate-ci-final.log`). `just fmt` returned 0 (`.gate-fmt-delivery.log`); the test-only candidate commit was formatted. Production source bytes and probe mutations are unchanged.

- `just ci` at the formatted candidate then exited 101 in supervision (21 passed / 4 failed): old retry/containment fixtures expected failed/adapter.exit for the now-typed existing-lane refusal (`.gate-ci-delivery.log`). Exact expectations were updated, keeping all authorization, containment and no-retry assertions. The combined whole-supervision command then had 25 passing supervision tests but exited 101 at the shared dispatch-refusal fixture (1 passed / 3 failed; `.gate-supervision-green.log`). Updating that fixture's exact expected status yielded the focused four-test green run. No production bytes changed after the mutation battery.

## Discrimination: base RED plus 14 one-site mutations

Each run below compiled and failed in the named test assertion, not in setup/compilation. All 15 runs returned raw exit 101. Every probe restored the exact candidate source bytes in a finally block, refreshed mtimes, and recorded identical SHA-256 maps. `git diff --exit-code HEAD -- src tests docs` returned 0 after restoration; both focused suites above then returned 0.

### base

All five changed production files restored from the pinned base; new tests retained. All three base tests fail on unequal commit IDs.

Command: `cargo test --locked --test mutation_engine worktree_base_ -- --test-threads=1`

Raw exit: 101; duration: 27.69 s. Log: `.probe-base.log`.

Failing tests:
- `worktree_base_fetches_the_published_commit_missing_from_the_checkout`
- `worktree_base_is_observed_or_published_never_a_stale_local_branch`
- `worktree_base_keeps_the_recorded_observation_when_origin_moves`

### local-checkout

Use local integration branch in checkout again: recorded checkout base differs from published base.

Command: `cargo test --locked --test mutation_engine worktree_base_is_observed_or_published_never_a_stale_local_branch -- --test-threads=1`

Raw exit: 101; duration: 17.14 s. Log: `.probe-local-checkout.log`.

Failing tests:
- `worktree_base_is_observed_or_published_never_a_stale_local_branch`

### local-worktree

Use local integration branch in worktree add again: created head differs from published base.

Command: `cargo test --locked --test mutation_engine worktree_base_is_observed_or_published_never_a_stale_local_branch -- --test-threads=1`

Raw exit: 101; duration: 6.55 s. Log: `.probe-local-worktree.log`.

Failing tests:
- `worktree_base_is_observed_or_published_never_a_stale_local_branch`

### ignore-observed

Replace the recorded base with the latest publication: head no longer equals the frozen observation.

Command: `cargo test --locked --test mutation_engine worktree_base_keeps_the_recorded_observation_when_origin_moves -- --test-threads=1`

Raw exit: 101; duration: 5.64 s. Log: `.probe-ignore-observed.log`.

Failing tests:
- `worktree_base_keeps_the_recorded_observation_when_origin_moves`

### no-fetch

Replace missing-object fetch with rev-parse: collection of the published commit fails with Not a valid object name.

Command: `cargo test --locked --test mutation_engine worktree_base_fetches_the_published_commit_missing_from_the_checkout -- --test-threads=1`

Raw exit: 101; duration: 5.09 s. Log: `.probe-no-fetch.log`.

Failing tests:
- `worktree_base_fetches_the_published_commit_missing_from_the_checkout`

### untyped-existing

Restore adapter.exit for existing worktrees: expected refusal.worktree.exists, observed adapter.exit.

Command: `cargo test --locked --test mutation_engine worktree_base_is_observed_or_published_never_a_stale_local_branch -- --test-threads=1`

Raw exit: 101; duration: 4.95 s. Log: `.probe-untyped-existing.log`.

Failing tests:
- `worktree_base_is_observed_or_published_never_a_stale_local_branch`

### deadline-default

Remove collect_outcome from the 1800-second table row: expected 1800, observed 60.

Command: `cargo test --locked --lib mutation::tests::effect_deadlines_are_documented_per_kind_and_bounded`

Raw exit: 101; duration: 10.19 s. Log: `.probe-deadline-default.log`.

Failing tests:
- `mutation::tests::effect_deadlines_are_documented_per_kind_and_bounded`

### tui-timeout

Remove WorkerTimeout from RunClass::from_code: worker-timeout has no closed arm.

Command: `cargo test --locked --lib tui::supervision::tests::every_recorded_class_maps_onto_the_closed_vocabulary`

Raw exit: 101; duration: 7.55 s. Log: `.probe-tui-timeout.log`.

Failing tests:
- `tui::supervision::tests::every_recorded_class_maps_onto_the_closed_vocabulary`

### no-wait

Bypass await_pane_worker: the worker must WAIT before collection assertion fails.

Command: `cargo test --locked --test supervision collection_waits_for_pane_delivery_without_operator_dispatch -- --test-threads=1`

Raw exit: 101; duration: 42.4 s. Log: `.probe-no-wait.log`.

Failing tests:
- `collection_waits_for_pane_delivery_without_operator_dispatch`

### no-stop

Never recognize a stopped worker: expected succeeded, observed ambiguous/effect.worker_timeout.

Command: `cargo test --locked --test supervision collection_waits_for_pane_delivery_without_operator_dispatch -- --test-threads=1`

Raw exit: 101; duration: 69.85 s. Log: `.probe-no-stop.log`.

Failing tests:
- `collection_waits_for_pane_delivery_without_operator_dispatch`

### waiting-ineligible

Set waiting-workers eligibility false: expected true, observed false.

Command: `cargo test --locked --test supervision collection_waits_for_pane_delivery_without_operator_dispatch -- --test-threads=1`

Raw exit: 101; duration: 21.17 s. Log: `.probe-waiting-ineligible.log`.

Failing tests:
- `collection_waits_for_pane_delivery_without_operator_dispatch`

### blocking-dispatch

Run collection synchronously on the supervisor thread: no waiting observation is published.

Command: `cargo test --locked --test supervision collection_waits_for_pane_delivery_without_operator_dispatch -- --test-threads=1`

Raw exit: 101; duration: 39.2 s. Log: `.probe-blocking-dispatch.log`.

Failing tests:
- `collection_waits_for_pane_delivery_without_operator_dispatch`

### no-deadline

Disable the remaining-budget exhaustion branch: the test deadline expires with collect still in flight.

Command: `cargo test --locked --test supervision collection_deadline_parks_worker_timeout_without_redispatch -- --test-threads=1`

Raw exit: 101; duration: 48.65 s. Log: `.probe-no-deadline.log`.

Failing tests:
- `collection_deadline_parks_worker_timeout_without_redispatch`

### timeout-class

Replace worker-timeout classification with needs-attention: expected worker-timeout, observed needs-attention.

Command: `cargo test --locked --test supervision collection_deadline_parks_worker_timeout_without_redispatch -- --test-threads=1`

Raw exit: 101; duration: 24.07 s. Log: `.probe-timeout-class.log`.

Failing tests:
- `collection_deadline_parks_worker_timeout_without_redispatch`

### empty-allowed

Disable requires_delta rejection: stopped/no-delta collection incorrectly succeeds.

Command: `cargo test --locked --test supervision collection_stopped_without_delta_is_still_refused -- --test-threads=1`

Raw exit: 101; duration: 22.72 s. Log: `.probe-empty-allowed.log`.

Failing tests:
- `collection_stopped_without_delta_is_still_refused`

The local runner is `.mutation-probes.py`; machine-readable receipts and restore hashes are in `.probe-results.jsonl`. The pinned-base leg uses the new tests against the actual base production files, not just a simulated stale-ref mutation.

## Structural search evidence

- `ast-grep run --lang rust --pattern 'fn effect_collect_outcome($$$ARGS) -> $RET { $$$BODY }' src/mutation.rs`: exit 0, one function matched; `.ast-collect.txt`.
- `ast-grep run --lang rust --pattern 'fn lane_integration_base($$$ARGS) -> $RET { $$$BODY }' src/mutation.rs`: exit 0, one function matched; `.ast-base.txt`.
- `ast-grep run --lang rust --pattern '"init"' tests --json=compact`: exit 0, ten initialization literal matches used to identify fixture prerequisites; `.ast-test-init.json`.

## Not exercised

- No live daemon, acceptance sandbox, real worker/pane, provider session, or live run was started, stopped, changed, or dispatched. All runtime witnesses use disposable test daemons, fake Herdr, and local Git repositories.
- No live #132 redrive, original missing-session-artifact investigation, or original poisoned-daemon investigation; the live-system fence forbids these here.
- The new wait witness ends at collection. It does not independently prove a new live p6/p7/p8 acceptance run or remote merge delivery.
- No wall-clock 1800/3600-second wait: tests use a reviewed one-second deadline and the deadline-table/ceiling unit tests.
- No native screenshot or manual TUI exercise; the closed vocabulary is exercised by unit tests.
- No hosted CI run, PR mutation, issue mutation, merge, deployment, or installation. Branch push only is authorized.
