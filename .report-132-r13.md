# Issue #132, round 13 — the reviewed published-ref cleanup proof, replayed onto integration base `5e02cf1`

Lane: canter's own implementer dispatch (run `run-e2b834468679b794`, step
`p4-132`, submission `qs_98e3a6f9232a38f7`), worktree `issues-132`, branch
`issue-132`, based on the run's recorded integration base
`5e02cf1ade68a103e944dfc074b9fe7b553ca541` (`origin/staging` at dispatch
time; carrying #214 via PR #215).

Commit graph: base `5e02cf1` -> content head
`91e38b0df63f757ded2cf70426c1bd07b3dcd5c2` -> the three report-only commits
the round-12 head carried (`.report-132-r10.md` `88bbd4ad…`,
`.report-132-r11.md` `4373815f…`, `.report-132-r12.md` `08dc00ee…`,
byte-identical to their copies on the previous head) -> this report's own
commit. This report's own commit follows the content head, so it can name it.

## What this round delivers, and why it is the bounded change

Issue #132's own text names the shape that must hold: *"the run's `merge` step
must never leave the integration checkout diverged from the published
integration ref, and the run must be able to complete on a repo whose policy
is squash."*

The merge-side half is in the base (#156/#168/#178/#182; #196 lands and
publishes under the plan's `merge_policy`, with the published ref always the
certified target, never a stale local view). The still-unlanded half is the
`cleanup` side: **when the sanctioned landing happens on the forge (the PR is
squash-merged), the checkout's own integration ref never sees it, so `cleanup`
must prove the landing against the FETCHED, verified PUBLISHED ref — and only
a checkout strictly behind it may certify from that view.** That content is
independently PASSed in the review lineage (`REVIEW-183-024783d4`,
`REVIEW-183-f4ea6876` — the latter names
`f4ea6876cb0ac8d39729f1a4bbfabe4eb2250a2b` exactly) and is replayed here onto
the rebuilt run base — the lineage's own refresh shape, never a re-design:

- `LandedProof::Published { published_head, merge_base }` — the third landed
  proof (ancestry / local content / published content), made against the
  fetched published head.
- `landed_in_published` — reads the published head
  (`published_integration_head`), refuses when the checkout's own ref IS the
  published view (no self-certification), FETCHES and verifies it
  (`fetched_published_head`), refuses whenever the local ref is not a strict
  ancestor of it (an unpublished or diverged local move is never certified),
  then proves the branch head by ancestry against the fetched head, or — for
  the squash landing, which never is an ancestor — by exact content on every
  path the branch changed relative to the fork point.
- `effect_cleanup` — when the local proof refuses `refusal.cleanup.unmerged`,
  tries the published route; if that view adds no proof, the ORIGINAL local
  refusal is returned unchanged. The salvage record of a published proof names
  `published_head` (and `merge_base` when the proof was by content) while
  keeping the checkout's own `integration_head`; deletion uses `-D`.
- `landed_in_integration` uses the base's shared fail-closed comparison
  helpers (`changed_paths_between`, `differing_paths_between`, landed with
  #196) instead of the round-5 private helper, so the local and the published
  proofs cannot drift.

Staging has moved one commit since the round-12 replay base
(`cce86742` -> `5e02cf1`: #214 via PR #215). No production behavior beyond
the replay is added in this round. The round-10..round-12 report files are
carried from the previous head byte-identically; prior reports are
byte-unchanged.

## Replay integrity — the reviewed content, byte-for-byte, on the new base

Every delivered path's delta against the new base is textually IDENTICAL to
its delta against the previous replay commit `9435305` AND to the originally
reviewed delta (`2d3d1ef8..1ba80d60`); hunk bodies compared, `@@` headers and
`index` lines excluded because offsets move:

| path | delta on the new base | vs `9435305` | vs reviewed delta |
| --- | --- | --- | --- |
| `src/mutation.rs` | +151/-64 | IDENTICAL | IDENTICAL |
| `tests/mutation_engine.rs` | +130/-0 | IDENTICAL | IDENTICAL |
| `CHANGELOG.md` | +20/-0 | IDENTICAL | IDENTICAL |
| `docs/contracts/spec-lifecycle.md` | +16/-2 | IDENTICAL | IDENTICAL |
| `docs/contracts/spec-plans.md` | +7/-3 | IDENTICAL | IDENTICAL |
| `.report-132-r7.md` | +112/-0 | IDENTICAL | IDENTICAL |
| `.report-132-r8.md` | +129/-0 | IDENTICAL | IDENTICAL |
| `.report-132-r9.md` | +141/-0 | IDENTICAL | IDENTICAL |

The content head's own stat is 8 files, 706 insertions(+), 69 deletions(-) —
the same count as the reviewed commit `7c9c9075` and the previous replay
`9435305`. Seven of the eight delivered files are byte-identical (sha256,
16 hex shown) to the previous head; only `src/mutation.rs` differs
(`6c977438f441f9fe` vs `dba49a5d7b1340c0`) because the base's own #214 work
rides along there: diff(previous head -> this content head) restricted to
that file is byte-identical, hunk for hunk, to the base's own
`cce86742 -> 5e02cf1` delta for the same file (clean three-way merge,
`cherry-pick` exit 0, no conflict, no base text dropped).

## Verification (raw exits, named logs)

All runs in the lane worktree at the head below, `CARGO_TARGET_DIR` outside
the checkout; logs in the lane artifact directory for this round (named by
basename below). Method: raw redirect to a named log, raw exit recorded, no
gate piped through a filter.

| # | Command (log basename) | Raw exit | Result |
| --- | --- | --- | --- |
| 1 | `cargo check --locked --all-targets` (`03-check.log`) | 0 | clean (1m12s) |
| 2 | `cargo test --locked --test mutation_engine cleanup_certifies_a_squash_landing_visible_only_on_the_published_ref -- --exact --nocapture` (`04-witness-solo.log`) | 0 | 1 passed; 0 failed (12.11 s) |
| 3 | `just ci`, attempt 1 (`05-just-ci.log`) | 101 | 3 unit binaries + 6 suites green; `daemon_rpc::non_draining_subscriber_is_disconnected_under_backpressure` FAILED at `:123` (`client.read`: `Resource temporarily unavailable (os error 35)` in the backpressure storm; host load 43 at start, fleet-wide load elsewhere that hour) |
| 4 | That selection solo (`05b-daemon-rpc-solo.log`) | 0 | 1 passed; 0 failed (110.62 s) |
| 5 | `just ci`, attempt 2 (`06-just-ci-run2.log`) | 101 | 34 test binaries green (incl. `mutation_engine` 33 passed, this delta's witness among them); `review_dispatch` FAILED 13/3 (load 35/115/88 at start; see adjudication) |
| 6 | `cargo test --locked --test review_dispatch` solo (`06b-review-dispatch-solo.log`) | 101 | 13 passed; 3 failed (10.87 s) — same three |
| 7 | Base-content probe: `git checkout 5e02cf1 -- src/mutation.rs tests/mutation_engine.rs` (reverted shas `1557c5d8…`, `046c4ad6…`) then the same selection (`06c-review-dispatch-base-probe.log`) | 101 | 13 passed; 3 failed, byte-identical failure set with the delta's source REVERTED; source restored byte-exactly afterwards (shas `6c977438…`, `45c4c58c…`; `git status` empty) |
| 8 | Remaining-suites batch (`07-remaining-suites.log`) | 101 | `run_control` 19 passed; `run_control_cli` 6; `service_plans` 3; `start_adopt` 11 — all green; `supervision` 27 passed / 2 failed (fixture `NotFound` read; `effect.worker_timeout` bounded wait) |
| 9 | `cargo test --locked --test supervision` solo (`07b-supervision-solo.log`) | 0 | 29 passed; 0 failed (95.06 s) |
| 10 | Final suites + doctests (`07c-later-suites-doc.log`) | 0 / 0 | `supervision_dispatch_refusal` 4 passed; `tui_board` 24; `tui_live_wiring` 10; `tui_operator` 5; `tui_supervision` 8 (all green); `--doc` 0 tests, exit 0 |
| 11 | `just doc && just build-release && just security` (`09-doc-build-release-security.log`) | 0 / 0 / 0 | doc generated; release build green (load 51.79 at start); scanner self-test OK; public-tree scan clean; cargo-deny `advisories ok, bans ok, licenses ok, sources ok`; cargo-audit clean (119 crate dependencies); `gitleaks directory scan … no leaks found` |
| 12 | `cargo test --locked --test review_dispatch` retry, then serialized `-- --test-threads=1` (`13-review-dispatch-retry.log`) | 101 / 101 | retry: 13/3, 7.37 s; serialized: 15 passed / 1 failed, 35.70 s (only the `no-take` case) — the failure set shrinks with serialized load, see adjudication |
| 13 | Mutation probe m1: the strict-behind guard in `landed_in_published` forced to `if false` (`10-m1-mutate.log`, `11-m1-red.log`) | 101 | RED at the guard's own property: the DIVERGED checkout is CERTIFIED instead of refused — `"removed":true`, `"landed_by":"content"`, published head named; assertion "expected refused response" at `tests/mutation_engine.rs:372` |
| 14 | Post-probe restore (`12-m1-restore.log`) | 0 | `src/mutation.rs` sha256 `6c977438f441f9fe…` re-verified equal to the recorded pristine hash; `git diff` empty; `git status` empty |

Method note for rows 3/5: `just ci` is fail-fast (fmt-check -> check -> lint
-> test -> doc -> build-release -> security), so each invocation covers the
recipe chain from the start; rows 9/10/11 cover every recipe and suite the
aborted invocations never reached.

## Adjudication

**Coverage.** 42 integration targets + 3 unit binaries + doctests. Every one
of them is green in at least one raw invocation above EXCEPT
`review_dispatch` (41/42 integration suites green; `daemon_rpc` green in rows
4 and 5; `supervision` green in row 9; the tail suites in rows 10/11).

**`review_dispatch` (rows 5, 6, 7, 12).** The three failing tests are the
#214/#215 delivery-witness tests added by the CURRENT BASE commit itself
(`5e02cf1`), all with `deadline_secs=1`: they expect a typed
`effect.review_timeout` / `refusal.prompt.undelivered`, and receive
`adapter.timeout` ("the operation exceeded its deadline and was cancelled")
from an adapter operation reaped at its deadline before the reviewer leg
registered (the fixture's reviewer pane is never written). The failure does
NOT follow this delta:

- Row 7 shows the identical failure set with the delta's source files
  REVERTED to the base (`5e02cf1`), source restored byte-exactly.
- The delta touches none of `tests/review_dispatch.rs`, and the failing
  suite's panes/deadlines are untouched by `src/mutation.rs`'s cleanup
  proofs (the cherry-pick composition proof above shows exactly the base's
  own text rides along, nothing else).
- Hosted CI at the same delta content (previous head `8687d8c5`, PR #183)
  reports every check `success`, including `--test review_dispatch`, and the
  base commit `5e02cf1` itself merged with green CI.
- Behavior under serialized load (row 12): the failure set shrinks from 3 to
  1 — a host-load/timing signature, not a logic change.

**`daemon_rpc` (row 3).** A client socket read returned `EAGAIN` in the
backpressure storm; the selection is green raw solo (row 4) and inside
attempt 2 (row 5) — the same suite the round-12 report recorded as
load-sensitive.

**`supervision` (row 8).** A fixture log read returned `NotFound` and a
bounded worker wait expired (`effect.worker_timeout`) under a load spurt; the
suite is green raw solo (row 9), the same class round 10 and round 12
recorded for this file.

**Canonical aggregate.** `just ci` never completed end to end on this loaded
host in two invocations; both raw exits are 101, each on a different suite
this delta does not touch, and every aborted recipe is covered green by rows
9-11. Hosted CI on the pushed head is the authoritative aggregate for the PR.

## Mutation proof (the guard is the discriminator)

The strict-behind guard in `landed_in_published`
(`if !is_commit_ancestor(ctx, &local_head, &fetched)`) was rewritten to
`if false` (exactly one occurrence; mutant sha256 `8cae932a…`). The delta's
own witness then fails at the guard's own property: the diverged checkout is
certified (`"removed":true`, `"landed_by":"content"`, `published_head`
named) where the witness demands `refusal.cleanup.unmerged`. Source restored
byte-exactly (sha256 re-verified equal to the pre-probe hash; `git diff`
empty; `git status` empty).

## Not delivered / not claimed

- No forge-merge route is implemented here: the `p7` landing form on a
  PR-protected integration ref (forge landing vs local landing) is the
  owner's ruling recorded in this issue's comments. This lane neither
  implements it nor dispatches, retries, pauses, releases or merges any run.
- No daemon, state or run was mutated by this lane. The only side effects are
  the branch's commits and the branch push.
- Hosted CI for the pushed head is reported by the PR, not by this report.
- No new production behavior beyond the replay; the witnesses are the ones
  the reviewed head already carried.
- `review_dispatch` is NOT claimed green on this host; the base-content probe
  (row 7) is the recorded reason it is adjudicated as base-side.

## Residual tension (recorded, not resolved here)

Cleanup's published route certifies a landing the forge performed; it never
performs or claims a forge merge itself (`spec-lifecycle.md`, `spec-plans.md`
state this explicitly). A run whose `merge` step must land by push on a
PR-protected integration ref still cannot complete end-to-end without either
(a) the forge landing performed externally and then certified here, or (b) the
owner ruling that changes the landing form. Recorded so round 13 is not read
as closing that question.
