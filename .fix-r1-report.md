# Fix round 1 report — issues #92 and #136

Status: implementation and local/Linux verification complete; push is frozen by the conductor while hosted run 34919718111 / rerun job 104232355364 is observed. No push was made after the freeze.

Base under repair: `c910d851e6fe628b6e0fbac0f72a598418babf56`
Remote branch at freeze: `047b5f0ae7c52a6b0fc7359ae32e6b7c0e4f1c13`
Verified local code head before this report: `13ac4a7b74eef784e0102f9db2daf7acfab2ae13`

## 1. Fixture processes are contained and reaped

Change

- `tests/support/process_group.rs:11-179` owns every fixture daemon in a dedicated process group. `GroupChild::terminate` performs group `SIGTERM`, waits at most five seconds, escalates to group `SIGKILL`, waits at most five seconds, reaps with `try_wait`, verifies the leader with `kill(pid, 0)`, and verifies that no non-zombie process-group member remains.
- `signal` at `tests/support/process_group.rs:115` defers Darwin `EPERM` and `ESRCH` from group signalling to the explicit leader/group verification. Unexpected signal errors still fail. A genuinely live member still makes `assert_gone` fail.
- All six direct daemon spawn sites audited in the five required files now route through `GroupChild::spawn`, which calls `process_group(0)`. Their stdout/stderr are files or `/dev/null`, never inherited:
  - `tests/executor_authoring.rs`
  - `tests/daemon_lifecycle.rs`
  - `tests/grant_issue.rs`
  - `tests/mutation_engine.rs`
  - `tests/supervision.rs` (two fixture constructors)
- The second-daemon refusal in `tests/grant_issue.rs` was also changed from synchronous `Command::output` to a process-group child with file-backed output and bounded wait/reap.
- Panic/failure cleanup uses the same helper through `Drop`.
- One explicit normal-body leak regression exists for each fixture style:
  - `tests/executor_authoring.rs:354`
  - `tests/daemon_lifecycle.rs:910`
  - `tests/grant_issue.rs:1190`
  - `tests/mutation_engine.rs:780`
  - `tests/supervision.rs:395`

Structural audit

Command:

`ast-grep run --pattern '$CMD.spawn()' --lang rust tests/executor_authoring.rs tests/daemon_lifecycle.rs tests/grant_issue.rs tests/mutation_engine.rs tests/supervision.rs`

Pre-fix result: six direct daemon process spawn sites were identified (`executor_authoring.rs:149`, `daemon_lifecycle.rs:89`, `grant_issue.rs:127`, `mutation_engine.rs:245`, `supervision.rs:230,266`). Post-fix, process creation is centralized in `tests/support/process_group.rs:19`.

GREEN

With `CARGO_TARGET_DIR=/tmp/canter-fix-r1-target CARGO_BUILD_JOBS=4`, each explicit detector exited 0:

- `cargo test --locked --test executor_authoring fixture_reaps_its_daemon_group -- --nocapture` — `EXECUTOR_LEAK_EXIT=0`
- `cargo test --locked --test daemon_lifecycle fixture_reaps_its_daemon_group -- --nocapture` — `LIFECYCLE_LEAK_EXIT=0`
- `cargo test --locked --test grant_issue fixture_reaps_its_daemon_group -- --nocapture` — `GRANT_LEAK_EXIT=0`
- `cargo test --locked --test mutation_engine fixture_reaps_its_daemon_group -- --nocapture` — `MUTATION_LEAK_EXIT=0`
- `cargo test --locked --test supervision fixture_reaps_its_daemon_group -- --nocapture` — `SUPERVISION_LEAK_EXIT=0`
- `cargo test --locked --test grant_issue a_second_daemon_is_still_refused_while_issuance_is_available -- --nocapture` — `SECOND_DAEMON_EXIT=0`

Darwin race regression

- `cargo test --locked --test grant_issue a_crash_after_the_commit_keeps_exactly_one_grant -- --nocapture`
- Raw result on committed head `99347d2674d5bf6f923fa2d655b44b2ce614dc1e`: `EPERM_CRASH_FOCUSED_EXIT=0`.
- The macOS-equivalent complete locked suite is also covered by final `just test` below.

RED / restoration probe

The disposable probe commented out the sole `GroupChild::Drop` reap, ran the real CI driver against `daemon_lifecycle`, then restored bytes:

- `PROBE_DRIVER_EXIT=101`
- `PROBE_DURATION_SECONDS=7.4`
- `PROBE_SURVIVORS=1`
- driver log: `::error::Tests --test daemon_lifecycle leaked 1 process(es); sweep reaped all`
- original/restored/head SHA-256: `2e5802b266023d5dd856d83ecc043b4c6e507fa5c62e0227420ee7114283733a`
- mutated SHA-256: `258966bd3fae53ca2ac7e69205a54c5c85a82870ba6249cae18125549aff7136`
- `PROBE_EXPECTED_RED_AND_SWEEP=PASS`
- `READY_LEAK_RESTORE_EXIT=0`

Residual: an unkillable or unauthorized genuinely live process remains a hard failure. Only `ESRCH`, or Darwin `EPERM` followed by proof that the child/group is gone, is accepted.

## 2. CI driver is bounded, diagnostic, and leak-safe

Change

- `scripts/ci-test-driver.py:19-421` runs each suite in a new session with stdin `/dev/null` and stdout/stderr redirected to per-suite files under `$RUNNER_TEMP/canter-test-logs` (`:324-327`).
- Each suite has a 300-second bound; the suite-only aggregate is 1,080 seconds.
- Every completed suite prints a bounded stdout/stderr tail, duration, last observed test name, survivor count, and timeout state.
- `reap_group` (`:156`) uses bounded `SIGTERM` then `SIGKILL`.
- `sweep_survivors` (`:144`) matches both the suite session and lexical/resolved private target paths, re-verifies PID command lines at signal time, terminates survivors, and fails if any remain.
- Signal and ordinary exit paths run the final survivor sweep.
- `write_summary` (`:241`) atomically updates `$RUNNER_TEMP/canter-test-summary.txt` after every suite and includes bounded failure tails.
- `.github/workflows/ci.yml:285-294` uploads the summary and complete per-suite log directory with `if: always()` using pinned `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` (`v4.6.2`).

Timeout/survivor probe

A fake cargo process opened a separate session and left a target-path child alive while the real driver used a one-second suite limit:

- `DRIVER_TIMEOUT_PROBE_EXIT=124`
- `DRIVER_TIMEOUT_PROBE_SURVIVORS=1`
- `DRIVER_TIMEOUT_PROBE_REMAINING=0`
- `DRIVER_TIMEOUT_PROBE_EXPECTED=PASS`

Summary-file probe

`python3 -u scripts/ci-test-driver.py --suite=--bins --aggregate-seconds 60 --per-suite-seconds 30 --log-dir /tmp/canter-fix-r1-summary-probe/canter-test-logs`

- driver exit 0
- summary exists at `/tmp/canter-fix-r1-summary-probe/canter-test-summary.txt`
- summary status `passed`, one suite row, exit 0, survivor count 0

Residual: if the Python driver itself wedges, the workflow-level OS cap in section 5 now ends the Tests step independently.

## 3. Full serial driver and deliberate leak proof

Linux exact-head full run

Host: `bazzite` (Linux x86_64), explicit two-CPU affinity, archive of `13ac4a7b74eef784e0102f9db2daf7acfab2ae13`, private cold Cargo target.

Commands represented by the bounded remote harness:

- `taskset -c 0,1 env CARGO_TARGET_DIR=<private> CARGO_BUILD_JOBS=2 timeout -k 15 300 cargo test --locked --no-run`
- `taskset -c 0,1 env CARGO_TARGET_DIR=<private> CARGO_BUILD_JOBS=2 RUNNER_TEMP=<private> timeout -k 15 1200 python3 -u scripts/ci-test-driver.py`

Raw results:

- `TWO_CORE_BUILD_EXIT=0`
- `TWO_CORE_BUILD_DURATION_SECONDS=38`
- `TWO_CORE_DRIVER_EXIT=0`
- `TWO_CORE_DRIVER_DURATION_SECONDS=250`
- `TWO_CORE_TOTAL_DURATION_SECONDS=288`
- `TWO_CORE_AFFINITY_CPUS=2`
- per-suite table: 39 rows, every exit 0, aggregate survivor count 0
- candidate archive SHA-256: `de9fd1417fc05d2c67dff6f28ab1b95a8619e46824c3329ee97e1888aa8c0310`
- owned remote scratch paths after cleanup: 0

Additional measured runs

- Linux/Bazzite two-CPU cold target at `047b5f0a`: build 55 seconds, driver 254 seconds, total 309 seconds, exits 0/0.
- Linux/Bazzite two-CPU cold target at `5008b896`: build 39 seconds, driver 253 seconds, total 292 seconds, exits 0/0.
- macOS at `5008b896`: warm pre-build 0 seconds, 39-suite driver 535 seconds, total 536 seconds, every exit 0, survivors 0.

The measured cold two-CPU build did not consume the 18-minute suite budget because compilation now has its own workflow step.

Deliberate leak result is the item-1 mutation above: the suite failed, survivor count was 1, the sweep reaped it, the driver returned without hanging, and source bytes matched HEAD afterwards.

## 4. Event-stream socket writes are bounded

Change

- `src/daemon.rs:57,7755-7784` configures a one-second write timeout before replay/live event writes. A persistent blocked write returns from `serve_event_stream`, dropping the stream so the peer observes EOF. Existing `sync_channel(SUBSCRIBER_QUEUE_CAP)` / `try_send` drop-on-full behavior was not changed.
- `src/daemon.rs:9112-9150` uses a non-draining Unix socket and an 8 MiB line to prove the write returns within the bound and the peer receives EOF. Its own receive deadline prevents the regression test from wedging.
- `src/client.rs:79` applies the existing 15-second I/O bound to writes as well as reads.
- `tests/daemon_rpc.rs:1040-1127` gives the storm socket explicit five-second read/write bounds. Existing assertions, including incomplete-storm delivery and EOF, remain intact.

Linux RED/GREEN witness

Host: `bazzite` (Linux x86_64). The RED mutation changed only the event-writer timeout to `None`; the fixed source was restored for GREEN.

- `timeout --signal=TERM --kill-after=5s 60s cargo test --locked --lib event_stream_write_timeout_closes_a_stalled_socket -- --nocapture`
  - unbounded-write mutation: `LINUX_RED_EXIT=101`; assertion reported that the writer remained blocked past the deadline
  - restored fix: `LINUX_GREEN_EXIT=0`
- `timeout --signal=TERM --kill-after=5s 120s cargo test --locked --test daemon_rpc non_draining_subscriber_is_disconnected_under_backpressure -- --test-threads=1 --nocapture`
  - `LINUX_INTEGRATION_EXIT=0`
- wrapper exit 0; remote source hashes matched their pre-probe values after restoration.

Residual: socket timeout closes a stalled event subscriber; normal draining subscribers and queue-level backpressure retain their prior semantics.

## 5. The workflow bounds the Tests step outside Python

Change

- `.github/workflows/ci.yml:258` raises only the Ubuntu job cap from 30 to 35 minutes.
- `.github/workflows/ci.yml:278-280` pre-builds all test executables under a separate five-minute step: `cargo test --locked --no-run`.
- `.github/workflows/ci.yml:282-283` executes the driver under the exact independent cap: `timeout -k 15 1200 python3 -u scripts/ci-test-driver.py`.
- The internal 1,080-second aggregate and 300-second suite limits remain active.

Budget arithmetic and measurement

- Job cap: 2,100 seconds.
- Pre-build cap: 300 seconds.
- OS cap including kill grace: 1,215 seconds.
- Guaranteed remainder inside the job after both bounded phases: 585 seconds (9m45s) for setup/check/clippy/docs/release/upload.
- The OS cap leaves 120 seconds beyond the internal aggregate before its 15-second kill grace.
- Actual cold two-CPU exact-head path: build 38 seconds + driver 250 seconds = 288 seconds.

Static contract probe:

- `WORKFLOW_OUTER_CAP=1200+15`
- `WORKFLOW_JOB_TIMEOUT_MINUTES=35`
- `WORKFLOW_PREBUILD_TIMEOUT_MINUTES=5`
- `DRIVER_INTERNAL_AGGREGATE_SECONDS=1080`
- `DRIVER_PER_SUITE_SECONDS=300`
- `WORKFLOW_CONTRACT=PASS`

Linux calling-pipe witness (real driver)

Host: `bazzite`. The harness connected combined driver stdout/stderr to a pipe with a dedicated reader. The normal case used a successful fake cargo command. The hard-kill case made the suite child ignore TERM so GNU `timeout -k 2 1` had to deliver SIGKILL while that escaped child remained attached only to its per-suite files.

- normal: driver exit 0 in 0.16 seconds; `PROMPT_EOF=1`; EOF lag 0.00 seconds
- hard kill: driver/timeout exit `-9` in 3.03 seconds (one-second TERM plus two-second kill grace); `PROMPT_EOF=1`; EOF lag 0.00 seconds
- harness verified and reaped its owned fake cargo/leaker PIDs
- `LINUX_OUTER_PIPE_WITNESS=PASS`

This proves both required cases: a normal driver exit and a `timeout -k` SIGKILL path release the caller's output pipe promptly.

Residual hosted evidence: run `34919718111` at remote head `047b5f0a` was cancelled with Tests still in progress and no artifact. A hosted run of the prepared follow-up head is still required after the conductor unfreezes pushes. No hosted-green claim is made here.

## Canonical gates at local code head `13ac4a7b74eef784e0102f9db2daf7acfab2ae13`

All commands used private `CARGO_TARGET_DIR=/tmp/canter-fix-r1-target` and `CARGO_BUILD_JOBS=4` where applicable.

- `just --list` — `JUST_LIST_EXIT=0`
- `just fmt-check` — `READY_JUST_FMT_EXIT=0`
- `just lint` — `READY_JUST_LINT_EXIT=0`
- `just test` — `READY_JUST_TEST_EXIT=0`; 40 result lines, 669 passed, 0 failed, 0 ignored
- `just ci` — `READY_JUST_CI_EXIT=0`; 40 result lines, 669 passed, 0 failed, 0 ignored
- `git diff c910d851e6fe628b6e0fbac0f72a598418babf56..HEAD --check` — `READY_DIFF_CHECK_EXIT=0`
- `python3 scripts/check-contract-fixtures.py` — `READY_CONTRACT_EXIT=0`
- `python3 scripts/test-check-contract-fixtures.py` — `READY_CONTRACT_SELFTEST_EXIT=0`
- `python3 scripts/test-check-public-tree.py` — `READY_PUBLIC_SELFTEST_EXIT=0`
- `python3 scripts/check-public-tree.py .` — `READY_PUBLIC_EXIT=0`
- `gitleaks dir --no-banner --redact --exit-code 1 .` — `READY_GITLEAKS_EXIT=0`

## Commit series on top of the original head

- `85f15d8658ade73c07de31b5fce69e524b8add86` — Reap test daemons and harden CI driver
- `99347d2674d5bf6f923fa2d655b44b2ce614dc1e` — Verify process groups after benign signal races
- `047b5f0ae7c52a6b0fc7359ae32e6b7c0e4f1c13` — Make CI test teardown and diagnostics durable
- `5008b896fa5e5ad218998e11d00156109781431e` — Separate test compilation from the CI suite budget
- `13ac4a7b74eef784e0102f9db2daf7acfab2ae13` — Bound the CI test step outside the Python driver

## Scope and delivery state

Changed product/test files relative to `c910d851`:

- `.github/workflows/ci.yml`
- `scripts/ci-test-driver.py`
- `src/client.rs`
- `src/daemon.rs`
- `tests/daemon_lifecycle.rs`
- `tests/daemon_rpc.rs`
- `tests/executor_authoring.rs`
- `tests/grant_issue.rs`
- `tests/mutation_engine.rs`
- `tests/supervision.rs`
- `tests/support/process_group.rs`

Orchestrator briefs and `F-LIST-92.md` remain untracked/ignored and were not added to product `.gitignore`.

Push state: frozen. Local ready head before this report is `13ac4a7b74eef784e0102f9db2daf7acfab2ae13`; remote remains `047b5f0ae7c52a6b0fc7359ae32e6b7c0e4f1c13`. The exact report-containing delivery commit is reported after creating it. No force push, PR mutation, merge, staging/main/release mutation, or live acceptance-state mutation was performed.
