# impl-198 report — a fan-out dispatch presents a host proof measured AT DISPATCH time (Refs #198)

Worktree `~/.herdr/worktrees/canter/impl-198`, branch `impl-198`, profile `fleet-impl`.
Candidate content commit **`51931a524d131be0551b616c46b819bf469cba90`**; base = published
`origin/staging` `6f334a2727ef240a5311851f78ea82e4465fb1c6` (verified with
`git ls-remote origin refs/heads/staging` before any edit). The DELIVERED head is this
report's own docs-only commit over that content (a report cannot cite its own SHA); no
source byte differs between them.

## 1. The defect, reproduced read-only first

- `daemon.log` (live sandbox, read-only): `run-85a856d6b9e9e7d0` p1/p2-132/p3/p4-132/p5-132
  dispatched 08:38:55–08:59:27Z with zero operator keys, then from 08:59:55Z repeated
  `supervision.dispatch_refused … step p6-132: refusal.admission.proof_stale` with
  `dispatch_backoff` ladders — exactly the issue's measurement.
- `state.db` (read-only): the run's newest presented admission carries
  `host_proof.measured_at = 2026-09-18T08:38:54Z` (submit time), i.e. `src/daemon.rs`
  re-presented the committed proof verbatim; `HostProof::fresh_at` allows 120 s, and no
  re-measure path existed.
- The live run (`run-85a856d6b9e9e7d0`, `run-c604f142afd5774c`) was never dispatched,
  paused, retried, released or resolved by this lane; both rows are byte-identical to
  their pre-lane state (`running` @ `p5-132` / `invalidated` @ `p5-132`).

## 2. What changed (8 files, +1158/−17)

| File | Δ lines | What |
| --- | --- | --- |
| `src/lifecycle.rs` | +153 | pure policy: `HostMeasurement` (`Measured{measured_at_unix, available_bytes}` / `Unmeasurable{reason}`), `HostProofRenewal`, `renew_lapsed_host_proof(presented, measurement, renewable, now)` + unit test |
| `src/daemon.rs` | +323 | `measure_host` (real host observation: free bytes at the lane root via `fs2::available_space`, stamped with the observation instant), `admission_with_renewed_proof`, `fanout_step_kind` (harness_start/prompt/reviewer-leg review_evidence), `renew_host_proof_for_dispatch` wired into `build_dispatch_request` (the supervisor's continuation path), 2 unit tests |
| `src/state.rs` | +39 | `record_host_proof_renewal` — one audited `host.proof.renewal` record (superseded instant, replacement measurement, observed free bytes), written BEFORE the renewed proof is presented |
| `tests/host_proof_renewal.rs` | +564 (new) | end-to-end witness over a real daemon + real CLI + fake harness (headless substrate) |
| `tests/supervision_dispatch_refusal.rs` | +41/−? | the two #141/#144 witnesses re-pointed at the still-refusing case (unmeasurable host) |
| `docs/contracts/spec-lifecycle.md` | +31 | §4.1: the supervisor's own measurement, bounded/audited/fail-closed |
| `docs/contracts/spec-daemon.md` | +23/−? | the apply + supervision paragraphs updated (no more "never fabricates" as the only answer) |
| `docs/OPERATIONS.md` | +1 | failure→remedy row for `refusal.admission.proof_stale` |

Semantics: the supervisor is a CALLER of the fan-out admission gate, so its own
continuation dispatch re-measures the host when the run's recorded proof has lapsed —
bounded (only a fan-out step, only a live run, only a proof that actually lapsed),
audited (`host.proof.renewal` journal record + `run.host_proof.renewed` log line, both
naming superseded/replacement/observed bytes), and fail-closed: an unmeasurable host
renews nothing, the recorded proof is presented unchanged and the gate still refuses
`refusal.admission.proof_stale` (`run.host_proof.unmeasurable` names why). No gate
weakens: caps, occupancy, pacing, overlap and the 120 s freshness bound itself are
untouched (`check_fanout_admission` is not modified).

## 3. Gates — `just ci` (canonical aggregate), one invocation, raw exit 0

Command: `just ci` (log `01-just-ci.log`), `CARGO_TARGET_DIR` outside the worktree.
Steps inside the aggregate: `cargo fmt --check`, `cargo check --locked --all-targets`,
`cargo clippy --locked --all-targets -- -D warnings`, `cargo test --locked`,
`cargo doc --locked --no-deps`, `cargo build --release --locked`, then security
(scanner self-test, public-tree scan, cargo-deny, cargo-audit, gitleaks dir scan:
"no leaks found"). **`JUST_CI_RAW_EXIT=0`**, 45 green suite results, no FAILED.

Touched suites inside that run: `host_proof_renewal` 2 passed (3.57 s),
`supervision_dispatch_refusal` 4 passed (22.76 s), lib unit tests 322 passed (2.85 s).

Extra gates at the delivered (report) head: `just fmt-check` and `just security`
re-run, raw exits in the lane's final message; doc-link check replicated locally
(clean); added lines contain no host paths and no secret-shaped content.

## 4. Mutation evidence (two probes, at the committed candidate content)

`src/daemon.rs` head sha256 `10ba3d45953ac184443f25df8010721efab5c5319ce74e1e9be602d14658be4f`.

- **Probe A — "echo the stored proof"** (renewal call removed; the exact anti-fabrication
  mutation the brief names): `cargo test --test host_proof_renewal` → **RAW_EXIT=101
  (RED)**, failing at "the frontier never reached p6-5" with the recorded status showing
  `observed.detail = refusal.admission.proof_stale`, `next_step = p3` — the live
  `p6-132` symptom reproduced in fixtures (`02-mutation-a-red.log`).
- **Probe B — "fabricate a measurement"** (`measure_host` always `Measured`):
  `cargo test --test supervision_dispatch_refusal` → **RAW_EXIT=101 (RED)**, both
  unmeasurable-host witnesses fail at their `refusal.admission.proof_stale` assertions —
  a fabricated measurement would silently pass, which is what the negative witnesses
  forbid (`05-mutation-b-red.log`).
- Restore: `cp` of the saved head file; restored sha256
  `10ba3d45953ac184443f25df8010721efab5c5319ce74e1e9be602d14658be4f` **identical** after
  each probe; re-runs GREEN: `host_proof_renewal` RAW_EXIT=0 (2 passed, 4.04 s),
  `supervision_dispatch_refusal` RAW_EXIT=0 (4 passed, 23.23 s).

## 5. What this proves, and what it does NOT

Proved (real runs, raw exits above): a submission whose recorded proof is older than the
freshness bound still reaches its fan-out steps — the run's own driver drove p3
(harness_start) and p4-5 (prompt) with ZERO operator dispatch and `run_retries` EMPTY to
the review frontier; the run's recorded dispatch context carries the dispatch-time
measurement (equal to the audited `replacement`, never the submit-time value); the
renewal rode the supervisor's own dispatch key; an unmeasurable host still refuses
`refusal.admission.proof_stale`, named, never eligible, never an attempt.

Disclosed gaps:

- The end-to-end positive witness uses a `harness_start` frontier (the issue's own "the
  `p3` case reproduces identically" shape) with the headless substrate and a fake
  harness. The live `p6-132` kind (a `review_evidence` step with a reviewer leg) is
  covered by scope at the unit level (`fanout_step_kind` unit test) and by the same
  dispatch path, but no fixture drives a reviewer-leg review step through the renewal
  end to end here.
- The operator's own `run dispatch` of a fan-out step is unchanged: it presents its
  explicit `--admission` attestation and the recorded proof otherwise (documented in
  §4.1); only the supervisor's continuation path renews.
- Hosted CI (the authority) and the live acceptance run were not exercised by this lane;
  nothing was pushed to `staging`, no PR was opened, and the live sandbox was untouched.
- The renewal's measurement is the host's lane-root filesystem (free bytes); other
  host-resource axes (load/memory) are not measured and no value-based policy is added.
