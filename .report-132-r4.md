# Issue #132, round 4 — the squash-landed cleanup proof, refreshed onto the current integration head

Lane `impl-132` (acceptance run `run-a79eec9c6f3ae005`, step p4) on branch `issue-132`.
Recorded base (the run's own checkout): `1dacc8e73026a7703c0d9485bc6a084b72c5d374`.
Candidate: `d30dab627621b89a98f168c4a21997b6617d911a` (this report commit follows it).

## What this round delivers, and why it is the bounded change

At this base the issue's last requirement was still unmet:

> Whatever shape is chosen, the run's `merge` step must never leave the
> integration checkout diverged from the published integration ref, and the
> run must be able to complete on a repo whose policy is squash.

The merge side is already in the integration base (#156/#168: `merge_policy`
input, read-only rehearsal, published-ref certification, typed unprovable-base
refusal). What still made completion impossible here was `effect_cleanup`: its
landed proof was ancestry-only, and this repository SQUASH-lands the PR into
`staging`, so the lane head is never an ancestor afterwards — every
`cleanup` refused `refusal.cleanup.unmerged` forever.

Candidate diff `1dacc8e..d30dab6`, 5 files, +380/−26 (tests diff: zero deleted
lines):

```
 CHANGELOG.md                     |  20 ++++
 docs/contracts/spec-lifecycle.md |  23 +++++
 docs/contracts/spec-plans.md     |  11 ++-
 src/mutation.rs                  | 194 ++++++++++++++++++++++++++++++++++-----
 tests/mutation_engine.rs         | 158 ++++++++++++++++++++++++++++++++
```

- `landed_in_integration` + `LandedProof`: ancestry, or — when not an ancestor —
  content-equivalence of every path the branch changed relative to its fork
  point, compared fail-closed (NUL-delimited names, both rename endpoints,
  literal pathspecs, replacement-character/malformed-framing refusal, empty-name
  refusal, empty-path-list guard). Deletion is `-D` only on the proven-content
  route; `-d` stays on the ancestor route; the salvage record names
  `landed_by: ancestor|content` (with `merge_base`), and the refusal names the
  count and first differing path.
- witnesses `cleanup_accepts_a_squash_landed_lane_and_still_refuses_unlanded_content`
  and `cleanup_content_proof_preserves_exact_paths_and_partial_landings` (real
  daemon effect + real git trees; unicode, newline/tab/quote/backslash,
  pathspec magic, glob, rename, literal U+FFFD; a Linux-only non-UTF-8 leg).

## Provenance: the reviewed round replayed, not new code

This is the #171/#175 lineage (`48dba33` content proof + `6a892f1` fail-closed
repair of the B-1/B-2/B-3 findings, issue #172) replayed onto `1dacc8e`.
Equivalence to the PASSED head `ec2e91a8d8fe7d0dd53281ce7d3a19f056b31b34`
(independent review `rev-171-fix1-r1`, verdict PASS):

- `CHANGELOG.md`, `docs/contracts/spec-lifecycle.md`,
  `docs/contracts/spec-plans.md`, `tests/mutation_engine.rs` are byte-identical
  (`git diff --stat ec2e91a -- <those paths>` empty; the tests blob sha256
  `a630d8fbec07d393d047ad06104101368c4802584fa30d5a2de4903ab1c533c9` matches the
  review's independently recorded hash).
- `src/mutation.rs` differs from `ec2e91a` only in the regions #174 changed:
  grepping the whole `git diff ec2e91a -- src/mutation.rs` for
  `landed_in_integration|LandedProof|literal-pathspecs` finds 0 matches — the
  cleanup-proof region is byte-identical to the reviewed head (delivered blob
  sha256 `acea19de0cde34a9b3e246ad208bbeaeb31222c7f6dca2390c69dd74d8394b12`).
- The prior lane reports are carried here byte-unchanged: `.report-132-r3.md`
  sha256 `d805832e90f1b3ed8dec3782c46e093340ea2865ea6bad7d58dfb3826f300068` and
  `.report-171-fix1.md` sha256
  `7235521cabd6db9fb3859b54969a81188736cd219ba483abe0e175dc5ec29d8b`.

## Verification at the delivered candidate (raw exits, named logs)

All runs in the lane worktree; `CARGO_TARGET_DIR` outside the checkout; logs in
the round's local evidence directory (named by file below).

| # | Command | Raw exit |
| --- | --- | --- |
| 1 | `cargo build --locked --tests` (`01-build.log`) | 0 |
| 2 | witness, serialized (`02-green-witness.log`) | 0 — 1 passed, 8.78s |
| 3 | matrix, serialized (`03-green-matrix.log`) | 0 — 1 passed, 17.61s |
| 4 | base control: same tests against `1dacc8e:src/mutation.rs` (`04a`, `04b`) | 101, 101 — 0 passed / 1 failed each |
| 5 | probes (`05-*`) | 101, 101, end-control 0 |
| 6 | `RUST_TEST_THREADS=1 just ci`, ONE invocation (`06-just-ci-serialized.log`) | **0** |

### Base RED — the witnesses discriminate the pre-fix tree

Against the base source both witnesses fail with the issue's own refusal:

```
04a-red-base-witness.log:
assertion `left == right` failed: apply x1 (seed 20) failed:
{"error":{"code":"refusal.cleanup.unmerged","message":"branch \"issue-123\" head is
not merged into \"staging\"; cleanup refuses unverified deletion"},"ok":false}
RAW_EXIT=101
04b-red-base-matrix.log: the same refusal, RAW_EXIT=101
```

At the delivered head that scenario answers `removed:true`,
`landed_by:"content"`, and every negative case refuses with the branch
surviving (`unicode/controls/pathspec/glob/rename/replacement` all print
`git branch --list issue-123 = "+ issue-123\n"`).

### Mutation probes at the delivered head (`probe-132.py`)

One-flag mutations against the committed bytes; the committed blob is restored
and its sha256 re-verified (`acea19de…`) after each leg; no mutation ran while
a splice was in flight.

| Mutation | First-failing case | Observed | Raw exit |
| --- | --- | --- | --- |
| `M2-find-renames` (first `--no-renames` → `--find-renames`) | rename | branch deleted (`git branch --list issue-123 = ""`) — partial landing certified | 101 |
| `M3-no-literal-pathspecs` (drop `--literal-pathspecs`) | pathspec | branch deleted — unlanded lane certified | 101 |
| end control after both restores | — | matrix green again, 1 passed, 20.17s | 0 |

### Canonical aggregate

```
06-just-ci-serialized.log:
  RUST_TEST_THREADS=1 CARGO_TARGET_DIR=<outside the checkout> just ci
  START 2026-09-16T23:48:23Z   END 2026-09-17T00:07:10Z
  RAW_EXIT=0
  42 green `test result: ok` blocks; 742 passed / 0 failed
  fmt-check, check, lint: green; doc + build-release: green
  security: scanner self-test green; public-tree scan green;
            cargo-deny "advisories ok, bans ok, licenses ok, sources ok";
            cargo-audit green; gitleaks "no leaks found"
  mutation_engine serialized: 25 passed / 0 failed in 82.37s
  (the hosted per-suite budget is 150s)
```

No test was deleted or weakened, no gate was lowered, and the refusal surface
was not relaxed: `refusal.cleanup.unmerged` still refuses every unproven
deletion.

## The acceptance run collected this delta itself

`run-a79eec9c6f3ae005`: steps p1, p2-132, p3, p4-132, p5-132 all spent with
`"error":null` — five successes, zero refusals (no
`refusal.collect.empty_delta` this time). `p5-132` resolved at
`2026-09-16T23:45:30Z`, one second after the candidate commit, and its recorded
response reads:

```
{"base_head":"1dacc8e73026a7703c0d9485bc6a084b72c5d374","branch":"issue-132",
 "changed_files":["CHANGELOG.md","docs/contracts/spec-lifecycle.md",
                  "docs/contracts/spec-plans.md","src/mutation.rs",
                  "tests/mutation_engine.rs"],
 "commits":["d30dab627621b89a98f168c4a21997b6617d911a"],
 "head":"d30dab627621b89a98f168c4a21997b6617d911a",
 "requires_delta":true,"worktree":"issues-132"}
```

## Hosted CI and pushed-head read-back

- `gh api repos/.../commits/d30dab6.../check-runs` (paginated): 48 check runs —
  **47 completed/success, 1 completed/skipped**; all 47 are `github-actions`
  (rust-macos, the rust-ubuntu matrix, policy, secret-scan, supply-chain). The
  one skip is `[code]smith`, a third-party opt-in autofix bot whose summary
  reads "not active on this PR" — not a repository gate (the repo workflows are
  `ci.yml` and `security.yml`).
- `git ls-remote origin issue-132` = `d30dab627621b89a98f168c4a21997b6617d911a`;
  PR #171 `headRefOid` read back at the same SHA.

## Not verified / disclosed

- The Linux-only non-UTF-8 filename leg (`#[cfg(target_os = "linux")]`) does not
  execute on this APFS host; its guard is witnessed portably via literal U+FFFD.
- Case-only / NFC-NFD collisions and dedicated submodule or malformed-output
  injection witnesses remain unexercised (carried over from the reviewed round's
  disclosed list).
- The p8 content route only takes effect in a daemon built at/after this
  content; the live acceptance's p8 completion is not demonstrated by this
  round. This round's own contract is the committed, gated, pushed delta and
  the p5 collection recorded above.
- The hosted checks at `d30dab6` are green; the report-only commit on top
  re-triggers the PR checks.
