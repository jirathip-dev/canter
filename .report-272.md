# Report — canter#272: a fix round's delivered head is re-bound by the run's own machinery

Lane: `.herdr/worktrees/canter/impl-272` (branch `impl-272`), base
`origin/staging` = `8a9e28e95f88d1459ed91011a5a8c39a8592d278`.

Delivered implementation head (all code + tests; every gate below was run at this exact
tree): `02c6e60b9a1ec404d7d3d5d4ec153f4b72368b52` (this report rides the branch's next
commit).

## The defect, reproduced inside the repo

`refusal.delivery.unbound` (#202 AC2, `src/mutation.rs:369-372`) fires whenever a step
consumes a head the run's own collector never certified. A run whose own fix round
delivered a MOVED head has exactly that shape: the FAIL handoff was dispatched for the
pre-fix head, the repair leg's lane checkout moved past it, and there is no path by which
the run's own collector observes the new head — the collector is one-shot
(`refusal.run.step_done`), a re-evaluation addresses a terminal-success producer
(`refusal.run.reevaluation_step`), and the held bounded-retry authorization is refused by
`run release` (`refusal.run.retry_pending`). The run parks forever holding a counted slot.

Reproduced with a mutation probe against the committed witnesses (the probe neutralizes the
new re-collect derivation: `return None` as the first statement of the re-collect intent).
With the probe in place the live-daemon witness parks in precisely the reported shape — the
review re-entry is dispatched at the STALE certificate and refused, and nothing further
happens for the whole window:

    mutation-witness.log (RAW_EXIT=101):
    the run never re-bound its fix delivery: no progress for 120s of 121s waited;
    observed: ["t1 status=new next=m1/merge attempts=[(p1,succeeded),(s1,succeeded),(o1,succeeded),(r1,succeeded)]",
               "t2 status=new next=r1/review_evidence attempts=[...,(r1,refused)]", ... "t219 ..."]

The same probe fails the unit witness (`src/supervision.rs:4709`). Restored by copy +
sha256 check (`src/supervision.rs: OK`), both witnesses green again.

## What the run's own machinery now does (no operator step-dispatch)

1. `src/supervision.rs` — the supervision evidence carries the run's recorded DELIVERY
   BINDING (`state.rs`: read from the same rows the verified-delivery consumer reads), and
   two derived facts: the fix delivery is UNCERTIFIED (the newest recorded verdict is still
   the `fail` that names the pre-fix head) / RE-CERTIFIED. `fix_recollect_intent` derives
   the run's ONE bounded continuation from those facts plus the repair leg's observable
   checkout (`observe_fix_leg`, which until now informed classification only): the run's
   own committed collector step, reason `supervision.recollect.fix_delivery`. A handoff
   whose checkout cannot be read back, a leg that did not move, or an already-re-certified
   delivery derives nothing. `dispatch_intent` wires this BEFORE the frontier walk, and the
   review re-entry goes through the run's own bounded control
   (`supervision.reevaluation.next_round` = `run.reevaluate`, one recovery act, #243).
2. `src/daemon.rs` — `recollect_step_params` presents the committed collector step's own
   inputs with the repair leg's recorded lane checkout as the worktree the collection
   OBSERVES. The fix lane is the engine's own `git worktree add --detach` checkout
   (`src/lane.rs`), so the committed `branch` binding is dropped rather than fabricated; the
   observed branch is whatever that checkout holds, and a checkout left without a branch
   refuses typed. `method_apply_from`'s supervised-apply eligibility read now takes the
   SAME `observe_fix_leg` read the driver derived from, so a dispatch the driver derived is
   never refused for a fact the driver read.
3. `src/state.rs` — `SupervisionEvidence.delivery` (the run's recorded certified binding).

The #202 AC2 fence is untouched: the re-collect is a REAL collection (`collect_outcome`)
that observes the leg's checkout itself; the certificate only moves because a collection
observed the head.

## Diff scope

    src/daemon.rs          | 126 +++++++++-
    src/state.rs           |  14 ++
    src/supervision.rs     | 331 +++++++++++++++++++++-
    tests/queue_advance.rs | 635 +++++++++++++++++++++++++++++++++++++++++++++
    4 files changed, 1100 insertions(+), 6 deletions(-)

No new `tests/` suite was added (the witness lives in the existing, already-enumerated
`queue_advance` suite), so `.github/workflows/ci.yml` needs no matrix change — the driver
test confirms it.

## Base / integration disclosure

- `origin/staging` MOVED during the lane: it is now
  `86e8579c1ec0db6598b60089d595821e573ccacf` ("A supervision pass reads the rows it
  names once… (Refs #268) (#271)"), not the brief's `8a9e28e9…` this branch is based on.
  `8a9e28e9` is an ancestor of it. Per the brief, no rebase was attempted here.
- Read-only merge dry-run against the moved staging (`git merge-tree --write-tree HEAD
  86e8579c`) → RAW exit 1: `src/daemon.rs` and `src/supervision.rs` auto-merge, and
  `src/state.rs` CONFLICTS (both sides restructure `supervision_evidence`'s reads —
  #271 collapsed the per-call-site evidence reads, this lane adds the recorded delivery
  binding there). Log: `/tmp/impl-272-logs/merge-dryrun.log`. The orchestrator owns that
  reconcile.
- `refs/heads/impl-272` did not exist on the remote before this lane's push.

## Acceptance criteria

| AC | Requirement | Evidence |
|----|-------------|----------|
| AC1 | After a fix delivery the run re-enters review at the delivered head and advances with NO operator step-dispatch — raw cursor before/after + audit rows | Live-daemon witness: BEFORE the driver's own acts the run's typed cursor is `next=m1/merge` with the newest recorded verdict the `fail` at the reviewed head and the certificate at that head; AFTER them the reviewer's `pass` is recorded at the DELIVERED head, the certificate names it, and the run advances onto the committed merge. The driver's own log lines: `supervision.dispatch … dispatched step o1 (supervision.recollect.fix_delivery)` and `… re-evaluated step r1 through the run's own bounded control (supervision.reevaluation.next_round)`. Every `apply` claim after the seeded handoff carries the DRIVER's own key (`ik_<run>-…`), asserted row by row, so no operator step-dispatch exists anywhere. |
| AC2 | The binding is re-established by the run's OWN machinery; the #202 AC2 fence is NOT loosened | The certificate moves only to the head the run's OWN collector observed, at the repair leg's own checkout (`certificate.step_id == "o1"` asserted); the fence code is unchanged in the diff. |
| AC3 | A run whose held authorization cannot be spent never pins a counted slot indefinitely | The re-collect is `refusal.run.step_done`-free precisely because it is the run's own control, not an operator step-dispatch: the run spends its bounded authorization and completes instead of parking. |
| AC4 | The review path records the reviewer's verdict once the delivery re-binds (no stale `FAIL at the pre-fix head` while a later PASS sits on disk) | Newest recorded evidence asserted at the DELIVERED head with `verdict == "pass"`; the FAIL row is retained as history. |
| AC5 | `Refs #272` wording; no direct push to `main`; no gate bypass; no host paths/private repo/profile/model names in added lines | Commit message carries `Refs #272`; branch pushed to `impl-272` only; `git diff` added lines contain no host path, private repository, profile or model name. |

## Gates (RAW exit per command, named logs; never piped as pass/fail)

All seven stages `just ci` composes were also run individually (one raw exit each):

| # | Command | RAW exit | Log |
|---|---------|----------|-----|
| 1 | `just fmt-check` | 0 | `/tmp/impl-272-logs/gate-fmt-check.log` |
| 2 | `just check` | 0 | `/tmp/impl-272-logs/gate-check.log` |
| 3 | `just lint` | 0 | `/tmp/impl-272-logs/gate-lint.log` |
| 4 | `just test` | 101 | `/tmp/impl-272-logs/gate-test.log` |
| 5 | `just doc` | 0 | `/tmp/impl-272-logs/gate-doc.log` |
| 6 | `just build-release` | 0 | `/tmp/impl-272-logs/gate-build-release.log` |
| 7 | `just security` | 0 | `/tmp/impl-272-logs/gate-security.log` |
| 8 | `python3 scripts/test-ci-test-driver.py` | 0 ("Ran 15 tests … OK") | `/tmp/impl-272-logs/ci-test-driver.log` |

`just test` is RED at this head and at the CLEAN BASE, identically — one pre-existing
parallel-suite flake in `tests/mutation_engine.rs`, not this diff. Adjudication:

- At this head: `test result: FAILED. 35 passed; 3 failed` — `disconnected_apply_is_resolved_
  after_its_effect_deadline_without_restart` ("effect never started", `tests/mutation_engine.rs:1136`),
  `cleanup_certifies_a_squash_landing_visible_only_on_the_published_ref` (fixture git
  `cannot lock ref 'HEAD': reference already exists`), and
  `p7_computes_hosted_ci_at_the_certified_head_and_never_publishes_over_red` (fake `gh`
  exceeded its deadline: `adapter.timeout` vs expected `effect.merge.ci_red`).
- Clean base `8a9e28e9` (temporary worktree, removed afterwards), same suite, same host:
  `test result: FAILED. 37 passed; 1 failed` with the IDENTICAL panic —
  `disconnected_apply_is_resolved_after_its_effect_deadline_without_restart … "effect never started"`
  (`/tmp/impl-272-logs/base-mutation_engine.log`, RAW_EXIT=101).
- In isolation at this head the three pass or resolve: `cargo test --locked --test
  mutation_engine disconnected_apply_is_resolved_after_its_effect_deadline` → RAW exit 0, `1
  passed` (`mutation-disconnected-rerun.log`); a repeat of the whole suite at this head left
  only the shared base flake (37 passed / 1 failed, `mutation_engine-alone-2.log`; the earlier
  `mutation_engine-alone.log` had the same single failure).
- Every other suite in `just test` was green at this head, including `queue_advance`
  (15/15) and the 368 lib tests.
- Serialised, the same suite is fully GREEN at this head: `cargo test --locked --test
  mutation_engine -- --test-threads=1` → RAW exit 0, `38 passed; 0 failed`
  (`/tmp/impl-272-logs/mutation_engine-serial.log`). The red is parallel-execution
  contention on this host (38 daemon-spawning tests at once, load average ~8.9), which the
  clean base reproduces — not this diff.
- One gate-hygiene disclosure: the shared `CARGO_TARGET_DIR` briefly held artifacts from the
  base-identity build, and the first post-base `cargo test` reported a spurious
  `E0560 struct SupervisionEvidence has no field named delivery` (fingerprint pollution, not
  the tree: the committed tree compiles — `cargo test --locked --no-run` RAW exit 0,
  `/tmp/impl-272-logs/rebuild-no-run.log`). The aggregate `just ci` was re-run after that
  rebuild: RAW exit **101**, failing only at the `test` stage with the SAME single
  base-reproduced flake (22 suites green; `mutation_engine` 37 passed / 1 failed, the
  `disconnected_apply…` deadline), `/tmp/impl-272-logs/gate-just-ci-aggregate.log`. Host
  load average was 8.6 at the time (sibling lanes in flight), which is what that test's
  10-second effect deadline is sensitive to.

Focused witnesses (raw exits):

| Command | RAW exit | Log |
|---------|----------|-----|
| `cargo test --locked --test queue_advance` | 0 (15 passed; 0 failed) | `/tmp/impl-272-logs/queue_advance-full.log` |
| `cargo test --locked --lib` | 0 (368 passed; 0 failed) | `/tmp/impl-272-logs/lib-full.log` |
| mutation probe → `cargo test --locked --test queue_advance a_fix_rounds_delivered_head` | 101 (RED) | `/tmp/impl-272-logs/mutation-witness.log` |
| mutation probe → `cargo test --locked --lib a_fix_delivery` | 101 (RED) | `/tmp/impl-272-logs/mutation-lib.log` |
| restored → `cargo test --locked --lib a_fix_delivery` | 0 (GREEN) | `/tmp/impl-272-logs/restore-lib.log` |

## Fix round 1

Round brief: both hosted reds at `c896cf0bdbdd78d00045b0c03e75d74f2064b630` (PR #275) are real and
are this round's whole scope. Lane-local target dir `CARGO_TARGET_DIR=/tmp/impl272-target` (never a
shared dir — a shared one has already hidden a compile break on this repo).

### Red 1 — `--test queue_advance` (14 passed / 1 failed): the re-entry was recorded twice

Hosted raw (`rust-ubuntu (--test queue_advance)`, run `36006863186`; the panic's audit trail, log
`/tmp/impl272-logs-r1/ci-log-failed.txt`):

    thread 'a_fix_rounds_delivered_head_is_re_bound_by_the_runs_own_machinery' panicked at tests/queue_advance.rs:3486:5:
    assertion `left == right` failed: the review re-entry is the run's own bounded, recorded control: [
      ("mutate.collect_outcome", "example-org/widgets:run-89ff94abfd369c41:o1"),
      ("outcome.mutate.collect_outcome", "ik_run-89ff94abfd369c41-o1-1790257581"),
      ("mutate.run.reevaluate", "run:run-89ff94abfd369c41:r1"),
      ("run.reevaluate", "…:step:r1:evidence:ev_ff38c12c8bc44992:operator:supervision:…"),
      ("mutate.review_evidence", "example-org/widgets:run-89ff94abfd369c41:r1"),
      ("outcome.run.reevaluate", "ik_run-89ff94abfd369c41-r1-1790257581"),
      ("mutate.run.reevaluate", "run:run-89ff94abfd369c41:r1"),
      ("run.reevaluate", "…:step:r1:evidence:ev_ff38c12c8bc44992:operator:supervision:…"),
      ("mutate.review_evidence", "example-org/widgets:run-89ff94abfd369c41:r1"),
      ("outcome.run.reevaluate", "ik_run-89ff94abfd369c41-r1-1790257582"),
      ("mutate.merge", "example-org/widgets:run-89ff94abfd369c41:m1")]
      left: 2   right: 1

Two `run.reevaluate` records name ONE verdict row (`ev_ff38c12c8bc44992`) and two review rounds were
dispatched for it. The driver's derivation was re-taken while the first round's own records had not
landed yet — the "is that round settled?" read (`evidence.in_flight`, the producer's latest attempt)
races the round's outcome journaling, and `RUN_REEVALUATION_MAX = 3` is a budget, not a
single-re-entry guarantee. Not reproducible on this host in five local runs of the very test (1
serial, 2 under 8 burners, 2 under 32 burners: all `1` record, all green) — the window is too narrow
here. **The hosted leg is the evidence; local green is not proof of a fix**, so the fix was proved
discriminatingly instead (below).

**Decision (derivation wrong, not the witness):** the re-entry is now single per recorded verdict.
`run_reevaluation_counts_locked` also returns the verdict row each recorded re-entry was taken from —
read from the same hash-chained record, no second read (#271's own discipline) —
`SupervisionEvidence.reevaluations` carries it, and `reevaluation_intent` derives NO second re-entry
while the run's newest verdict is still that row. A re-run that ANSWERS records a new verdict row, so
the remedy stays reachable for the fresh FAIL inside the same bound; a re-run whose dispatch refuses
is a recorded dispatch refusal the operator reads. The control's own bound is untouched, and the
witness keeps its assertion at `1` unchanged.

Discriminating proof of the gate (local, deterministic): the new case (6) of
`supervision::tests::a_fix_delivery_the_runs_own_collection_never_observed_is_re_collected` asserts
`dispatch_intent == None` while the newest verdict row is the one a recorded re-entry already names,
and a fresh verdict row re-opens the remedy. Mutation probe (neutralize the gate: `if false && …`) →
`cargo test --locked --lib a_fix_delivery` RAW exit 101
(`a verdict that already owns its one re-entry is never re-entered again`,
`/tmp/impl272-logs-r1/mutation-r1.log`); restored by copy + `shasum -a 256 -c` → `src/supervision.rs: OK`
→ RAW exit 0 (`/tmp/impl272-logs-r1/lib-post-4.log`).

### Red 2 — `--test run_control` (28 passed / 1 failed, `tests/run_control.rs:4731`)

Reproduced locally on the first run (`/tmp/impl272-logs-r1/run_control-pre.log`, RAW exit 101): the
failing test is `a_delivered_repair_leg_is_named_and_the_next_round_binds_the_delivered_head`, from PR
#257 (`Refs #256`) — the previous implementation of this behaviour, not this lane's diff. Its
fixture's own daemon log names the mechanism (`run_control-pre-fixture-daemon.log`):

    supervision.dispatch_refused | run run-2e78d69ba3161a7f step p5: refusal.worker.output_location:
      collection base bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb is unavailable or is not an ancestor
      of the worktree HEAD
    supervision.dispatch_backoff | run run-2e78d69ba3161a7f step p5: 1 consecutive refused dispatches; next attempt in 9s

The driver did act: its FIRST act is now the run's own RE-COLLECT of the delivered head (correct per
#272 AC2 — no step may consume a head the run's own collection never observed), and that fixture
recorded its collection base as a synthetic sha (`"bb".repeat(20)`) that exists in no repository. The
pre-#272 path read no base at all (it re-dispatched the review producer directly), so the synthetic
base was never exercised; under #272 the engine correctly fails closed on it for the whole window and
the p6 inner claim the witness polls for never appears.

**Decision — (b) the fixture, not the assertion, is superseded.** The assertion is kept at full
strength; nothing is weakened, skipped, deleted or relaxed, and what it proves is not changed:

- The fixture's recorded collection base is now a REAL commit the repair leg's checkout holds
  (`certified`, which the delivered head descends from) — the new contract's own precondition. The
  pre-#272 fixture had no reason for a real base; #272's re-collect measures its delta over exactly
  that base, and a base no checkout can reach can only park the run on the collection's own typed
  refusal.
- (0) the pre-#256 read and (1) the observed disposition are untouched, byte for byte.
- (2) now proves BOTH halves of the new contract and is strictly stronger: (2a) the run's OWN
  certificate names the DELIVERED head (new — driven with zero operator control), and (2b) the same
  inner-claim assertions verbatim: `feature_head` is the DELIVERED head, and never the certified one.
- The one relaxation is a WAIT bound (30 s → 60 s) for two driver acts instead of one; it asserts
  nothing weaker.
- `tests/run_control.rs:3372`'s durable-bound assertion carries the reader's new third field
  (mechanical; still `RUN_REEVALUATION_MAX` with the newest verdict row asserted too).

Evidence at the fixed head (`-- --nocapture`, `/tmp/impl272-logs-r1/run_control-post-2.log`):

    PRE-#256 READ class=waiting-workers reason=supervision.fix_round_dispatched detail=lane-0123456789abcdef eligible=false
    supervision.status.observed => {"class":"needs-attention","detail":"lane-0123456789abcdef (reviewed at 66429ea0ece3, delivered at b27fc4478d8d)","eligible":false,"reason":"supervision.fix_round_head_moved"}
    CERTIFIED => step=p5 head=b27fc4478d8d6468aee9555ed5e9f1495ce1dc88
    INNER DISPATCH request => {"id":"disp_p6","method":"apply","params":{…"feature_head":"b27fc4478d8d…"…}}

### Diff scope (this round)

    src/state.rs         | the re-evaluation reader returns (step, count, newest verdict row)
    src/supervision.rs   | the re-entry is single per recorded verdict + its unit witness case
    tests/run_control.rs | the #256 fixture's collection base is real; section (2) re-pointed

### Gates (raw exits, named logs; lane-local target dir)

| # | Command | RAW exit | Log |
|---|---------|----------|-----|
| 1 | `cargo test --locked --test run_control` | 0 (`29 passed; 0 failed`) | `/tmp/impl272-logs-r1/gate-run_control.log` |
| 2 | `cargo test --locked --test queue_advance` | 0 (`15 passed; 0 failed`) | `/tmp/impl272-logs-r1/gate-queue_advance.log` |
| 3 | `cargo clippy --locked --all-targets -- -D warnings` | 0 | `/tmp/impl272-logs-r1/gate-clippy.log` |
| 4 | `cargo fmt --check` | 0 | `/tmp/impl272-logs-r1/gate-fmt-check.log` |
| 5 | `cargo test --locked --lib` | 0 (`368 passed; 0 failed`) | `/tmp/impl272-logs-r1/gate-lib.log` |
| 6 | `just ci` (1st) | 101 | `/tmp/impl272-logs-r1/gate-just-ci.log` (load before `{ 2.95 3.27 3.30 }`, after `{ 2.66 2.95 3.17 }`) |
| 7 | `just ci` (2nd) | 101 | `/tmp/impl272-logs-r1/gate-just-ci-2.log` (load before `{ 2.63 2.83 3.08 }`, after `{ 3.00 2.83 2.98 }`) |

Both aggregates fail in ONE suite this round's diff does not touch — `tests/review_dispatch.rs`,
19 passed / 4 failed, `adapter.timeout` where `effect.review_timeout` / `refused` is asserted: the
documented local-only class on a host that RUNS a live `herdr` (the review effect reaches the real
host executable). Adjudication, not a pass:

- It is not load: the same suite isolated (`cargo test --locked --test review_dispatch`) reds
  identically at load `{ 2.40 2.89 3.14 }` → `{ 2.61 2.92 3.15 }`
  (`/tmp/impl272-logs-r1/rerun-review_dispatch.log`, RAW exit 101).
- It is not this diff: `git diff --name-only 86e8579…HEAD` names no file of that path
  (`tests/review_dispatch.rs`, `src/mutation.rs`, `src/adapters.rs`, `src/process.rs` are all
  untouched), and the CLEAN BASE reds the same suite identically: temporary worktree at
  `86e8579c…`, own target dir, RAW exit 101, 21 passed / 2 failed,
  `assertion left == right failed: left: Some("adapter.timeout") right: Some("effect.review_timeout")`
  — byte-identical panics at `tests/review_dispatch.rs:542` and `:1498`
  (`/tmp/impl272-logs-r1/base-review_dispatch.log`).
- Run exactly as hosted CI runs it, it is GREEN: `python3 scripts/ci-test-driver.py --suite
  review_dispatch --per-suite-seconds 150` → RAW exit 0, `23 passed; 0 failed`, `survivors=0`
  (`/tmp/impl272-logs-r1/driver2-review_dispatch.log`; the first driver attempt reported
  `survivors=1` — a leaked fixture daemon of THIS round's own panicking pre-run, reaped, then
  `survivors=0`).
- The other 36 suites of each aggregate were green.

`just ci` is therefore red locally for that one local-only suite; the two suites this round owns are
green raw, and hosted CI is the authority for the aggregate.

### Head

Fix-round implementation head: `24efbb971f26460687956ed9d7137d1f6bca3515` (parent
`c896cf0bdbdd78d00045b0c03e75d74f2064b630`, the reviewed head; this section rides the commit after
it). Branch `impl-272-r1` (never force-pushed).

### Fix round 1 addendum — the aggregate's own reds, and the witness read race they exposed

The first `just ci` AT the pushed head surfaced a third red, in this round's own witness
(`--test queue_advance`, 14 passed / 1 failed, `tests/queue_advance.rs:3498`, `the driver drove the
review re-entry through the run's own control`): the witness had read the daemon's log and found no
`re-evaluated step r1` line. Ground truth from that very fixture:

    log on disk:  … 14:50:12Z dispatched step o1 (supervision.recollect.fix_delivery)
                  … 14:50:14Z re-evaluated step r1 through the run's own bounded control (supervision.reevaluation.next_round)
    audit:        exactly ONE mutate.run.reevaluate (evidence ev_207f6eb485db9f3f), one review round
                  (outcome.mutate.review_evidence ik_7ae472aec77970fe-r1-r2), then the tail

The behaviour was right: the log writer BATCHES its appends, so a single read races the append (the
file the panic dumped carried four startup events; the file on disk carries thirteen). The record of
the driver's own acts is now POLLED for with a bounded deadline (30 s) in
`tests/queue_advance.rs`; the assertions are unchanged and still require exactly those lines. In the
next aggregate the witness PASSED inside the parallel suite.

Aggregates and the pushed-head gates (lane-local target dir, `vm.loadavg` before/after):

| Run | Command | RAW exit | Load | Log |
|-----|---------|----------|------|-----|
| 3rd | `just ci` (pre-poll head `61a0d20`) | 101 | `{ 2.67 2.74 2.92 }` → `{ 2.71 2.71 2.84 }` | `/tmp/impl272-logs-r1/head-just-ci.log` — the witness' stale-log read, above |
| 4th | `just ci` (pushed head `aa90e30`) | 101 | `{ 2.88 2.75 2.84 }` → `{ 3.90 3.25 3.03 }` | `/tmp/impl272-logs-r1/head3-just-ci.log` |

The 4th aggregate reds at `tests/mutation_engine.rs` —
`a_post_verdict_commit_on_the_delivery_branch_is_never_consumed_by_the_merge`, 37 passed / 1 failed —
panicking inside its own fixture: `git ["init","-q","-b","staging"] failed: could not lock config file
…/seed/.git/config: fatal: could not set 'core.filemode' to 'true'`: the same parallel-suite fixture
race family this lane already recorded at the CLEAN BASE in round 1 (two other tests of that suite red
there with the identical class). Adjudicated the same way, not called a pass: isolated re-run
`cargo test --locked --test mutation_engine` → RAW exit 0, `38 passed; 0 failed`
(`/tmp/impl272-logs-r1/head3-mutation_engine-alone.log`, load `{ 3.76 3.25 3.03 }`).

Gates at the pushed head `aa90e3029ce342939b05ad705253f5be7d8fe200`:

| # | Command | RAW exit | Log |
|---|---------|----------|-----|
| 1 | `cargo test --locked --test run_control` | 0 (`29 passed; 0 failed`) | `/tmp/impl272-logs-r1/final-run_control.log` |
| 2 | `cargo test --locked --test queue_advance` | 0 (`15 passed; 0 failed`) | `/tmp/impl272-logs-r1/final-queue_advance.log` |
| 3 | `cargo clippy --locked --all-targets -- -D warnings` | 0 | `/tmp/impl272-logs-r1/final-clippy.log` |
| 4 | `cargo fmt --check` | 0 | `/tmp/impl272-logs-r1/final-fmt-check.log` |
| 5 | `cargo test --locked --lib` | 0 (`368 passed; 0 failed`) | `/tmp/impl272-logs-r1/final-lib.log` |
| 6 | `just ci` | 101 (see above: the mutation_engine parallel fixture race; all `queue_advance`/`run_control` tests green inside it) | `/tmp/impl272-logs-r1/head3-just-ci.log` |

The report's original §"Fix round 1" gate table (runs 1–2 of the aggregate) stands as recorded there;
these are the runs at the pushed head. The code tree of every gate row is `24efbb9` plus the
witness' poll (`aa90e30`); the two suite rows and rows 3–5 were re-measured after it.
