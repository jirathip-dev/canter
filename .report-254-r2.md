# .report-254-r2.md — canter #254's acceptance run: the recorded FAIL handoff is read from the response column, the disposition names its lane, and the run's own bounded re-evaluation is driven

Lane: `issue-254` — canter queue submission `qs_a0ee148741d4a8d4`, item
`jirathip-dev/canter#254`, run `run-f7c16df27ba370dd`, worktree scope
`worktrees/issues/254`. Pinned revision (the submission's `issue_revision`):
`b753fa3214eccc63456e2b66b43114d31689b230` — identical to the lane worktree
HEAD and to `origin/staging` at the time of writing. This PR's delta is this
report only: the issue's required fix is already implemented and merged at the
pinned revision, so the honest delivery record is what this lane re-verifies
and what the live store now shows. No production code is changed; `Refs #254`.

## 1. Preflight: the fix is contained at the pinned revision

- Delivery 1: PR #255 "The recorded FAIL handoff is read where the daemon
  persists it, and the run drives its own bounded re-evaluation (Refs #254)"
  — head branch `impl-254`, pushed head
  `8c504b10ca12f1285564723918fdf2fbd95ad708`, merged to `staging`
  2026-09-22T20:38:48Z as the squash merge commit
  `fe1ab5fc294c78c0d079a055785fa22690c3b6fd`.
- Delivery 2 (the #255 review's moved-head binding, Refs #256): PR #257 — head
  branch `impl-256`, pushed head
  `12404c936f438dfc64920a79ad26384b5954fc5b`, merged 2026-09-23T00:04:41Z as
  the squash merge commit `9bdff98dedab9526ebbdeaa9d05efe1447f5f17b`.
- `git merge-base --is-ancestor fe1ab5fc294c78c0d079a055785fa22690c3b6fd HEAD`
  — raw exit `0` (contained); the same read for
  `9bdff98dedab9526ebbdeaa9d05efe1447f5f17b` — raw exit `0`.
- `git log --oneline origin/staging..HEAD | wc -l` — `0` before this report's
  own commit (the branch carried nothing of its own).
- The fix surface is present and unweakened at HEAD, each cited line resolving
  in the pinned tree:
  - the reader is `newest_fix_round_of` (`src/state.rs:9946`): it takes
    `result.fix_round` from the apply row's outcome **or** its response — the
    document the daemon actually persists (`outcome.result` is `null` on apply
    rows) — and it still requires `schema == "hf-fix-round/v1"` plus all four
    named fields (`round`, `bound`, `lane`, `feature_head`);
  - the fix-round dispositions are the only classifiers for a recorded FAIL
    with a visible handoff: `codes::FIX_DISPATCHED` (`src/supervision.rs:423`,
    returned at `:1938`, class `waiting-workers`, detail = the recorded lane)
    and `codes::FIX_HEAD_MOVED` (`src/supervision.rs:430`, returned at `:1949`
    with `fix_round_head_moved_detail` at `:2187`, class `needs-attention`,
    detail = lane + both head prefixes); `codes::REVIEW_FAILED` survives only
    in the arm that has no handoff at all (`src/supervision.rs:1965`);
  - the driver's derivation `reevaluation_intent` (`src/supervision.rs:1268`)
    accepts the recorded FAIL's non-passing check and is bounded by
    `RUN_REEVALUATION_MAX` (`src/run_control.rs:90` = `3`, counted from the
    durable journal).
- The first lane's committed evidence (`.report-254.md` §6) named what it could
  not prove: a live reviewer leg to a written verdict, the effect's own lane
  re-binding, the second-granularity tie in the fixture, and — the item this
  round is about — that driving the re-evaluation immediately after the handoff
  "recomputes the recorded head and can come back failing … bounded by the
  control's own per-`(run, step)` bound (3 …), which then parks typed". §2/§3
  below are the live reads of the running installation.

## 2. AC1/AC2 live: the rows the daemon writes are the rows the reader reads

Read-only (`sqlite3 "file:…state.db?mode=ro"`; the shipped binary only for its
read commands), against the live store now:

```
response carries hf-fix-round/v1: 10        outcome carries hf-fix-round/v1: 0
apply rows spent: 820
```

i.e. every recorded fix-round handoff lives where the daemon persists it (the
`hf-rpc-response/v1` line in the apply row's RESPONSE column) and none lives in
`outcome.result` — the exact shape the reader was fixed to read. The two rows
the issue names are still there, unchanged:

```
ik_run-4a4d15a67be20eea-p6-245-1790092090  status spent
  outcome.result = null
  response.result.fix_round = {agent: "impl-245-r2", bound: 3, round: 1,
    feature_head: "e7bf162e0262b0c478125cb38b4d8ccfbf746e87",
    lane: "lane-fd0ec9179c1820d1", schema: "hf-fix-round/v1", …}
ik_run-1726c41ee1ff4ea1-p6-250-1790034610  status spent
  response.result.fix_round = {agent: "impl-250-r2", bound: 3, round: 1, …}
```

The classifier's live dispositions, read with
`canter supervision status --run … --json` (raw exit `0` each) — the class a
recorded FAIL reports is the fix round's own, never a bare
`supervision.review_failed`:

```
run-3c68e508443d62c0  needs-attention  supervision.fix_round_redispatch_exhausted
  detail: "lane-dba2f545998db2c9 (lane checkout gone and the re-dispatch of
  "p6-258" is exhausted: 0 refused dispatches (refusal.profile.binding)
  against the spent 3 bounded retries; the certified head
  dcb5c0ed5adeaa5f646aef38152f0a8d115edf71 still fails …)"
run-b57a1cabee4f3774  needs-attention  supervision.fix_round_redispatch_exhausted
  detail: "lane-4ec42db7053bbd3b (lane checkout gone and the re-dispatch of
  "p6-261" is exhausted: 0 refused dispatches (refusal.admission.cap_repository)
  against the spent 3 bounded retries; the certified head
  1c5764862d978b2b5a913b188ae022e99b4fe63a still fails …)"
```

Both details name the recorded fix-round lane and the certified head that still
fails — and the head each names is the `feature_head` its own recorded handoff
carries. Neither run (nor any run that recorded a review FAIL: `#250`, `#232`,
`#236`, `#245`, `#261`) reads `supervision.review_failed`; the four runs that
were invalidated read `supervision.invalidated`, and the two above read the fix
round's own exhausted re-dispatch. The two earlier live dispositions this
issue's own orchestrator evidence pass recorded —
`supervision.fix_round_dispatched` (naming `lane-fd0ec9179c1820d1`) and
`supervision.fix_round_head_moved` (lane + both head prefixes) — are the same
arm at `src/supervision.rs:1938`/`:1949`, taken before the #294 slice refined
the exhausted case.

## 3. AC3: the run's own bounded re-evaluation, driven — live rows

**A recorded FAIL drives the run's own recovery control (live).** For
`run-4a4d15a67be20eea` (issue #245 — the run this issue was filed about), the
durable audit trail after its recorded FAIL at `16:11:13Z` is its own
supervision deriving the bounded re-evaluation three times, each carrying the
same reason text and each re-dispatching the producer step it computed from:

```
1405|2026-09-22T20:39:17Z|mutate.run.reevaluate|run:run-4a4d15a67be20eea:p6-245
1406|2026-09-22T20:39:17Z|run.reevaluate|…:step:p6-245:evidence:ev_ad697422588c77f9:operator:supervision:reason:supervision derived this bounded re-evaluation of p6-245: the newest recorded review evidence carries a non-passing check, so its consumer is refused while the producer cannot be re-run; recompute the checks by their own producer at the same certified head (issue #243)
1411|2026-09-22T20:54:46Z|mutate.run.reevaluate|run:run-4a4d15a67be20eea:p6-245
1417|2026-09-22T21:04:52Z|mutate.run.reevaluate|run:run-4a4d15a67be20eea:p6-245
1422|2026-09-22T21:05:33Z|outcome.run.reevaluate|ik_run-4a4d15a67be20eea-p6-245-1790111092
```

No operator `run.*` control exists between those rows: the `operator:supervision`
field is the driver's own attribution, and the only other `run.*` row for this
run is its later release (below). Each re-evaluation re-dispatched the producer
step it computed from (`ik_…-p6-245-r2/-r3/-r4`, all `spent`; the review
verdicts they recorded kept coming back `fail` at the same certified head — the
control's documented semantics), and the run's frontier read
(`canter supervision status --run run-4a4d15a67be20eea --json`, raw exit `0`)
is `cursor.next_step = p7-245` (`merge`) — the same frontier the issue's own
pre-fix read recorded, with all four `p6-245` attempts succeeded as effect
runs. The run then sat parked; the only operator act on it is its release
(`audit seq 1533/1534`, 2026-09-23T23:17:09Z, reason: "parked 26h at p7-245 on
lane-fd0ec9179c1820d1 which no longer exists; issue #245 is CLOSED and its
delivery landed as e4661195f (PR #253). Nothing left to publish.").

**A live run advanced past a re-evaluated non-passing check (live).** For
`run-38bbb470709562a5` (issue #137), which reads `done`:

```
evidence 2026-09-26T01:54:22Z  verdict pass, carrying the FAILED check
  "hosted_ci_not_reverified_after_review_window_and_heavy_gates_cargo_check_lint_test_not_rerun_locally"
audit 2632 2026-09-26T01:55:15Z  mutate.run.reevaluate  run:run-38bbb470709562a5:p6-137
evidence 2026-09-26T02:21:18Z  recomputed, that check now passed
  ("hosted_ci_at_pinned_head_59_success_1_skipped_bot_check")
audit 2638 2026-09-26T02:21:48Z  mutate.run.reevaluate  run:run-38bbb470709562a5:p6-137
evidence 2026-09-26T02:53:06Z  all checks passed
driver log 2026-09-26T02:53:40Z  dispatched step p7-137
driver log 2026-09-26T02:53:58Z  dispatched step p8-137 → run done
```

The remaining half of AC3's own wording — a live **review FAIL** whose repair
leg delivers, whose delivered head is re-bound and re-reviewed to a PASS, and
whose cursor then crosses the FAIL — has not yet happened on this installation:
the runs that recorded a review FAIL had their re-evaluations recompute at the
same certified head (the control's documented semantics) and then parked typed
(#258/#261) or were invalidated/released (#245/#250/#232/#236). That live
sequence is the acceptance-spine re-run now in flight on this host — this run's
own family — and this lane records it as NOT yet witnessed rather than claiming
it. What this lane CAN and does re-verify deterministically is §4.

## 4. The #254 witnesses re-run at the pinned revision (raw exits)

`CARGO_TARGET_DIR` = a lane-local directory outside the worktree (the
`just security` gitleaks scan reads the working directory; a target dir inside
it false-positives on generated `cargo doc` search-index blobs).

| Command | Raw exit | Result |
| --- | --- | --- |
| `cargo test --locked --test run_control a_recorded_fail_handoff_is_named_and_drives_the_run_past_the_fail -- --nocapture` | `0` | `test result: ok. 1 passed; 0 failed` (0.38s) |
| `cargo test --locked --test run_control a_recorded_handoff_for_another_head_is_named_and_still_drives_the_run -- --nocapture` | `0` | `test result: ok. 1 passed; 0 failed` |
| `cargo test --locked --lib a_recorded_fix_round_is_read_back_from_the_runs_own_apply_row` | `0` | `test result: ok. 1 passed; 0 failed` |

Their printed reads (raw, `--nocapture`):

```
CLASSIFY class=waiting-workers reason=supervision.fix_round_dispatched detail=lane-0123456789abcdef eligible=false
READ BACK handoff round=1 bound=3 lane=lane-0123456789abcdef head=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
DRIVER step=p6 kind=review_evidence reason=supervision.reevaluation.next_round
CURSOR PAST THE FAIL step=p7 kind=merge reason=supervision.dispatch.next_step
CLASSIFY (moved) class=needs-attention reason=supervision.fix_round_head_moved
  detail=lane-0123456789abcdef (handoff recorded at cccccccccccc, newest evidence at aaaaaaaaaaaa) eligible=false
DRIVER (moved) step=p6 kind=review_evidence reason=supervision.reevaluation.next_round
```

Witness 1 (`tests/run_control.rs:3597`) asserts the classifier reports
`waiting-workers` / `supervision.fix_round_dispatched` with the lane as detail,
the handoff read back from the response column, the driver's ONE act = the
run's own recovery control on the producer (`p6`, never the tail), and — after
the producer's re-run records a pass for the same certified head —
`next_unachieved_step` = `p7` with the publish step dispatched. Witness 2
(`tests/run_control.rs:3793`) asserts the moved-head case is
`supervision.fix_round_head_moved` naming the lane and both heads AND still
drives the producer. The state leg (`src/state.rs:19209`) pins the response
column read, another run's row ignored, and a `v2` schema refused. The sibling
daemon-level witness `a_fix_rounds_delivered_head_is_re_bound_by_the_runs_own_machinery`
(`tests/queue_advance.rs:3125`) drives the same machinery through a real daemon
fixture.

Discrimination probe — does the fix bite at THIS revision? The response read in
`newest_fix_round_of` was replaced by the pre-fix outcome-only read (the literal
replace was asserted to match exactly once), the witnesses re-run, and the file
restored from a pre-mutation copy; the restored bytes' sha256 equals the
pre-mutation value and the worktree is clean again:

```
src/state.rs sha256 b109afd92ec0f8f32f3a242de97e9edb0d915a9c8d0e51d559bae79fdb740d49
  -> 7118f7d1532c679030e218d15517972a5e13a1dac1d47edebf411bbbd7666b87  (mutant: response-column read removed)
RED  cargo test …a_recorded_fail_handoff… -> exit 101
     CLASSIFY class=needs-attention reason=supervision.review_failed detail=p6 eligible=false   <- the issue's exact park
     panicked at tests/run_control.rs:3693: assertion `left == right` failed
RED  cargo test …a_recorded_fix_round_is_read_back_from_the_runs_own_apply_row… -> exit 101
     panicked at src/state.rs:19260
GREEN after restore -> exit 0 (same witness, `test result: ok. 1 passed`)
restored sha256 = b109afd92ec0f8f32f3a242de97e9edb0d915a9c8d0e51d559bae79fdb740d49  (equal to the pre-mutation value)
git status --porcelain: empty
```

No `git reset`, `git checkout` or `git stash` was used; the worktree was clean
before and after.

## 5. Gates

`just --list` was run once; the canonical recipe is `just ci`
(fmt-check → check → lint → test → doc → build-release → security).

| Command | Raw exit | Log |
| --- | --- | --- |
| `CARGO_TARGET_DIR=<lane-local, outside the worktree> just ci` at the content head (the tree this report is committed on top of) | `0` | `just-ci-content-head.log` — 50 suites, 952 passed, 0 failed; all seven recipes green; scanner self-test OK; public-tree scan OK; `cargo-deny` "advisories ok, bans ok, licenses ok, sources ok"; `cargo-audit` scanned 119 crate dependencies; `gitleaks … no leaks found`. Started 2026-09-26T10:29:59Z, raw exit recorded 10:44:19Z. |

The delivered tip differs from that gated content head ONLY by this report file,
so the tree-reading legs are re-run at the tip and their raw exits are recorded
in the PR body (the report cannot cite a run that happens after its own commit
without moving the head again). No gate was piped through `grep` as a pass/fail
test, and no flake adjudication was needed: the test leg was green on its first
invocation.

## 6. What was NOT verified

- The live review-FAIL → repair → re-reviewed PASS → advance sequence (AC3's
  own live half): §3 records what the live store shows and states plainly that
  the full sequence is not yet in it. It belongs to the acceptance-spine re-run
  in flight; this lane did not dispatch, release, re-evaluate or otherwise touch
  any run.
- Hosted CI on this PR head (the workflows run on the PR; the local aggregate is
  what this report records).
- The independent exact-head review verdict — the fleet's review leg is that
  gate.
- A live reviewer leg to a written verdict (a synthetic fixture owns no
  harness), the same disclosure the first lane's report carries.

## 7. Boundary

Every read above is read-only: SQLite opened `mode=ro`, the shipped binary used
for `status` reads only. No operator control (`run.dispatch`, `run.retry`,
`run.release`, `run.resolve`, `run.reevaluate`, `run.retire-lane`) was run by
this lane against any run; the runs, lanes and retry rows quoted are what their
own armed supervision recorded. Nothing was merged here and this branch carries
exactly one commit on top of the pinned revision.
